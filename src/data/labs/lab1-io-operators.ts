import type { LabProgram } from '../../types';

export const lab1IoOperators: LabProgram = {
  id: 'lab-1',
  number: 1,
  title: 'I/O, Operators and Control Structures',
  slug: 'io-operators-control',
  unitIds: ['unit-1'],
  objective:
    'Write C programs demonstrating input/output functions, all operator types, and control structure usage including switch-case, if-else ladders, nested loops, and conditional checks.',
  description:
    'This lab covers the foundational building blocks of every C program: reading input with scanf, producing output with printf, using arithmetic/relational/logical operators, and controlling program flow with if-else, switch-case, for loops, and while loops. You will write four complete programs that exercise these concepts in practical scenarios.',

  solution: [
    '/* ============================================================',
    ' * Program 1: Calculator using switch-case',
    ' * Compile: gcc -std=c11 -Wall calculator.c -o calculator',
    ' * ============================================================ */',
    '#include <stdio.h>',
    '',
    'int main(void) {',
    '    double a, b, result;',
    '    char op;',
    '',
    '    printf("Enter expression (e.g. 3 + 4): ");',
    '    if (scanf("%lf %c %lf", &a, &op, &b) != 3) {',
    '        printf("Invalid input.\\n");',
    '        return 1;',
    '    }',
    '',
    '    switch (op) {',
    '        case \'+\': result = a + b; break;',
    '        case \'-\': result = a - b; break;',
    '        case \'*\': result = a * b; break;',
    '        case \'/\':',
    '            if (b == 0) {',
    '                printf("Error: Division by zero.\\n");',
    '                return 1;',
    '            }',
    '            result = a / b;',
    '            break;',
    '        case \'%\':',
    '            if (b == 0) {',
    '                printf("Error: Modulo by zero.\\n");',
    '                return 1;',
    '            }',
    '            result = (int)a % (int)b;',
    '            break;',
    '        default:',
    '            printf("Unknown operator: %c\\n", op);',
    '            return 1;',
    '    }',
    '',
    '    printf("%.2lf %c %.2lf = %.2lf\\n", a, op, b, result);',
    '    return 0;',
    '}',
    '',
    '',
    '/* ============================================================',
    ' * Program 2: Grade classifier using if-else ladder',
    ' * Compile: gcc -std=c11 -Wall grade.c -o grade',
    ' * ============================================================ */',
    '#include <stdio.h>',
    '',
    'int main(void) {',
    '    int marks;',
    '',
    '    printf("Enter marks (0-100): ");',
    '    if (scanf("%d", &marks) != 1) {',
    '        printf("Invalid input.\\n");',
    '        return 1;',
    '    }',
    '',
    '    if (marks < 0 || marks > 100) {',
    '        printf("Marks out of range.\\n");',
    '        return 1;',
    '    }',
    '',
    '    char grade;',
    '    if (marks >= 90) {',
    '        grade = \'A\';',
    '    } else if (marks >= 80) {',
    '        grade = \'B\';',
    '    } else if (marks >= 70) {',
    '        grade = \'C\';',
    '    } else if (marks >= 60) {',
    '        grade = \'D\';',
    '    } else {',
    '        grade = \'F\';',
    '    }',
    '',
    '    printf("Marks: %d  =>  Grade: %c\\n", marks, grade);',
    '    return 0;',
    '}',
    '',
    '',
    '/* ============================================================',
    ' * Program 3: Right-triangle star pattern using nested loops',
    ' * Compile: gcc -std=c11 -Wall pattern.c -o pattern',
    ' * ============================================================ */',
    '#include <stdio.h>',
    '',
    'int main(void) {',
    '    int n;',
    '',
    '    printf("Enter number of rows: ");',
    '    if (scanf("%d", &n) != 1 || n <= 0) {',
    '        printf("Please enter a positive integer.\\n");',
    '        return 1;',
    '    }',
    '',
    '    for (int i = 1; i <= n; i++) {',
    '        for (int j = 1; j <= i; j++) {',
    '            printf("* ");',
    '        }',
    '        printf("\\n");',
    '    }',
    '',
    '    return 0;',
    '}',
    '',
    '',
    '/* ============================================================',
    ' * Program 4: Number properties checker',
    ' * Compile: gcc -std=c11 -Wall numcheck.c -o numcheck',
    ' * ============================================================ */',
    '#include <stdio.h>',
    '#include <stdbool.h>',
    '',
    'bool is_prime(int n) {',
    '    if (n <= 1) return false;',
    '    if (n <= 3) return true;',
    '    if (n % 2 == 0 || n % 3 == 0) return false;',
    '    for (int i = 5; i * i <= n; i += 6) {',
    '        if (n % i == 0 || n % (i + 2) == 0)',
    '            return false;',
    '    }',
    '    return true;',
    '}',
    '',
    'int main(void) {',
    '    int num;',
    '',
    '    printf("Enter an integer: ");',
    '    if (scanf("%d", &num) != 1) {',
    '        printf("Invalid input.\\n");',
    '        return 1;',
    '    }',
    '',
    '    /* Odd / Even check */',
    '    printf("%d is %s.\\n", num, (num % 2 == 0) ? "even" : "odd");',
    '',
    '    /* Positive / Negative / Zero */',
    '    if (num > 0)',
    '        printf("%d is positive.\\n", num);',
    '    else if (num < 0)',
    '        printf("%d is negative.\\n", num);',
    '    else',
    '        printf("The number is zero.\\n");',
    '',
    '    /* Prime check */',
    '    if (is_prime(num))',
    '        printf("%d is a prime number.\\n", num);',
    '    else',
    '        printf("%d is not a prime number.\\n", num);',
    '',
    '    return 0;',
    '}',
  ].join('\n'),

  solutionExplanation: [
    'Program 1 (Calculator) reads two operands and an operator character using a single scanf call with the format string "%lf %c %lf". The switch statement dispatches to the correct arithmetic operation. Note the division-by-zero guard before performing / and %. The modulo operator requires integer operands, so the doubles are cast to int.',
    '',
    'Program 2 (Grade Classifier) uses an if-else ladder to map a numeric score into a letter grade. The conditions are checked top-down: >= 90 first, then >= 80, and so on. Because each condition is checked only when all previous ones were false, we do not need to write compound conditions like marks >= 80 && marks < 90.',
    '',
    'Program 3 (Pattern Printer) uses two nested for loops. The outer loop controls the row number (1 through n). The inner loop prints exactly i stars on row i, producing a right-angled triangle.',
    '',
    'Program 4 (Number Properties) demonstrates three independent checks on the same number. The odd/even test uses the modulo operator (num % 2). The sign test uses relational operators. The prime check is extracted into a separate function for clarity; it uses trial division up to the square root of n, skipping multiples of 2 and 3 for efficiency.',
  ].join('\n'),

  vivaQuestions: [
    {
      id: 'lab1-vq1',
      question: 'What do the format specifiers %d, %f, %lf, %c, and %s mean in printf and scanf?',
      answer:
        '%d is for int, %f is for float (in printf it also prints double), %lf is for double in scanf, %c is for a single char, and %s is for a string (char array). In printf, both float and double use %f because float is promoted to double when passed to a variadic function.',
      followUp: 'What happens if you use %d to print a float variable?',
    },
    {
      id: 'lab1-vq2',
      question: 'What is the difference between the = operator and the == operator?',
      answer:
        '= is the assignment operator: it stores a value into a variable (e.g., x = 5 puts 5 into x). == is the equality comparison operator: it returns 1 (true) if both sides are equal, 0 (false) otherwise. A common bug is writing if (x = 5) instead of if (x == 5); the first assigns 5 to x and always evaluates to true.',
      followUp: 'Will if (x = 0) ever execute the if-body? Why or why not?',
    },
    {
      id: 'lab1-vq3',
      question: 'What is operator precedence, and why does it matter?',
      answer:
        'Operator precedence determines the order in which operators are evaluated in an expression. For example, in 2 + 3 * 4 the multiplication happens first (result 14, not 20) because * has higher precedence than +. When in doubt, use parentheses to make the intended order explicit.',
      followUp: 'What is the precedence relationship between && and ||?',
    },
    {
      id: 'lab1-vq4',
      question: 'What is switch fall-through and how do you prevent it?',
      answer:
        'In a switch statement, execution continues into the next case label after a match unless a break statement is encountered. This is called fall-through. For example, without break after case 1, entering 1 would also execute the code under case 2. Always use break at the end of each case to prevent unintended fall-through.',
      followUp: 'Can you give a situation where fall-through is used intentionally?',
    },
    {
      id: 'lab1-vq5',
      question: 'What is the difference between break and continue inside a loop?',
      answer:
        'break immediately exits the innermost loop entirely, and execution resumes after the loop. continue skips the rest of the current iteration and jumps back to the loop condition check (for a while/do-while) or to the increment expression (for a for loop).',
    },
    {
      id: 'lab1-vq6',
      question: 'What is the difference between a while loop and a do-while loop?',
      answer:
        'A while loop checks its condition before executing the body, so the body may execute zero times if the condition is initially false. A do-while loop executes the body first and then checks the condition, guaranteeing the body runs at least once.',
      followUp: 'When would you prefer do-while over while in practice?',
    },
    {
      id: 'lab1-vq7',
      question: 'What happens when you perform integer division in C, for example 7 / 2?',
      answer:
        'When both operands of the / operator are integers, C performs integer division: it truncates the decimal part and returns only the integer quotient. So 7 / 2 gives 3, not 3.5. To get a floating-point result, at least one operand must be a float or double, e.g. 7.0 / 2 gives 3.5.',
      followUp: 'What does the expression -7 / 2 evaluate to in C11?',
    },
    {
      id: 'lab1-vq8',
      question: 'How does the prime-checking algorithm work and why do we only test up to the square root of n?',
      answer:
        'A prime number has no divisors other than 1 and itself. We test divisibility starting from 2 up to the square root of n because if n = a * b and both a and b are greater than the square root of n, then a * b would be greater than n, which is a contradiction. So at least one factor must be less than or equal to the square root.',
      followUp: 'Why does the optimized version skip even numbers after checking 2?',
    },
    {
      id: 'lab1-vq9',
      question: 'Why should you always check the return value of scanf?',
      answer:
        'scanf returns the number of items successfully read. If the user enters invalid input (e.g., a letter when %d is expected), scanf returns fewer items than requested. Checking the return value lets you detect bad input and handle it gracefully instead of using uninitialized or garbage values.',
    },
    {
      id: 'lab1-vq10',
      question: 'What is the ternary operator and how does it differ from if-else?',
      answer:
        'The ternary operator condition ? expr1 : expr2 is a compact inline conditional expression. It evaluates to expr1 if the condition is true, or expr2 if false. Unlike if-else, which is a statement, the ternary operator is an expression and can be used inside printf, assignments, and other expressions. Example: printf("%s", x > 0 ? "positive" : "non-positive");',
    },
  ],

  gdbWalkthrough: {
    id: 'lab1-gdb',
    title: 'Debugging the Calculator with GDB',
    programFile: 'calculator.c',
    breakpoints: [1, 12, 18],
    watchedVariables: ['a', 'b', 'op', 'result'],
    steps: [
      {
        command: 'gcc -g -std=c11 -Wall calculator.c -o calculator',
        output: '(no output — compilation successful)',
        explanation:
          'Compile with the -g flag to include debug symbols. This lets GDB show source-level variable names and line numbers instead of raw memory addresses.',
      },
      {
        command: 'gdb ./calculator',
        output: 'GNU gdb ...\nReading symbols from ./calculator...done.',
        explanation:
          'Launch GDB with the compiled binary. GDB loads the symbol table and waits for your commands.',
      },
      {
        command: 'break main',
        output: 'Breakpoint 1 at 0x...: file calculator.c, line 4.',
        explanation:
          'Set a breakpoint at the start of main(). The program will pause here before executing any code inside main.',
      },
      {
        command: 'run',
        output: 'Starting program: ./calculator\nBreakpoint 1, main () at calculator.c:4',
        explanation:
          'Run the program. It pauses at the breakpoint we set in main(). No output appears yet because printf has not been reached.',
      },
      {
        command: 'next',
        output: 'Enter expression (e.g. 3 + 4):',
        explanation:
          'The next command executes one line. This executes the printf that shows the prompt. The program is now waiting at the scanf line.',
      },
      {
        command: 'next',
        output: '10 + 5',
        explanation:
          'Stepping over scanf causes it to wait for input. We type "10 + 5" and press Enter. scanf reads the three values into a, op, and b.',
      },
      {
        command: 'print a',
        output: '$1 = 10',
        explanation:
          'Inspect the variable a. It holds 10.0 as expected — scanf successfully parsed the first number.',
      },
      {
        command: 'print op',
        output: "$2 = 43 '+'",
        explanation:
          'Inspect the operator variable. GDB shows both the integer value (43, which is the ASCII code for +) and the character representation.',
      },
      {
        command: 'print b',
        output: '$3 = 5',
        explanation:
          'Inspect the variable b. It holds 5.0. All three inputs are correctly parsed.',
      },
      {
        command: 'break 18',
        output: 'Breakpoint 2 at 0x...: file calculator.c, line 18.',
        explanation:
          'Set a breakpoint at the switch statement so we can see which case is selected.',
      },
      {
        command: 'continue',
        output: 'Breakpoint 2, main () at calculator.c:18',
        explanation:
          'Continue execution until the next breakpoint. The program is now at the switch statement.',
      },
      {
        command: 'step',
        output: 'calculator.c:19: case \'+\': result = a + b; break;',
        explanation:
          'Step into the switch. Since op is +, execution enters the + case. GDB shows us landing on the addition line.',
      },
      {
        command: 'next',
        output: '',
        explanation:
          'Execute the addition and the break statement. The result is now calculated.',
      },
      {
        command: 'print result',
        output: '$4 = 15',
        explanation:
          'Verify the result: 10 + 5 = 15. The calculation is correct.',
      },
      {
        command: 'continue',
        output: '10.00 + 5.00 = 15.00\n[Inferior 1 (process ...) exited normally]',
        explanation:
          'Let the program finish. It prints the formatted result and exits with code 0 (success).',
      },
    ],
  },

  relatedTopicIds: ['u1-t7', 'u1-t8', 'u1-t9'],
};
