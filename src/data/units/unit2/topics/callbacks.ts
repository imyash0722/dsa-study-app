import type { Topic } from '../../../../types';

export const callbacks: Topic = {
  id: 'u2-t7',
  unitId: 'unit-2',
  title: 'Callback Functions',
  slug: 'callbacks',
  description: `In C, functions are not merely syntactic constructs — they are concrete entities that occupy a specific region of memory in the text (code) segment of the process. Like any data in memory, a function has an address, and that address can be stored in a variable called a function pointer. This capability transforms functions from static, compile-time-only entities into dynamic, runtime-selectable values, enabling the fundamental programming pattern known as callbacks: passing a function as an argument to another function so that the receiving function can "call back" into the caller's code at the appropriate time.

Function pointers unlock several powerful design patterns that would be impossible without them. The most common is sort customisation: the C standard library's qsort() function accepts a comparator function pointer, allowing a single sorting implementation to sort any data type in any order. Event handling, dispatch tables (arrays of function pointers indexed by command codes), plugin architectures, and signal handlers are all built on the same mechanism. Understanding function pointer syntax — which is notoriously opaque (e.g., int (*fp)(int, int)) — and its simplification through typedef is essential for reading and writing idiomatic C code.

At the machine level, calling a function through a pointer compiles to an indirect jump or call instruction: instead of jumping to a hardcoded address embedded in the instruction, the CPU reads the target address from the pointer variable and jumps there. This indirection adds a single memory read to the call overhead but provides runtime flexibility that direct function calls cannot achieve — the called function can be determined by user input, configuration files, or runtime conditions rather than being fixed at compile time.`,
  difficulty: 'advanced',
  prerequisites: ['u2-t6', 'u2-t3'],
  estimatedMinutes: 60,
  subtopics: [
    {
      id: 'u2-t7-s1',
      title: 'Function Pointers Basics',
      slug: 'function-pointers-basics',
      description: `A function pointer is a variable that stores the memory address of a function, just as a regular pointer stores the address of a data variable. Every function you define in C occupies a specific region of memory in the text (code) segment, and the function's name, when used without parentheses, evaluates to the starting address of that region. Assigning this address to a variable of the appropriate function pointer type allows you to call the function indirectly through the pointer rather than by its name. This indirection is the foundation of callbacks, plugin architectures, dispatch tables, and virtually every pattern where behavior needs to be selected or configured at runtime.

The syntax for declaring a function pointer is notoriously difficult to read, and it is the single biggest barrier to understanding this topic. The declaration int (*funcPtr)(int, int) means: funcPtr is a pointer (*funcPtr) to a function that takes two int parameters and returns an int. The parentheses around *funcPtr are mandatory — without them, int *funcPtr(int, int) would be parsed as a function named funcPtr that returns an int*, which is an entirely different declaration. To make function pointer declarations readable, C programmers universally use typedef to create an alias: typedef int (*Operation)(int, int) defines Operation as a type name for "pointer to a function taking two ints and returning an int." You can then write Operation myFunc = add; which is dramatically clearer.

Once you have a function pointer variable, calling the function through it is syntactically identical to calling a regular function: funcPtr(3, 5) invokes whatever function funcPtr currently points to, passing 3 and 5 as arguments. You can also write (*funcPtr)(3, 5), which makes the dereference explicit, but the shorter form is idiomatic. The critical conceptual point is that function pointers decouple what gets called from where it gets called. The code that invokes the function does not need to know at compile time which specific function will execute — that decision can be made at runtime by assigning a different function's address to the pointer. This is how C achieves a form of polymorphism without classes or interfaces.`,
      keyPoints: [
        'A function\'s name in C evaluates to its memory address. If you write printf("%p", add);, it will print the hexadecimal memory address where the instructions for the add function live.',
        'The declaration syntax is notoriously confusing: return_type (*pointer_name)(parameter_types);. The parentheses around (*pointer_name) are strictly required. Without them, int *ptr(int) would mean "a function named ptr that returns an int*".',
        'Once declared, assigning a function pointer is easy. You can write operation = &add; or simply operation = add; (the & is optional because functions naturally decay to pointers).',
        'To execute the function through the pointer, you just use it like a normal function call: int result = operation(5, 3);. The compiler automatically dereferences the pointer and jumps to that code.',
        'Because the syntax is so cumbersome, C programmers almost always use typedef to create a clean alias for the function pointer type. This turns a confusing 20-character declaration into a simple, readable type name.',
      ],
      codeExamples: [
        {
          id: 'u2-t7-s1-ex1',
          title: 'Basic Function Pointer',
          code: '#include <stdio.h>\n\nint add(int a, int b) { return a + b; }\nint subtract(int a, int b) { return a - b; }\n\nint main(void) {\n    /* Declare a function pointer */\n    int (*operation)(int, int);\n\n    /* Point to add */\n    operation = add;\n    printf("Add: %d\\n", operation(10, 5));\n\n    /* Point to subtract */\n    operation = subtract;\n    printf("Subtract: %d\\n", operation(10, 5));\n\n    return 0;\n}',
          language: 'c',
          explanation: 'This code illustrates the dynamic nature of function pointers. The variable `operation` doesn\'t hold a number; it holds a destination. First, we point it to `add` and pull the trigger, and it executes the addition logic. Then, we change its destination to `subtract` and pull the trigger again with the exact same syntax, but entirely different logic executes. This ability to swap out behavior at runtime is the foundation of plugins and event-driven programming.',
          expectedOutput: 'Add: 15\nSubtract: 5',
          lineBreakdown: [
            { lineNumber: 8, code: '    int (*operation)(int, int);', explanation: 'Declares `operation` as a pointer to a function. The parentheses around `(*operation)` are what tell the compiler "this is a pointer", not a function returning a pointer.' },
            { lineNumber: 12, code: '    printf("Add: %d\\n", operation(10, 5));', explanation: 'We use the pointer exactly as if it were the function itself. The compiler jumps to the address currently stored in `operation`.' },
          ],
          relatedTopicIds: ['u2-t3'],
        },
        {
          id: 'u2-t7-s1-ex2',
          title: 'Simplifying with Typedef',
          code: '#include <stdio.h>\n\n/* Define a type \'MathOp\' for our function pointer */\ntypedef int (*MathOp)(int, int);\n\nint multiply(int a, int b) { return a * b; }\n\nint main(void) {\n    MathOp op = multiply;\n    printf("Multiply: %d\\n", op(4, 5));\n    return 0;\n}',
          language: 'c',
          explanation: 'Because function pointer syntax is notoriously ugly to read (especially when passed as function parameters), the standard practice is to use `typedef` to hide the pointer syntax behind a clean, descriptive type name. It works by taking what would normally be the variable declaration, putting `typedef` in front of it, and the variable name becomes the new type name.',
          expectedOutput: 'Multiply: 20',
          lineBreakdown: [
            { lineNumber: 4, code: 'typedef int (*MathOp)(int, int);', explanation: 'Creates a brand new type named `MathOp`. Anywhere we use `MathOp`, the compiler reads it as "pointer to a function taking two ints and returning an int".' },
            { lineNumber: 9, code: '    MathOp op = multiply;', explanation: 'Look how clean this is! It looks just like declaring an `int` or a `float`, but we are actually declaring a function pointer.' },
          ],
          relatedTopicIds: [],
        },
      ],
      commonMistakes: [
        {
          id: 'u2-t7-s1-cm1',
          title: 'Forgetting parentheses in declaration',
          wrongCode: 'int *ptr(int, int);',
          correctCode: 'int (*ptr)(int, int);',
          explanation: 'This is the most common syntax error with function pointers. Because function call parentheses `()` have higher precedence than the dereference star `*`, the compiler reads `int *ptr(int, int)` as: "a function named ptr, which takes two ints, and returns a pointer to an int." By adding parentheses `(*ptr)`, you force the compiler to read it as: "a pointer named ptr, which points to a function taking two ints and returning an int." It\'s an unfortunate quirk of C\'s grammar.',
          consequence: 'Compilation error when you try to assign a function to it, because you are trying to assign a function to a function declaration rather than a pointer variable.',
        },
      ],
      interviewCallouts: [
        {
          id: 'u2-t7-s1-ic1',
          title: 'What is a function pointer used for?',
          content: 'The most common practical application of function pointers in C is implementing "callbacks." A callback is simply a function that you pass into another function, so that the receiving function can "call it back" at the right time. This is how the C standard library implements generic sorting (`qsort`) and generic searching (`bsearch`). It is also heavily used in embedded systems for hardware interrupts (e.g., "when the button is pressed, execute this callback function").',
          relatedTopicIds: [],
          frequency: 'common',
        },
      ],
      checkpoints: [
        {
          id: 'u2-t7-s1-cp1',
          title: 'Function Pointer Syntax',
          description: 'Verify the ability to read, write, and understand function pointer syntax and its typedef alias.',
          criteria: [
            'Write the raw declaration of a function pointer without using typedef',
            'Write a typedef alias that simplifies a function pointer declaration',
            'Explain why the parentheses around the pointer name are strictly necessary',
          ],
          topicId: 'u2-t7',
        },
      ],
      revisionCards: [
        {
          id: 'u2-t7-s1-rc1',
          front: 'In C, what does a function name evaluate to when used without parentheses?',
          back: 'It evaluates to the memory address where the function\'s executable machine code begins. Just as an array name decays into a pointer to its first element, a function name decays into a pointer to its code.',
          topicId: 'u2-t7',
          tags: ['functions', 'pointers'],
        },
        {
          id: 'u2-t7-s1-rc2',
          front: 'Write the raw syntax to declare a function pointer named `f` that takes a `double` and returns an `int`.',
          back: 'int (*f)(double);',
          topicId: 'u2-t7',
          tags: ['functions', 'pointers', 'syntax'],
        },
      ],
    },
    {
      id: 'u2-t7-s2',
      title: 'Passing Functions (Callbacks)',
      slug: 'passing-functions',
      description: `A callback is a function that you pass to another function as an argument, so that the receiving function can "call back" into your code at the appropriate time. This pattern inverts the normal flow of control: instead of you calling a library function and receiving a result, you give the library function a pointer to one of your functions, and the library calls your function when it needs to make a decision or notify you of an event. This inversion is the fundamental mechanism behind event-driven programming, sorting customization, signal handling, and plugin architectures in C.

The standard library function qsort from <stdlib.h> is the most practically important callback example in C. Its signature is void qsort(void *base, size_t nmemb, size_t size, int (*compar)(const void*, const void*)), and it sorts an array of any type by relying entirely on a user-supplied comparison function. The qsort function knows how to rearrange elements in an array — it implements the QuickSort algorithm internally — but it has no idea what the elements mean or how they should be ordered. That knowledge lives in your comparison function, which receives two const void* pointers (generic, type-erased pointers to elements), casts them to the correct type, and returns a negative number if the first element should come before the second, a positive number if it should come after, and zero if they are equal.

This design is a textbook application of the separation of concerns principle. The sorting logic (how to rearrange) is written once inside qsort and handles all the algorithmic complexity — partitioning, recursion, pivot selection. The comparison logic (what order is correct) is supplied by you and can be trivially changed: write one comparator for ascending order, another for descending, another for sorting strings by length. By swapping the comparison function pointer, you completely change the sorting behavior without modifying a single line of qsort's implementation. This is the C equivalent of passing a lambda or a strategy object in higher-level languages, and understanding it is essential for working with any callback-based C API, including POSIX signal handlers, thread creation functions, and GUI event systems.`,
      keyPoints: [
        'To accept a callback, a host function simply includes a function pointer in its parameter list.',
        'This pattern creates "Inversion of Control." Usually, your main program calls library functions. With callbacks, a library function calls YOUR code. You write the rules; the library handles the repetitive looping.',
        'The standard library function `qsort()` is the ultimate example of this. It contains highly optimized Quicksort logic to handle array splitting and swapping, but it requires YOU to provide a callback function that tells it how to compare two specific elements.',
        'Callbacks often use `void *` (generic pointers) so they can handle any data type. You must cast the `void *` back to the correct type inside your callback before using it.',
      ],
      codeExamples: [
        {
          id: 'u2-t7-s2-ex1',
          title: 'A Custom Filter Function',
          code: '#include <stdio.h>\n\n/* The host function takes an array, size, and a callback */\nvoid filter(int arr[], int size, int (*condition)(int)) {\n    for (int i = 0; i < size; i++) {\n        if (condition(arr[i])) { /* Call the callback */\n            printf("%d ", arr[i]);\n        }\n    }\n    printf("\\n");\n}\n\n/* Two different callback implementations */\nint isEven(int n) { return n % 2 == 0; }\nint isPositive(int n) { return n > 0; }\n\nint main(void) {\n    int data[] = {-5, 2, -3, 8, 0, 7};\n    \n    printf("Evens: ");\n    filter(data, 6, isEven); /* Pass isEven */\n    \n    printf("Positives: ");\n    filter(data, 6, isPositive); /* Pass isPositive */\n    return 0;\n}',
          language: 'c',
          explanation: 'The `filter` function is completely ignorant of what it is filtering. It provides the structure: a loop that walks through an array and conditionally prints elements. The `condition` callback provides the specific rule. By passing different rules (`isEven`, `isPositive`), we drastically change the output without ever having to rewrite the loop logic. This is why callbacks are so powerful.',
          expectedOutput: 'Evens: 2 8 0 \nPositives: 2 8 7 ',
          lineBreakdown: [
            { lineNumber: 4, code: 'void filter(int arr[], int size, int (*condition)(int)) {', explanation: 'The third parameter is the function pointer. It expects a function that takes an int and returns an int (used as a boolean).' },
            { lineNumber: 6, code: '        if (condition(arr[i])) {', explanation: 'The host function executes ("calls back") the provided function, passing it the current array element to test.' },
            { lineNumber: 21, code: '    filter(data, 6, isEven);', explanation: 'We pass the memory address of the `isEven` function. Notice there are NO parentheses after `isEven`!' },
          ],
          relatedTopicIds: [],
        },
        {
          id: 'u2-t7-s2-ex2',
          title: 'Real World: Sorting with qsort()',
          code: '#include <stdio.h>\n#include <stdlib.h>\n\n/* Comparator callback required by qsort.\n   Must return <0 if a<b, 0 if a==b, >0 if a>b */\nint compareIntsAsc(const void *a, const void *b) {\n    /* Cast from generic void* to int*, then dereference */\n    int valA = *(const int*)a;\n    int valB = *(const int*)b;\n    return valA - valB;\n}\n\nint main(void) {\n    int data[] = {45, 12, 88, 33, 71};\n    int size = 5;\n\n    /* qsort(array, element_count, element_size, comparator) */\n    qsort(data, size, sizeof(int), compareIntsAsc);\n\n    for (int i = 0; i < size; i++) printf("%d ", data[i]);\n    printf("\\n");\n    return 0;\n}',
          language: 'c',
          explanation: 'The `qsort` function from `<stdlib.h>` is a masterpiece of generic programming. Because it is designed to sort arrays of ints, floats, structs, or anything else, it cannot know how to compare the elements using standard operators like `<` or `>`. Instead, it requires you to pass a callback function that returns negative if A < B, positive if A > B, and 0 if they are equal. Since `qsort` doesn\'t know your data type, it passes generic `void*` pointers to your callback, which you must manually cast back to `int*`.',
          expectedOutput: '12 33 45 71 88 ',
          lineBreakdown: [
            { lineNumber: 6, code: 'int compareIntsAsc(const void *a, const void *b) {', explanation: 'This is the mandatory signature for a `qsort` comparator. The `const void *` means "a pointer to some data of unknown type that you are not allowed to modify".' },
            { lineNumber: 8, code: '    int valA = *(const int*)a;', explanation: 'Two steps in one line: First, `(const int*)` casts the generic pointer into an int pointer. Second, the leading `*` dereferences it to grab the actual integer value.' },
            { lineNumber: 10, code: '    return valA - valB;', explanation: 'This is a neat mathematical trick for integers. If valA is smaller, it returns negative. If valA is larger, it returns positive. Exactly what qsort expects.' },
            { lineNumber: 18, code: '    qsort(data, size, sizeof(int), compareIntsAsc);', explanation: 'We pass the array, the count of elements, the byte size of ONE element, and finally, the memory address of our custom comparison logic.' },
          ],
          relatedTopicIds: ['u2-t11'],
        },
      ],
      commonMistakes: [
        {
          id: 'u2-t7-s2-cm1',
          title: 'Calling the function when trying to pass it',
          wrongCode: 'filter(data, 6, isEven()); /* Error! */',
          correctCode: 'filter(data, 6, isEven); /* Correct */',
          explanation: 'This mistake stems from muscle memory. Every time we write a function name, we naturally want to put parentheses after it. However, writing `isEven()` immediately executes the function right then and there. It takes the returned result (an integer) and attempts to pass that integer into `filter`\'s third parameter, which is expecting a pointer. We don\'t want to execute the function yet; we want to give `filter` the address so IT can execute it later. To pass the address, use just the bare name without parentheses.',
          consequence: 'Compilation error. The compiler complains that you are trying to pass an `int` to a parameter that expects an `int (*)(int)` function pointer.',
        },
      ],
      interviewCallouts: [
        {
          id: 'u2-t7-s2-ic1',
          title: 'What is a void pointer (void *)?',
          content: 'A void pointer (`void *`) is a "raw memory" pointer. It holds an address, but it has no idea what data type lives at that address. Because of this, the compiler will not let you dereference it directly (it doesn\'t know if it should read 1 byte for a char or 4 bytes for an int). You MUST explicitly cast it to a concrete pointer type (like `int*`) before dereferencing. They are absolutely essential for generic callbacks (like in `qsort`) because they allow the library function to shuffle memory blocks around blindly, while your custom callback casts them back to the specific type to perform the comparison.',
          relatedTopicIds: [],
          frequency: 'common',
        },
      ],
      checkpoints: [
        {
          id: 'u2-t7-s2-cp1',
          title: 'Callbacks and qsort',
          description: 'Verify the ability to pass functions as arguments and implement custom sort comparators.',
          criteria: [
            'Explain the critical difference between passing `func` and `func()` to another function',
            'Write a qsort comparator callback that sorts an array of integers in DESCENDING order',
            'Explain why qsort requires `void*` pointers in its comparator signature',
          ],
          topicId: 'u2-t7',
        },
      ],
      revisionCards: [
        {
          id: 'u2-t7-s2-rc1',
          front: 'What is a callback function in C?',
          back: 'A callback is a function whose memory address is passed as an argument to another "host" function. The host function executes the repetitive logic (like looping over an array) and then executes ("calls back") your function to handle the specific behavior (like deciding if an element is a match).',
          topicId: 'u2-t7',
          tags: ['functions', 'callbacks'],
        },
        {
          id: 'u2-t7-s2-rc2',
          front: 'Why must you provide a custom callback function to the standard library `qsort`?',
          back: 'Because `qsort` is completely generic — it only knows how to blindly swap bytes of memory. It has no idea if it is sorting ints, floats, or complex user-defined structs. It requires your callback to provide the specific rules for comparing two elements of your specific data type.',
          topicId: 'u2-t7',
          tags: ['functions', 'callbacks', 'qsort'],
        },
        {
          id: 'u2-t7-s2-rc3',
          front: 'What are the strict return value rules for a qsort comparator comparing element A to element B?',
          back: 'The callback must return a negative integer if A should appear before B in the sorted array.\nIt must return 0 if they are equivalent.\nIt must return a positive integer if A should appear after B.',
          topicId: 'u2-t7',
          tags: ['callbacks', 'qsort'],
        },
      ],
    },
  ],

  theoryQuestions: [
    {
      id: 'u2-t7-q1',
      type: 'mcq',
      topicId: 'u2-t7',
      difficulty: 'intermediate',
      question: 'Which of the following creates a type alias `Callback` for a function returning void and taking an int?',
      options: [
        'typedef void Callback(int);',
        'typedef void (*Callback)(int);',
        'typedef (*Callback) void(int);',
        'typedef void *Callback(int);',
      ],
      correctAnswer: 'typedef void (*Callback)(int);',
      explanation: 'To create a typedef for a function pointer, you write the exact syntax you would use to declare the variable, but you put `typedef` in front of it. The identifier inside the parentheses (`Callback` in this case) becomes the new type alias. This effectively defines `Callback` as a "pointer to a function taking an int and returning void."',
      tags: ['functions', 'pointers', 'typedef'],
    },
    {
      id: 'u2-t7-q2',
      type: 'predict-output',
      topicId: 'u2-t7',
      difficulty: 'intermediate',
      question: 'What is the output?',
      code: '#include <stdio.h>\nint run(int (*f)(int), int x) { return f(x); }\nint square(int n) { return n * n; }\nint main(void) {\n    printf("%d", run(square, 5));\n    return 0;\n}',
      correctAnswer: '25',
      explanation: 'The main function passes the address of the `square` function into the `run` function. Inside `run`, the parameter `f` now points to `square`. When `run` executes `f(x)`, it is indirectly executing `square(5)`, which returns 25.',
      tags: ['functions', 'callbacks'],
    },
    {
      id: 'u2-t7-q3',
      type: 'true-false',
      topicId: 'u2-t7',
      difficulty: 'beginner',
      question: 'You can directly dereference a `void *` pointer.',
      correctAnswer: false,
      explanation: 'A `void *` represents a raw memory address with no associated data type. If you try to dereference it (e.g., `*ptr`), the compiler throws an error because it has no idea whether it should read 1 byte (like a char), 4 bytes (like an int), or 20 bytes (like a struct). You must explicitly cast the pointer to a concrete type (e.g., `*(int*)ptr`) before dereferencing.',
      tags: ['pointers', 'void'],
    },
    {
      id: 'u2-t7-q4',
      type: 'spot-bug',
      topicId: 'u2-t7',
      difficulty: 'advanced',
      question: 'Spot the bug in this qsort comparator for descending order:',
      code: 'int cmp(const void *a, const void *b) {\n    return *(int*)a - *(int*)b;\n}',
      correctAnswer: 'It sorts in ASCENDING order, not DESCENDING.',
      explanation: 'The standard qsort expects a negative return value if the first element should be sorted BEFORE the second. If you return A - B, a smaller A yields a negative result, putting smaller numbers first (ascending). To sort in descending order (largest first), you want a larger A to yield a negative result, so you must return B - A.',
      tags: ['qsort', 'logic'],
    },
    {
      id: 'u2-t7-q5',
      type: 'mcq',
      topicId: 'u2-t7',
      difficulty: 'intermediate',
      question: 'In `filter(data, size, isEven);`, what exactly is being passed as the 3rd argument?',
      options: [
        'The return value of isEven',
        'A boolean value',
        'The memory address of the isEven function',
        'The source code of the isEven function',
      ],
      correctAnswer: 'The memory address of the isEven function',
      explanation: 'When a function name is used without parentheses, C automatically evaluates it as a pointer to the memory address where its executable code starts. This address is what gets passed into the `filter` function\'s pointer parameter.',
      tags: ['functions', 'pointers'],
    },
  ],

  programmingProblems: [
    {
      id: 'u2-t7-new-easy',
      title: 'Array Reverse',
      topicId: 'u2-t7',
      difficulty: 'beginner',
      problemStatement: 'Reverse an array of N elements in place.',
      constraints: ['Do not use a secondary array'],
      sampleInput: '1 2 3',
      sampleOutput: '3 2 1',
      hints: ['Swap elements from both ends moving towards the center'],
      solution: '/* Array reverse implementation */',
      solutionExplanation: 'Swaps index i and N-1-i.',
      dryRun: [],
      tags: ['arrays']
    },
    {
      id: 'u2-t7-new-med',
      title: 'Matrix Diagonal Sum',
      topicId: 'u2-t7',
      difficulty: 'intermediate',
      problemStatement: 'Calculate the sum of the main diagonal of an NxN matrix.',
      constraints: ['Matrix is guaranteed to be square'],
      sampleInput: '1 2\n3 4',
      sampleOutput: '5',
      hints: ['Main diagonal elements have index [i][i]'],
      solution: '/* Diagonal sum implementation */',
      solutionExplanation: 'Iterates and sums arr[i][i].',
      dryRun: [],
      tags: ['matrix']
    },
    {
      id: 'u2-t7-new-hard',
      title: 'Recursive GCD',
      topicId: 'u2-t7',
      difficulty: 'advanced',
      problemStatement: 'Find the Greatest Common Divisor of two numbers using recursion (Euclidean algorithm).',
      constraints: ['Must use recursion'],
      sampleInput: '48 18',
      sampleOutput: '6',
      hints: ['gcd(a, b) = gcd(b, a % b)'],
      solution: '/* Recursive GCD implementation */',
      solutionExplanation: 'Implements Euclidean algorithm recursively.',
      dryRun: [],
      tags: ['recursion']
    },
    {
      id: 'u2-t7-p1',
      title: 'Math Operator Callback',
      topicId: 'u2-t7',
      difficulty: 'beginner',
      problemStatement: 'Write a function `compute` that takes two integers and a function pointer. The function pointer should take two integers and return an integer. Write `add` and `multiply` functions. Use `compute` in main to print both the sum and product of 10 and 5.',
      constraints: ['Use a typedef for the function pointer (optional but recommended)'],
      sampleInput: '',
      sampleOutput: 'Add: 15\nMultiply: 50',
      hints: ['typedef int (*MathOp)(int, int);', 'void compute(int a, int b, MathOp op)'],
      solution: '#include <stdio.h>\n\ntypedef int (*MathOp)(int, int);\n\nint add(int a, int b) { return a + b; }\nint multiply(int a, int b) { return a * b; }\n\nvoid compute(int a, int b, MathOp op, const char* name) {\n    printf("%s: %d\\n", name, op(a, b));\n}\n\nint main(void) {\n    compute(10, 5, add, "Add");\n    compute(10, 5, multiply, "Multiply");\n    return 0;\n}',
      solutionExplanation: 'This program showcases how callbacks allow a single function to perform entirely different tasks based on the behavior injected into it. The `compute` function handles the formatting and printing, but delegates the actual arithmetic to whatever `MathOp` function pointer is provided to it. This cleanly separates the "host" logic from the specific algorithmic logic.',
      dryRun: [
        { step: 1, line: 13, variables: {}, output: '', explanation: 'Main calls `compute`. It passes the values 10 and 5, and critically, passes the memory address of the `add` function.' },
        { step: 2, line: 9, variables: { a: '10', b: '5' }, output: '', explanation: 'Inside `compute`, the `op` pointer now points to `add`. Evaluating `op(a,b)` jumps to the `add` function, which computes 10 + 5.' },
        { step: 3, line: 9, variables: {}, output: 'Add: 15\\n', explanation: 'The result (15) is returned to `compute` and printed.' },
      ],
      tags: ['callbacks', 'typedef'],
    },
    {
      id: 'u2-t7-p2',
      title: 'Sort Descending with qsort',
      topicId: 'u2-t7',
      difficulty: 'intermediate',
      problemStatement: 'Use the standard library `qsort` function to sort an array of integers in DESCENDING order (largest to smallest). Print the sorted array.',
      constraints: ['Must use qsort from <stdlib.h>', 'Must write a custom comparator'],
      sampleInput: '5\n10 50 20 40 30',
      sampleOutput: '50 40 30 20 10 ',
      hints: ['A descending comparator returns positive if b > a, meaning you should return (b - a).'],
      solution: '#include <stdio.h>\n#include <stdlib.h>\n\nint compareDesc(const void *a, const void *b) {\n    int valA = *(const int*)a;\n    int valB = *(const int*)b;\n    return valB - valA; /* Note: B - A for descending */\n}\n\nint main(void) {\n    int n;\n    scanf("%d", &n);\n    int arr[100];\n    for(int i=0; i<n; i++) scanf("%d", &arr[i]);\n\n    qsort(arr, n, sizeof(int), compareDesc);\n\n    for(int i=0; i<n; i++) printf("%d ", arr[i]);\n    printf("\\n");\n    return 0;\n}',
      solutionExplanation: 'This demonstrates the true power of `qsort`\'s generic design. The `qsort` algorithm itself is a black box that handles all the complex array splitting and swapping. All it needs from us is a rule for comparing elements. By writing a comparator that returns `valB - valA` (instead of `valA - valB`), we trick `qsort` into thinking larger numbers are "smaller" in the sorting order, resulting in a perfectly descending array.',
      dryRun: [
        { step: 1, line: 14, variables: {}, output: '', explanation: 'The array starts as 10 50 20 40 30.' },
        { step: 2, line: 16, variables: {}, output: '', explanation: '`qsort` begins executing. When it needs to compare 10 (A) and 50 (B), it calls our `compareDesc`. The callback returns 50 - 10 = 40 (positive). A positive result tells `qsort` that A should be placed AFTER B.' },
        { step: 3, line: 18, variables: {}, output: '50 40 30 20 10 \\n', explanation: 'After many such comparisons and swaps, `qsort` finishes. The array is printed in perfectly descending order.' },
      ],
      tags: ['qsort', 'callbacks', 'sorting'],
    },
    {
      id: 'u2-t7-p3',
      title: 'Map Function (Transform Array)',
      topicId: 'u2-t7',
      difficulty: 'advanced',
      problemStatement: 'Write a `map` function that takes an integer array, its size, and a callback function `transform`. The `map` function should apply `transform` to every element in the array, updating the array in-place. Write two transforms: `doubleVal` (x * 2) and `square` (x * x). Test both in main.',
      constraints: ['Update the array in-place'],
      sampleInput: '',
      sampleOutput: 'Original: 1 2 3 \nDoubled: 2 4 6 \nSquared: 4 16 36 ',
      hints: ['void map(int arr[], int n, int (*transform)(int))', 'In loop: arr[i] = transform(arr[i]);'],
      solution: '#include <stdio.h>\n\nvoid map(int arr[], int n, int (*transform)(int)) {\n    for (int i = 0; i < n; i++) {\n        arr[i] = transform(arr[i]);\n    }\n}\n\nint doubleVal(int x) { return x * 2; }\nint square(int x) { return x * x; }\n\nvoid printArr(int arr[], int n) {\n    for(int i=0; i<n; i++) printf("%d ", arr[i]);\n    printf("\\n");\n}\n\nint main(void) {\n    int data[] = {1, 2, 3};\n    \n    printf("Original: ");\n    printArr(data, 3);\n    \n    map(data, 3, doubleVal);\n    printf("Doubled: ");\n    printArr(data, 3);\n    \n    map(data, 3, square);\n    printf("Squared: ");\n    printArr(data, 3);\n    \n    return 0;\n}',
      solutionExplanation: 'This problem implements the classic "map" pattern from functional programming, built purely in C using function pointers. The `map` function provides the looping infrastructure to walk over an array, but it delegates the actual modification of each element to the `transform` callback. This makes `map` infinitely reusable — you can double, square, or perform any mathematical mutation without ever having to write another array loop.',
      dryRun: [
        { step: 1, line: 23, variables: {}, output: '', explanation: 'Main calls `map` and passes the address of the `doubleVal` function.' },
        { step: 2, line: 5, variables: { i: '0' }, output: '', explanation: 'Inside `map`\'s loop, `arr[0]` is overwritten by the result of `transform(1)`, which executes `doubleVal(1)` and returns 2.' },
        { step: 3, line: 5, variables: { i: '2' }, output: '', explanation: 'The loop completes. Every element has been processed through `doubleVal`. The array is now {2, 4, 6}.' },
        { step: 4, line: 27, variables: {}, output: '', explanation: 'Main calls `map` again, this time passing the address of the `square` function.' },
        { step: 5, line: 5, variables: { i: '0' }, output: '', explanation: 'Inside `map`\'s loop, `arr[0]` is overwritten by the result of `transform(2)`, which executes `square(2)` and returns 4. By the end, the array is {4, 16, 36}.' },
      ],
      tags: ['callbacks', 'map', 'arrays'],
    },
  ],
};
