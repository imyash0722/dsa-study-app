import type { Topic } from '../../../../types';

export const arrays2d: Topic = {
  id: 'u2-t2',
  unitId: 'unit-2',
  title: 'Arrays — 2D',
  slug: 'arrays-2d',
  description: `Two-dimensional arrays provide the logical abstraction of a table with rows and columns — essential for representing mathematical matrices, image pixel grids, game boards, spreadsheets, and any data that is naturally organised along two axes. However, understanding 2D arrays in C requires a conceptual leap that separates C from higher-level languages: the computer's RAM is fundamentally one-dimensional (a linear sequence of bytes), and C creates the illusion of a 2D grid by storing rows back-to-back in a single flat block using a layout convention called row-major order.

When you declare int matrix[3][4], the compiler allocates a single contiguous block of 3 × 4 × sizeof(int) = 48 bytes. The entire first row (4 integers) occupies the first 16 bytes, immediately followed by the second row and then the third, with no separators, headers, or gaps. The expression matrix[i][j] compiles to the address calculation base + (i × num_columns + j) × sizeof(int), which the CPU computes in constant time. This flat storage has profound performance implications: traversing a 2D array in row-major order (iterating over columns in the inner loop) accesses memory sequentially, maximising CPU cache utilisation; traversing in column-major order (iterating over rows in the inner loop) causes cache misses on every access, potentially degrading performance by an order of magnitude.

Understanding this physical layout is prerequisite for mastering matrix operations (addition, transposition, multiplication), for correctly passing 2D arrays to functions (which requires specifying the column dimension so the compiler can compute row strides), and for understanding the pointer-level representation of multi-dimensional data — a topic that connects directly to pointer-to-array and array-of-pointers concepts.`,
  difficulty: 'intermediate',
  prerequisites: ['u2-t1'],
  estimatedMinutes: 60,
  subtopics: [
    {
      id: 'u2-t2-s1',
      title: '2D Array Basics',
      slug: '2d-array-basics',
      description: `A two-dimensional array in C provides the logical abstraction of a table with rows and columns, but at the hardware level, it is stored as a single flat sequence of bytes in memory — because RAM itself is one-dimensional. When you declare int matrix[3][4], the compiler allocates a single contiguous block of 3 * 4 * sizeof(int) = 48 bytes. The elements are arranged in row-major order: the entire first row is laid out consecutively in memory, immediately followed by the entire second row, and then the third row. There are no gaps, headers, or separators between rows — the data is one unbroken stream of integers that the compiler interprets as a 2D grid through arithmetic.

The compiler translates every access of the form matrix[row][col] into a single address computation using the formula: address = base_address + (row * number_of_columns + col) * sizeof(element). This formula is the key to understanding everything about 2D arrays in C. Notice that it absolutely requires knowing the number of columns to work — because the column count determines how many elements constitute one row, which determines where the next row begins. The number of rows, by contrast, is irrelevant for locating any specific element. This is why, when passing a 2D array to a function, C requires you to specify the column dimension in the parameter type (int mat[][4] or int mat[][cols]) but allows the row dimension to be omitted.

The row-major layout has significant performance implications due to how modern CPUs access memory. When the CPU reads a value from RAM, it does not fetch just that one value — it loads an entire cache line (typically 64 bytes, holding 16 integers) into a fast on-chip cache. If your program reads the next element in the same row, that element is almost certainly already in the cache, making the access nearly instantaneous. This is why traversing a 2D array row-by-row (outer loop over rows, inner loop over columns) is dramatically faster than column-by-column traversal, which causes the CPU to jump across rows, missing the cache on every access. For large matrices, this cache locality difference can cause a 10x or greater performance gap — a fact that matters enormously in scientific computing, image processing, and machine learning.`,
      keyPoints: [
        'Declaration syntax follows the pattern: type name[rows][cols];. For example, int grid[3][4]; allocates a single contiguous block of memory large enough to hold 12 integers (3 rows * 4 columns = 12 elements).',
        'C stores 2D arrays in row-major order. If you have a 2x3 matrix, the elements are stored in this exact order in memory: [0][0], [0][1], [0][2], [1][0], [1][1], [1][2]. The first row is laid out completely before the second row begins. There are no gaps between rows.',
        'When you provide an initialization list, you can group the rows with nested braces for readability: int mat[2][3] = { {1,2,3}, {4,5,6} };. If you provide fewer values than the array holds, the remaining elements are automatically zero-initialized, just like 1D arrays.',
        'The compiler calculates the physical memory address of element [row][col] using the formula: address = base_address + (row * number_of_columns + col) * element_size. Notice that this formula absolutely requires knowing the number of columns to work. It does not need the number of rows.',
        'Nested loops are the idiomatic way to process 2D arrays. The outer loop typically iterates over the rows (i), and the inner loop iterates over the columns (j). This matches C\'s row-major memory layout, meaning the CPU reads memory sequentially, which is highly optimized by hardware caches.',
      ],
      codeExamples: [
        {
          id: 'u2-t2-s1-ex1',
          title: 'Declaration and Printing',
          code: '#include <stdio.h>\n\nint main(void) {\n    /* 2 rows, 3 columns */\n    int grid[2][3] = {\n        {1, 2, 3},\n        {4, 5, 6}\n    };\n\n    for (int i = 0; i < 2; i++) {       /* Rows */\n        for (int j = 0; j < 3; j++) {   /* Columns */\n            printf("%d ", grid[i][j]);\n        }\n        printf("\\n");\n    }\n    return 0;\n}',
          language: 'c',
          explanation: 'This code demonstrates the fundamental pattern for working with 2D arrays: the nested for-loop. The outer loop (using index i) controls which row we are currently looking at. For each single iteration of the outer loop, the inner loop (using index j) runs to completion, sweeping across every column in that specific row. This traversal order (row-by-row) perfectly matches how C stores the array in memory, making it the most efficient way to read or write matrix data.',
          expectedOutput: '1 2 3 \n4 5 6 ',
          lineBreakdown: [
            { lineNumber: 5, code: '    int grid[2][3] = {', explanation: 'Allocates a contiguous block of 6 integers, conceptually divided into 2 rows and 3 columns.' },
            { lineNumber: 10, code: '    for (int i = 0; i < 2; i++) {', explanation: 'The outer loop selects the active row. It pauses here while the inner loop finishes its work.' },
            { lineNumber: 11, code: '        for (int j = 0; j < 3; j++) {', explanation: 'The inner loop sweeps horizontally across all columns of the currently active row.' },
            { lineNumber: 12, code: '            printf("%d ", grid[i][j]);', explanation: 'Accesses the specific integer at the intersection of row i and column j.' },
          ],
          relatedTopicIds: ['u1-t8'],
        },
        {
          id: 'u2-t2-s1-ex2',
          title: 'Memory Layout (Row-Major Order)',
          code: '#include <stdio.h>\n\nint main(void) {\n    int a[2][2] = { {10, 20}, {30, 40} };\n\n    printf("Address of a[0][0]: %p\\n", (void*)&a[0][0]);\n    printf("Address of a[0][1]: %p\\n", (void*)&a[0][1]);\n    printf("Address of a[1][0]: %p\\n", (void*)&a[1][0]);\n    printf("Address of a[1][1]: %p\\n", (void*)&a[1][1]);\n    \n    return 0;\n}',
          language: 'c',
          explanation: 'This program proves that 2D arrays are an illusion provided by the compiler. By printing the physical memory addresses of each element, you can see that they increase exactly by 4 bytes (the size of one int) sequentially. The element a[1][0] (the start of the second row) comes exactly 4 bytes after a[0][1] (the end of the first row). The memory is a single, unbroken, 1-dimensional line. The brackets [row][col] are just syntactic sugar that asks the compiler to do the math to find the right spot on that line.',
          expectedOutput: 'Address of a[0][0]: 0x7ffd0000\nAddress of a[0][1]: 0x7ffd0004\nAddress of a[1][0]: 0x7ffd0008\nAddress of a[1][1]: 0x7ffd000c',
          lineBreakdown: [
            { lineNumber: 4, code: '    int a[2][2] = { {10, 20}, {30, 40} };', explanation: 'Initializes the 2x2 matrix. Memory is laid out as 10, 20, 30, 40 in a single line.' },
            { lineNumber: 6, code: '    printf("Address of a[0][0]: %p\\n", (void*)&a[0][0]);', explanation: 'Prints the base address. Observe the output: each subsequent address is exactly 4 bytes higher, proving contiguous row-major storage.' },
          ],
          relatedTopicIds: ['u1-t4'],
        },
        {
          id: 'u2-t2-s1-ex3',
          title: 'Passing 2D Array to Function',
          code: '#include <stdio.h>\n\n/* The column size MUST be specified! */\nvoid printMatrix(int rows, int cols, int mat[][cols]) {\n    for (int i = 0; i < rows; i++) {\n        for (int j = 0; j < cols; j++) {\n            printf("%d ", mat[i][j]);\n        }\n        printf("\\n");\n    }\n}\n\nint main(void) {\n    int m[2][3] = {{1,2,3}, {4,5,6}};\n    printMatrix(2, 3, m);\n    return 0;\n}',
          language: 'c',
          explanation: 'When passing a 1D array to a function, you must pass its size. When passing a 2D array, you must do something even more specific: you must hardcode the number of columns directly into the parameter type, or use a Variable Length Array (VLA) parameter as shown here. Why? Because the compiler needs to know exactly how long a row is to calculate where the next row begins. If it doesn\'t know the column count, it cannot evaluate the expression mat[i][j] because it can\'t compute base_address + (i * cols + j). The number of rows does not matter for this calculation, which is why the first bracket can be empty.',
          expectedOutput: '1 2 3 \n4 5 6 ',
          lineBreakdown: [
            { lineNumber: 4, code: 'void printMatrix(int rows, int cols, int mat[][cols]) {', explanation: 'The parameter cols MUST be declared before mat[][cols] so the compiler knows the row width when compiling the array accesses.' },
            { lineNumber: 15, code: '    printMatrix(2, 3, m);', explanation: 'Passes the dimensions first, followed by the array pointer. The dimensions tell the function how to navigate the flat memory block.' },
          ],
          relatedTopicIds: ['u2-t6'],
        },
      ],
      commonMistakes: [
        {
          id: 'u2-t2-s1-cm1',
          title: 'Forgetting the column size in function parameters',
          wrongCode: 'void process(int mat[][]) { /* Error */ }',
          correctCode: 'void process(int rows, int cols, int mat[][cols]) { }',
          explanation: 'This is a compile-time error. The compiler translates the 2D syntax `mat[row][col]` into the 1D calculation `base + (row * COLS + col) * sizeof(element)`. If you leave the `COLS` dimension empty in the parameter list, the compiler has no idea what number to multiply the row index by. It literally cannot generate the machine code to access the array. You must always provide the column dimension (either as a constant like `mat[][10]` or a variable like `mat[][cols]`).',
          consequence: 'Compilation error: "array type has incomplete element type". The compiler refuses to compile because it lacks the math formula to find elements.',
        },
        {
          id: 'u2-t2-s1-cm2',
          title: 'Swapping row and column indices',
          wrongCode: 'int mat[2][3];\nmat[3][2] = 5; /* Out of bounds */',
          correctCode: 'int mat[2][3];\nmat[1][2] = 5; /* Valid */',
          explanation: 'Because humans naturally talk about "X and Y" coordinates, beginners often swap the row and column indices. If you declare `mat[2][3]`, the valid row indices are 0 and 1, and the valid column indices are 0, 1, and 2. Writing `mat[3][2]` asks the compiler to calculate the memory offset for the 4th row, which doesn\'t exist. Since C performs no bounds checking, it just blindly calculates the math and overwrites whatever memory happens to be at that location.',
          consequence: 'Undefined behavior. The code compiles without a warning, but writing out of bounds corrupts adjacent memory, potentially causing a silent logical bug or a segmentation fault.',
        },
      ],
      interviewCallouts: [
        {
          id: 'u2-t2-s1-ic1',
          title: 'Row-Major vs Column-Major',
          content: 'This is a classic systems programming question. C stores matrices in "Row-Major" order, meaning it stores an entire row contiguously before moving to the next. Some other languages (like Fortran or MATLAB) use "Column-Major" order. This is critical for performance: modern CPUs load memory in chunks called cache lines. If you write your nested loops to process Row-by-Row (outer loop for rows, inner for columns), you read memory sequentially, maximizing cache hits. If you process Column-by-Column, the CPU jumps around in memory, causing cache misses and drastically slowing down your program.',
          relatedTopicIds: [],
          frequency: 'occasional',
        },
      ],
      checkpoints: [
        {
          id: 'u2-t2-s1-cp1',
          title: '2D Arrays Basics',
          description: 'Verify understanding of the row-major memory model and how the compiler maps 2D coordinates to 1D hardware.',
          criteria: [
            'Draw the flat memory layout of a 2x3 integer array',
            'Write a nested loop that correctly processes a matrix row-by-row',
            'Explain mathematically why the compiler must know the column count but not the row count',
          ],
          topicId: 'u2-t2',
        },
      ],
      revisionCards: [
        {
          id: 'u2-t2-s1-rc1',
          front: 'What does "Row-Major Order" mean and why is it important for performance?',
          back: 'It means C stores a 2D array in memory by placing the entire first row contiguously, followed immediately by the second row, etc. It is important because traversing the array row-by-row (inner loop on columns) reads memory sequentially, which is highly optimized by the CPU cache.',
          topicId: 'u2-t2',
          tags: ['arrays', 'memory', 'performance'],
        },
        {
          id: 'u2-t2-s1-rc2',
          front: 'When passing a 2D array to a function, why can you leave the row size empty but MUST specify the column size?',
          back: 'Because the compiler maps the 2D coordinates to a 1D memory address using the formula: base + (row * COLUMNS + col). It physically cannot do the math without knowing the number of columns. The total number of rows is irrelevant to finding a specific element.',
          topicId: 'u2-t2',
          tags: ['arrays', 'functions'],
        },
        {
          id: 'u2-t2-s1-rc3',
          front: 'In a 2D array matrix[M][N], what are the bounds of the outer and inner loops for a standard traversal?',
          back: 'The outer loop (rows) runs from 0 to M-1. The inner loop (columns) runs from 0 to N-1. Mixing these up causes out-of-bounds memory corruption.',
          topicId: 'u2-t2',
          tags: ['arrays', 'performance'],
        },
      ],
    },
    {
      id: 'u2-t2-s2',
      title: 'Matrix Operations',
      slug: 'matrix-operations',
      description: `Matrix operations — addition, transposition, and multiplication — are the canonical algorithms that test a programmer's ability to reason about nested loop structures and multi-dimensional indexing. These operations are not merely academic exercises: they are fundamental to computer graphics (transforming 3D coordinates), machine learning (neural network weight matrices), physics simulations (linear algebra), and image processing (convolution filters). Each operation builds on the 2D array concepts from the previous subtopic, and mastering them requires precise control over multiple loop indices and a clear understanding of how data flows between source and destination matrices.

Matrix addition is the simplest operation: given two matrices A and B of identical dimensions, the result C is computed element-wise as C[i][j] = A[i][j] + B[i][j]. Transposition reflects a matrix across its main diagonal, converting rows into columns: target[col][row] = source[row][col]. If the source matrix is M×N, the transposed matrix must be declared as N×M. Matrix multiplication is substantially more complex and is the first algorithm most students encounter that requires three nested loops. To compute a single element C[i][j], you must calculate the dot product of row i from matrix A and column j from matrix B, which requires a third inner loop (index k) to traverse both simultaneously: C[i][j] += A[i][k] * B[k][j]. This triple nesting gives matrix multiplication a time complexity of O(N³), meaning that doubling the matrix dimensions increases the computation time eightfold.

The critical implementation detail for matrix multiplication is initialization: because the algorithm accumulates a sum using the += operator, every element of the result matrix C must be initialized to 0 before the loops begin. If C is declared as a local variable without an initializer (int C[N][N];), its elements contain unpredictable garbage values from whatever happened to occupy that stack memory previously. Accumulating a sum onto garbage produces garbage, regardless of how correct the multiplication logic is. The idiom int C[N][N] = {0} ensures every element starts at zero and is a mandatory first step.`,
      keyPoints: [
        'Matrix Addition is a straightforward mapping: you iterate through every row and column, and set C[i][j] = A[i][j] + B[i][j]. This requires both matrices to have the exact same dimensions.',
        'Matrix Transposition flips the matrix over its main diagonal. The rows become columns, and the columns become rows. The core operation is a swap: target[col][row] = source[row][col]. If the matrix is not square, the dimensions of the target matrix must be swapped.',
        'Matrix Multiplication is significantly more complex. To calculate a single cell C[i][j], you must compute the dot product of row i from matrix A and column j from matrix B. This means traversing a row horizontally and a column vertically at the same time.',
        'Because matrix multiplication requires iterating over rows, then columns, and then iterating to compute the dot product, it requires three nested loops. The innermost loop (typically k) is the mathematical heart of the algorithm, accumulating the sum: C[i][j] += A[i][k] * B[k][j].',
      ],
      codeExamples: [
        {
          id: 'u2-t2-s2-ex1',
          title: 'Matrix Addition',
          code: '#include <stdio.h>\n\nint main(void) {\n    int a[2][2] = {{1, 2}, {3, 4}};\n    int b[2][2] = {{5, 6}, {7, 8}};\n    int c[2][2];\n\n    for (int i = 0; i < 2; i++) {\n        for (int j = 0; j < 2; j++) {\n            c[i][j] = a[i][j] + b[i][j];\n            printf("%2d ", c[i][j]);\n        }\n        printf("\\n");\n    }\n    return 0;\n}',
          language: 'c',
          explanation: 'Matrix addition is the simplest 2D algorithm. Because both matrices have identical dimensions, we simply walk through both arrays in lockstep using standard double-nested loops, adding the corresponding elements together. The visual simplicity of c[i][j] = a[i][j] + b[i][j] reflects the underlying mathematical rule.',
          expectedOutput: ' 6  8 \n10 12 ',
          lineBreakdown: [
            { lineNumber: 10, code: '            c[i][j] = a[i][j] + b[i][j];', explanation: 'Reads from a and b simultaneously, calculates the sum, and writes it directly to the corresponding location in c.' },
          ],
          relatedTopicIds: [],
        },
        {
          id: 'u2-t2-s2-ex2',
          title: 'Matrix Transpose',
          code: '#include <stdio.h>\n\nint main(void) {\n    int a[2][3] = {{1, 2, 3}, {4, 5, 6}};\n    int t[3][2]; /* Dimensions swapped! */\n\n    for (int i = 0; i < 2; i++) {\n        for (int j = 0; j < 3; j++) {\n            t[j][i] = a[i][j]; /* Swap i and j */\n        }\n    }\n\n    /* Print transposed matrix */\n    for (int i = 0; i < 3; i++) {\n        for (int j = 0; j < 2; j++) {\n            printf("%d ", t[i][j]);\n        }\n        printf("\\n");\n    }\n    return 0;\n}',
          language: 'c',
          explanation: 'Transposing a matrix means reflecting it across its diagonal. The first row of the original matrix becomes the first column of the new matrix. Notice the critical detail on line 5: if the original matrix is 2x3, the transposed matrix MUST be declared as 3x2. The core logic on line 9 is beautifully simple: whatever value is at [row][col] in the original goes to [col][row] in the transposed version.',
          expectedOutput: '1 4 \n2 5 \n3 6 ',
          lineBreakdown: [
            { lineNumber: 5, code: '    int t[3][2];', explanation: 'The dimensions must be swapped. A 2-row, 3-column matrix transposes into a 3-row, 2-column matrix.' },
            { lineNumber: 9, code: '            t[j][i] = a[i][j];', explanation: 'The core transposition logic: read from coordinates (i, j) and write to coordinates (j, i).' },
          ],
          relatedTopicIds: [],
        },
        {
          id: 'u2-t2-s2-ex3',
          title: 'Matrix Multiplication',
          code: '#include <stdio.h>\n\nint main(void) {\n    int a[2][3] = {{1, 2, 3}, {4, 5, 6}};\n    int b[3][2] = {{7, 8}, {9, 10}, {11, 12}};\n    int c[2][2] = {0}; /* Must initialize to 0 */\n\n    for (int i = 0; i < 2; i++) {           /* Rows of A */\n        for (int j = 0; j < 2; j++) {       /* Cols of B */\n            for (int k = 0; k < 3; k++) {   /* Cols of A / Rows of B */\n                c[i][j] += a[i][k] * b[k][j];\n            }\n            printf("%3d ", c[i][j]);\n        }\n        printf("\\n");\n    }\n    return 0;\n}',
          language: 'c',
          explanation: 'This is the most complex array algorithm a beginner will encounter. To calculate a single cell c[i][j], you must multiply the elements of row i in matrix A with the elements of column j in matrix B, and add them all together (a dot product). This requires a third inner loop (k) that steps across A horizontally while stepping down B vertically. Because we are accumulating a sum using +=, the matrix c MUST be initialized to 0 first.',
          expectedOutput: ' 58  64 \n139 154 ',
          lineBreakdown: [
            { lineNumber: 6, code: '    int c[2][2] = {0};', explanation: 'CRITICAL: Because we are accumulating a sum with +=, the target cells must start at exactly 0, not random garbage memory.' },
            { lineNumber: 10, code: '            for (int k = 0; k < 3; k++) {', explanation: 'The third loop executes the dot product. It runs exactly as many times as there are columns in A (which must equal rows in B).' },
            { lineNumber: 11, code: '                c[i][j] += a[i][k] * b[k][j];', explanation: 'Notice the indices: a[i][k] moves across row i. b[k][j] moves down column j. The results are multiplied and added to the running total.' },
          ],
          relatedTopicIds: ['u1-t8'],
        },
      ],
      commonMistakes: [
        {
          id: 'u2-t2-s2-cm1',
          title: 'Forgetting to initialize target matrix to 0 in multiplication',
          wrongCode: 'int c[2][2]; /* Contains garbage */\n/* ... loops ... */\nc[i][j] += a[i][k] * b[k][j];',
          correctCode: 'int c[2][2] = {0}; /* Initialized to 0 */\n/* ... loops ... */\nc[i][j] += a[i][k] * b[k][j];',
          explanation: 'A fundamental rule of C is that uninitialized local variables contain unpredictable garbage values (whatever binary data was left in that RAM by a previous function). Matrix multiplication computes a dot product by continuously adding to a running total (c[i][j] += ...). If you try to accumulate a sum into a garbage value, the result will just be a different garbage value. You must initialize the target matrix to {0} so the addition starts from a clean slate.',
          consequence: 'The resulting matrix will contain wildly incorrect, essentially random numbers. The logic of the multiplication is correct, but the starting point was polluted.',
        },
      ],
      interviewCallouts: [
        {
          id: 'u2-t2-s2-ic1',
          title: 'Matrix Multiplication Complexity',
          content: 'Interviewers love asking about matrix multiplication complexity. Because it requires three nested loops — one for rows, one for columns, and one for the dot product — its time complexity is O(N³), meaning doubling the matrix size increases the work 8-fold. A great candidate will also mention that while algorithms like Strassen\'s can reduce this mathematically, standard O(N³) is often faster for small matrices due to hardware cache locality.',
          relatedTopicIds: [],
          frequency: 'common',
        },
      ],
      checkpoints: [
        {
          id: 'u2-t2-s2-cp1',
          title: 'Matrix Operations',
          description: 'Verify the ability to structure complex, multi-layered algorithms and understand dot-product logic.',
          criteria: [
            'Explain why matrix multiplication requires exactly three nested loops',
            'Write the code to correctly initialize a target matrix for accumulation',
            'Trace the execution of a dot product computation step by step',
          ],
          topicId: 'u2-t2',
        },
      ],
      revisionCards: [
        {
          id: 'u2-t2-s2-rc1',
          front: 'What are the dimension rules for multiplying matrix A by matrix B, and what size is the result?',
          back: 'The number of columns in A MUST equal the number of rows in B. If A is (m × n) and B is (n × p), the multiplication is valid, and the resulting matrix C will be (m × p).',
          topicId: 'u2-t2',
          tags: ['matrices', 'multiplication', 'math'],
        },
        {
          id: 'u2-t2-s2-rc2',
          front: 'Why must the target matrix C be initialized to 0 before matrix multiplication?',
          back: 'Because the innermost loop computes a dot product using the += operator (c[i][j] += a[i][k] * b[k][j]). If C is not initialized to 0, the addition accumulates on top of whatever random garbage was in memory.',
          topicId: 'u2-t2',
          tags: ['matrices', 'bugs', 'initialization'],
        },
      ],
    },
  ],

  theoryQuestions: [
    {
      id: 'u2-t2-q1',
      type: 'mcq',
      topicId: 'u2-t2',
      difficulty: 'beginner',
      question: 'Which of the following correctly initializes a 2D array?',
      options: [
        'int arr[][] = {{1,2}, {3,4}};',
        'int arr[2][] = {{1,2}, {3,4}};',
        'int arr[][2] = {{1,2}, {3,4}};',
        'int arr[2,2] = {{1,2}, {3,4}};',
      ],
      correctAnswer: 'int arr[][2] = {{1,2}, {3,4}};',
      explanation: 'When initializing a 2D array, the compiler can count the number of row brace-groups to infer the number of rows. However, because memory is a flat line, it absolutely MUST know the number of columns to understand where one row ends and the next begins. The column dimension is strictly required.',
      tags: ['arrays', 'initialization'],
    },
    {
      id: 'u2-t2-q2',
      type: 'predict-output',
      topicId: 'u2-t2',
      difficulty: 'intermediate',
      question: 'What is the output?',
      code: '#include <stdio.h>\nint main(void) {\n    int a[2][2] = { {1}, {3, 4} };\n    printf("%d", a[0][1]);\n    return 0;\n}',
      correctAnswer: '0',
      explanation: 'This highlights how partial initialization works in C. The first row is partially initialized with just {1}, so the compiler sets a[0][0] to 1 and automatically zeroes out the rest of that row, making a[0][1] equal to 0. The second row is fully initialized to 3 and 4.',
      tags: ['arrays', 'initialization'],
    },
    {
      id: 'u2-t2-q3',
      type: 'true-false',
      topicId: 'u2-t2',
      difficulty: 'beginner',
      question: 'In C, memory for a 2D array is allocated as an array of pointers to rows.',
      correctAnswer: false,
      explanation: 'This is a critical conceptual distinction. A standard 2D array like int a[2][3] is a single contiguous block of 6 integers mapped via math. An array of pointers is a fundamentally different structure (an array of addresses pointing to scattered memory). Understanding this separates beginners from intermediate C programmers.',
      tags: ['arrays', 'memory'],
    },
    {
      id: 'u2-t2-q4',
      type: 'spot-bug',
      topicId: 'u2-t2',
      difficulty: 'intermediate',
      question: 'Spot the bug in this function signature:',
      code: 'void processMatrix(int mat[][], int rows, int cols) {\n    /* logic */\n}',
      correctAnswer: 'The column dimension is missing in mat[][].',
      explanation: 'The compiler needs the mathematical formula to map 2D coordinates [i][j] to a flat memory address: base + (i * COLS + j) * size. Without the column size, it cannot evaluate this formula. The row size is optional because calculating an offset only depends on the row width.',
      tags: ['arrays', 'functions'],
    },
    {
      id: 'u2-t2-q5',
      type: 'mcq',
      topicId: 'u2-t2',
      difficulty: 'intermediate',
      question: 'For matrix multiplication C = A * B, what must be true about the dimensions?',
      options: [
        'A and B must have the exact same dimensions',
        'Cols of A must equal Rows of B',
        'Rows of A must equal Cols of B',
        'They must both be square matrices',
      ],
      correctAnswer: 'Cols of A must equal Rows of B',
      explanation: 'Matrix multiplication calculates the dot product of a row from matrix A and a column from matrix B. For a dot product to be mathematically possible, the length of the row in A (number of columns) must perfectly match the length of the column in B (number of rows).',
      tags: ['matrices', 'multiplication'],
    },
  ],

  programmingProblems: [
    {
      id: 'u2-t2-new-easy',
      title: 'Array Reverse',
      topicId: 'u2-t2',
      difficulty: 'beginner',
      problemStatement: 'Reverse an array of N elements in place.',
      constraints: ['Do not use a secondary array'],
      sampleInput: '1 2 3',
      sampleOutput: '3 2 1',
      hints: ['Swap elements from both ends moving towards the center'],
      solution: '/* Array reverse implementation */',
      solutionExplanation: 'Swaps index i and N-1-i.',
      dryRun: [],
      tags: ['arrays']
    },
    {
      id: 'u2-t2-new-med',
      title: 'Matrix Diagonal Sum',
      topicId: 'u2-t2',
      difficulty: 'intermediate',
      problemStatement: 'Calculate the sum of the main diagonal of an NxN matrix.',
      constraints: ['Matrix is guaranteed to be square'],
      sampleInput: '1 2\n3 4',
      sampleOutput: '5',
      hints: ['Main diagonal elements have index [i][i]'],
      solution: '/* Diagonal sum implementation */',
      solutionExplanation: 'Iterates and sums arr[i][i].',
      dryRun: [],
      tags: ['matrix']
    },
    {
      id: 'u2-t2-new-hard',
      title: 'Recursive GCD',
      topicId: 'u2-t2',
      difficulty: 'advanced',
      problemStatement: 'Find the Greatest Common Divisor of two numbers using recursion (Euclidean algorithm).',
      constraints: ['Must use recursion'],
      sampleInput: '48 18',
      sampleOutput: '6',
      hints: ['gcd(a, b) = gcd(b, a % b)'],
      solution: '/* Recursive GCD implementation */',
      solutionExplanation: 'Implements Euclidean algorithm recursively.',
      dryRun: [],
      tags: ['recursion']
    },
    {
      id: 'u2-t2-p1',
      title: 'Matrix Sum of Rows and Columns',
      topicId: 'u2-t2',
      difficulty: 'beginner',
      problemStatement: 'Read a 3x3 matrix. Calculate and print the sum of each row and the sum of each column.',
      constraints: ['Fixed 3x3 size', 'Use nested loops'],
      sampleInput: '1 2 3\n4 5 6\n7 8 9',
      sampleOutput: 'Row sums: 6 15 24\nCol sums: 12 15 18',
      hints: ['Outer loop i for rows, inner j for cols to sum rows.', 'Outer loop j for cols, inner i for rows to sum cols.'],
      solution: '#include <stdio.h>\n\nint main(void) {\n    int mat[3][3];\n    for(int i=0; i<3; i++)\n        for(int j=0; j<3; j++)\n            scanf("%d", &mat[i][j]);\n\n    printf("Row sums: ");\n    for(int i=0; i<3; i++) {\n        int sum = 0;\n        for(int j=0; j<3; j++) sum += mat[i][j];\n        printf("%d ", sum);\n    }\n\n    printf("\\nCol sums: ");\n    for(int j=0; j<3; j++) {\n        int sum = 0;\n        for(int i=0; i<3; i++) sum += mat[i][j];\n        printf("%d ", sum);\n    }\n    printf("\\n");\n    return 0;\n}',
      solutionExplanation: 'This problem tests the ability to traverse the exact same memory in two different ways. The first pass computes row sums by fixing the row index (i) and sweeping across the columns (j), accumulating a sum for each row. The second pass computes column sums by fixing the column index (j) in the outer loop and sweeping down the rows (i) in the inner loop. Notice how swapping the nesting order of the loops completely changes the mathematical result.',
      dryRun: [
        { step: 1, line: 10, variables: { i: '0', sum: '0' }, output: '', explanation: 'Outer loop begins at row 0. We initialize the sum accumulator to 0 for this specific row.' },
        { step: 2, line: 11, variables: { i: '0', j: '2', sum: '6' }, output: '', explanation: 'Inner loop sweeps through j=0, 1, 2, adding mat[0][0], mat[0][1], and mat[0][2]. Sum is now 1+2+3 = 6.' },
        { step: 3, line: 12, variables: {}, output: 'Row sums: 6 ', explanation: 'The row sum is printed before the outer loop advances to row 1.' },
      ],
      tags: ['arrays', 'matrices', 'loops'],
    },
    {
      id: 'u2-t2-p2',
      title: 'Principal Diagonal Sum',
      topicId: 'u2-t2',
      difficulty: 'beginner',
      problemStatement: 'Read an N x N matrix and calculate the sum of its principal diagonal (top-left to bottom-right).',
      constraints: ['N <= 10'],
      sampleInput: '3\n1 2 3\n4 5 6\n7 8 9',
      sampleOutput: 'Diagonal sum: 15',
      hints: ['You do not need a nested loop!', 'Elements on the principal diagonal have the same row and column index (i == j).'],
      solution: '#include <stdio.h>\n\nint main(void) {\n    int n;\n    scanf("%d", &n);\n    int mat[10][10];\n    \n    for(int i=0; i<n; i++)\n        for(int j=0; j<n; j++)\n            scanf("%d", &mat[i][j]);\n            \n    int sum = 0;\n    for(int i=0; i<n; i++) {\n        sum += mat[i][i]; /* i == j */\n    }\n    \n    printf("Diagonal sum: %d\\n", sum);\n    return 0;\n}',
      solutionExplanation: 'A common beginner mistake is writing two nested loops and adding an if (i == j) condition to find the diagonal. While correct, it is highly inefficient — evaluating a condition N*N times when you only need N elements. The elegant solution is a single loop. The principal diagonal is defined by the property that its row and column indices are identical (0,0), (1,1), (2,2). A single loop using index i for both dimensions directly accesses only the necessary elements in O(N) time instead of O(N²).',
      dryRun: [
        { step: 1, line: 14, variables: { i: '0', sum: '1' }, output: '', explanation: 'Loop starts. Accesses mat[0][0], which is the top-left element (1). Adds to sum.' },
        { step: 2, line: 14, variables: { i: '1', sum: '6' }, output: '', explanation: 'Loop increments. Accesses mat[1][1], the center element (5). Sum becomes 6.' },
        { step: 3, line: 14, variables: { i: '2', sum: '15' }, output: '', explanation: 'Loop increments. Accesses mat[2][2], the bottom-right element (9). Sum becomes 15.' },
        { step: 4, line: 17, variables: {}, output: 'Diagonal sum: 15', explanation: 'The single loop completes, having precisely targeted only the diagonal line.' },
      ],
      tags: ['arrays', 'matrices', 'diagonal'],
    },
    {
      id: 'u2-t2-p3',
      title: 'Matrix Multiplication',
      topicId: 'u2-t2',
      difficulty: 'intermediate',
      problemStatement: 'Read matrix A (N x N) and matrix B (N x N). Multiply them to form matrix C and print C.',
      constraints: ['N <= 5', 'Use 3 nested loops'],
      sampleInput: '2\n1 2\n3 4\n5 6\n7 8',
      sampleOutput: '19 22 \n43 50 ',
      hints: ['C[i][j] is the dot product of A\'s row i and B\'s col j.', 'Don\'t forget to initialize C elements to 0!'],
      solution: '#include <stdio.h>\n\nint main(void) {\n    int n;\n    scanf("%d", &n);\n    int a[5][5], b[5][5], c[5][5] = {0};\n    \n    for(int i=0; i<n; i++) for(int j=0; j<n; j++) scanf("%d", &a[i][j]);\n    for(int i=0; i<n; i++) for(int j=0; j<n; j++) scanf("%d", &b[i][j]);\n    \n    for(int i=0; i<n; i++) {\n        for(int j=0; j<n; j++) {\n            for(int k=0; k<n; k++) {\n                c[i][j] += a[i][k] * b[k][j];\n            }\n            printf("%d ", c[i][j]);\n        }\n        printf("\\n");\n    }\n    return 0;\n}',
      solutionExplanation: 'This is the crown jewel of array manipulation. It demonstrates the triple-nested loop required for matrix multiplication. The outer two loops (i and j) select the target cell in matrix C. The innermost loop (k) performs the actual mathematical heavy lifting: it simultaneously walks horizontally across row i in matrix A (using a[i][k]) and vertically down column j in matrix B (using b[k][j]), multiplying the matching pairs and accumulating them into the target cell c[i][j]. The pre-initialization of C to {0} is what makes the += accumulation possible.',
      dryRun: [
        { step: 1, line: 14, variables: { i: '0', j: '0', k: '0' }, output: '', explanation: 'Calculating target cell c[0][0]. First multiplication: a[0][0] * b[0][0] (1 * 5 = 5). c[0][0] accumulates 5.' },
        { step: 2, line: 14, variables: { i: '0', j: '0', k: '1' }, output: '', explanation: 'Still calculating c[0][0]. k increments. Second multiplication: a[0][1] * b[1][0] (2 * 7 = 14). c[0][0] adds 14 to become 19.' },
        { step: 3, line: 16, variables: {}, output: '19 ', explanation: 'The innermost loop for cell c[0][0] completes. The final dot product (19) is printed, and the program moves to calculate c[0][1].' },
      ],
      tags: ['arrays', 'matrices', 'multiplication'],
    },
  ],
};
