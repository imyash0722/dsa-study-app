import type { Topic } from '../../../../types';

export const pointerToArray: Topic = {
  id: 'u2-t4',
  unitId: 'unit-2',
  title: 'Pointer to an Array',
  slug: 'pointer-to-array',
  description: `One of the most defining and powerful characteristics of C is the intimate relationship between arrays and pointers — a relationship so deep that the compiler almost never treats an array as a complete, self-contained object. In most expression contexts, an array name silently converts (decays) into a pointer to its first element: the expression arr evaluates to &arr[0], a pointer of type T* for an array of type T[]. This implicit conversion, known as array decay, is not a bug or an implementation shortcut — it is a deliberate language design decision that enables C to pass arrays to functions efficiently (as a pointer rather than a copy), to use pointer arithmetic interchangeably with subscript notation, and to traverse memory with maximum performance.

Understanding array decay explains several C behaviours that otherwise seem mysterious: why sizeof(arr) returns the total array size inside the declaring function but returns the pointer size inside a called function (because the parameter has already decayed), why arrays are effectively always passed by reference (the function receives a pointer to the original data, not a copy), and why the subscript operator arr[i] is defined by the standard as exactly equivalent to *(arr + i) — making pointer arithmetic and array indexing two syntactic forms of the same underlying memory operation.

The three exceptions to array decay — sizeof(arr), &arr, and string literal initialisers — reveal the remaining contexts in which the compiler still treats an array as a complete object rather than a pointer. Mastering these exceptions, along with the distinction between a pointer to an array (int (*p)[N], which advances by N elements per increment) and a pointer to an element (int *p, which advances by one element per increment), is essential for correctly handling multi-dimensional arrays, passing arrays to functions with size information preserved, and reasoning about memory layout in C.`,
  difficulty: 'intermediate',
  prerequisites: ['u2-t1', 'u2-t3'],
  estimatedMinutes: 60,
  subtopics: [
    {
      id: 'u2-t4-s1',
      title: 'Array Decay and Pointer Equivalence',
      slug: 'array-decay',
      description: `Array decay is the implicit conversion, performed automatically by the C compiler, in which an array name evaluates to a pointer to its first element in almost every context where it appears. When you declare int arr[5], the compiler allocates 20 contiguous bytes on the stack (assuming 4-byte integers), and the name arr becomes a compile-time label for the starting address of that block. In any expression — passing arr to a function, assigning it to a pointer variable, or using it in arithmetic — the compiler silently replaces arr with &arr[0], a pointer to the first element. This decay is not a runtime operation; it is a compile-time rewrite that produces no additional machine instructions.

The equivalence between array notation and pointer arithmetic is a direct consequence of this decay. The expression arr[i] is defined by the C standard as syntactic sugar for *(arr + i): the compiler takes the base address (arr, which has decayed to &arr[0]), adds i scaled by sizeof(int) bytes, and dereferences the resulting address. Because this translation is purely mechanical, pointers support the same bracket syntax: if int *p = arr, then p[2] is identical to *(p + 2) and to arr[2]. All three expressions compile to the same machine instruction.

There are exactly three contexts where array decay does not occur: the sizeof operator (sizeof(arr) returns the total size of the array in bytes, not the size of a pointer), the address-of operator applied to the array name (&arr yields a pointer to the entire array, typed as int (*)[5], not a pointer to the first element), and string literal initialisation of character arrays (char s[] = "hello" copies the string into the array rather than decaying the literal to a pointer). Understanding these exceptions is critical for writing correct C code, because confusing sizeof(arr) (which gives the full array size) with sizeof(p) (which gives the pointer size, typically 8 bytes) is a pervasive source of buffer-length calculation bugs.`,
      keyPoints: [
        'Whenever an array name is evaluated in an expression (with very few exceptions like `sizeof`), it decays into a pointer to its 0th element. `arr` is absolutely identical to `&arr[0]`.',
        'Because an array name evaluates to a memory address, you can assign it directly to a pointer: `int *p = arr;`. You do not need the `&` operator here, because `arr` already decayed into an address.',
        'Array indexing `arr[i]` is actually an illusion provided by the compiler. Under the hood, the compiler rewrites `arr[i]` into pointer arithmetic: `*(arr + i)`. It takes the base address, moves forward by `i` elements, and dereferences it.',
        'Because of this equivalence, pointers can use array bracket notation! If you have `int *p = arr;`, writing `p[2]` works perfectly and is rewritten to `*(p + 2)`.',
        'Crucial difference: An array name is a constant address tied to its memory block; you cannot reassign it (`arr = p` is illegal). A pointer is a variable; you can change where it points (`p = arr` is completely legal).',
      ],
      codeExamples: [
        {
          id: 'u2-t4-s1-ex1',
          title: 'Array Name as a Pointer',
          code: '#include <stdio.h>\n\nint main(void) {\n    int arr[3] = {10, 20, 30};\n    int *p = arr; /* Equivalent to int *p = &arr[0]; */\n\n    printf("arr[0] = %d\\n", arr[0]);\n    printf("*p     = %d\\n", *p);\n\n    printf("Address of arr[0] : %p\\n", (void*)&arr[0]);\n    printf("Value of arr name : %p\\n", (void*)arr);\n    \n    return 0;\n}',
          language: 'c',
          explanation: 'This code proves that the array name `arr` and the address of its first element `&arr[0]` are exactly the same thing. When we write `int *p = arr;`, the compiler implicitly turns `arr` into the address of the first block of memory, allowing the pointer `p` to latch onto the start of the array.',
          expectedOutput: 'arr[0] = 10\n*p     = 10\nAddress of arr[0] : 0x7ffd0000\nValue of arr name : 0x7ffd0000',
          lineBreakdown: [
            { lineNumber: 5, code: '    int *p = arr;', explanation: 'The compiler sees the array name `arr`, decays it into `&arr[0]`, and stores that memory address into the pointer variable `p`.' },
            { lineNumber: 11, code: '    printf("Value of arr name : %p\\n", (void*)arr);', explanation: 'When passed to `printf`, `arr` again decays to a pointer. We cast it to `void*` just to satisfy standard C requirements for printing addresses.' },
          ],
          relatedTopicIds: [],
        },
        {
          id: 'u2-t4-s1-ex2',
          title: 'Notation Equivalence',
          code: '#include <stdio.h>\n\nint main(void) {\n    int arr[3] = {10, 20, 30};\n    int *p = arr;\n\n    /* All 4 of these print the exact same element (20) */\n    printf("arr[1]     = %d\\n", arr[1]);\n    printf("*(arr + 1) = %d\\n", *(arr + 1));\n    printf("p[1]       = %d\\n", p[1]);\n    printf("*(p + 1)   = %d\\n", *(p + 1));\n\n    return 0;\n}',
          language: 'c',
          explanation: 'This example breaks the illusion of C\'s array syntax. The bracket notation `[i]` is not a fundamental operation; it is just "syntactic sugar" (a convenient shorthand) for `*(pointer + i)`. The compiler literally translates `arr[1]` into `*(arr + 1)` before compiling it. This reveals a profound truth: because bracket notation is just pointer math, you can use bracket notation on raw pointers just as easily as on arrays.',
          expectedOutput: 'arr[1]     = 20\n*(arr + 1) = 20\np[1]       = 20\n*(p + 1)   = 20',
          lineBreakdown: [
            { lineNumber: 9, code: '    printf("*(arr + 1) = %d\\n", *(arr + 1));', explanation: '`arr` gives the base address. `+ 1` uses pointer arithmetic to calculate the address of the next integer (jumping 4 bytes forward). The leading `*` dereferences that address to extract the value 20.' },
            { lineNumber: 10, code: '    printf("p[1]       = %d\\n", p[1]);', explanation: 'Even though `p` is just a pointer, we can use array brackets on it. The compiler translates this to `*(p + 1)`, which works identically.' },
          ],
          relatedTopicIds: [],
        },
      ],
      commonMistakes: [
        {
          id: 'u2-t4-s1-cm1',
          title: 'Trying to reassign an array name',
          wrongCode: 'int arr[5];\nint arr2[5];\narr = arr2; /* Error! */',
          correctCode: 'int *p1;\nint arr2[5];\np1 = arr2; /* OK */',
          explanation: 'Because an array decays to a pointer, many beginners assume an array name IS a pointer variable. It is not. An array name is fundamentally tied to the specific block of memory allocated for it. You can think of it as a constant address label. You cannot tell an array to "point somewhere else" any more than you can reassign the literal number 5 to equal 6. Pointer variables, on the other hand, are designed to have their stored addresses changed freely.',
          consequence: 'Compilation error. The compiler will complain that you are trying to assign to an expression with an array type (a non-modifiable lvalue).',
        },
        {
          id: 'u2-t4-s1-cm2',
          title: 'Missing parentheses in pointer arithmetic',
          wrongCode: 'int val = *arr + 1; /* Gets arr[0] and adds 1 to it */',
          correctCode: 'int val = *(arr + 1); /* Gets arr[1] */',
          explanation: 'Pointer dereferencing `*` and addition `+` have different precedence levels in C. The dereference operator `*` binds much tighter than addition. Therefore, `*arr + 1` is evaluated as `(*arr) + 1`. It goes to the first element (which might be 10), and adds 1 to that value (yielding 11). It does NOT advance to the next element. To perform pointer arithmetic first, you must use parentheses: `*(arr + 1)`.',
          consequence: 'Severe logical error. You will fetch the wrong element entirely and perform unintended arithmetic on it, leading to bugs that the compiler will not catch.',
        },
      ],
      interviewCallouts: [
        {
          id: 'u2-t4-s1-ic1',
          title: 'Is an array the same as a pointer?',
          content: 'No. This is a classic trick question to test if you truly understand array decay. An array is a block of contiguous memory that physically stores multiple values. A pointer is a single variable (taking up 8 bytes on modern systems) that simply stores a memory address. They are fundamentally different things. The confusion arises because, in most expressions, C automatically converts the array name into a pointer to its first element. They are not the same, but the compiler makes them look the same to make array traversal easier.',
          relatedTopicIds: [],
          frequency: 'common',
        },
      ],
      checkpoints: [
        {
          id: 'u2-t4-s1-cp1',
          title: 'Array and Pointer Syntax',
          description: 'Verify understanding of array decay and the fundamental equivalence between array notation and pointer arithmetic.',
          criteria: [
            'Rewrite a standard array access `arr[i]` using strict pointer arithmetic',
            'Explain the fundamental difference between an array name and a pointer variable',
            'Trace the step-by-step evaluation of the expression `*(arr + 2)`',
          ],
          topicId: 'u2-t4',
        },
      ],
      revisionCards: [
        {
          id: 'u2-t4-s1-rc1',
          front: 'What does "array decay" mean in C?',
          back: 'Array decay is the automatic conversion performed by the compiler where an array name, when used in an expression, evaluates to a pointer to its very first element (i.e., `arr` becomes `&arr[0]`).',
          topicId: 'u2-t4',
          tags: ['arrays', 'pointers', 'decay'],
        },
        {
          id: 'u2-t4-s1-rc2',
          front: 'How does the compiler rewrite the array indexing syntax `arr[i]`?',
          back: 'The compiler literally translates `arr[i]` into pointer arithmetic: `*(arr + i)`. It takes the base address, adds `i` steps forward, and dereferences the resulting address.',
          topicId: 'u2-t4',
          tags: ['arrays', 'pointers', 'syntax'],
        },
        {
          id: 'u2-t4-s1-rc3',
          front: 'Why is it legal to write `p++` for a pointer, but illegal to write `arr++` for an array name?',
          back: 'Because an array name evaluates to a constant memory address (where the array physically lives). It is not a variable, so you cannot change it. A pointer is a variable designed specifically to hold and change addresses.',
          topicId: 'u2-t4',
          tags: ['arrays', 'pointers'],
        },
      ],
    },
    {
      id: 'u2-t4-s2',
      title: 'Traversing Arrays with Pointers',
      slug: 'traversing-with-pointers',
      description: `Pointer traversal is the technique of iterating through an array by maintaining a pointer that physically advances through memory, rather than using an integer index to compute element addresses from the base on each iteration. The traversal begins by initialising a pointer to the array's first element (int *p = arr), and on each loop iteration, the pointer is incremented (p++) to advance it to the next element. Because the compiler knows the type of the pointed-to data, p++ does not advance the address by one byte — it advances by sizeof(int) bytes (typically 4), ensuring the pointer always lands precisely on the next element's starting address. The loop terminates when the pointer reaches a precomputed boundary: int *end = arr + n, where n is the number of elements.

The one-past-the-end pointer is a carefully defined concept in the C standard. Computing the address arr + n is legal even though no valid element exists at that location; the standard explicitly permits forming (but not dereferencing) a pointer to the position immediately following the last element, specifically so it can serve as an end sentinel in loop conditions. Dereferencing this pointer — reading *end — is undefined behavior, because that memory belongs to some other variable or is unallocated. The correct usage is purely as a comparison target: while (p < end). This pattern is directly analogous to iterator-based loops in C++ (begin/end) and is the idiomatic foundation of pointer-based algorithms throughout the C standard library.

Historically, pointer traversal (*p++) was measurably faster than indexed access (arr[i]) because the former required only an increment operation per iteration, while the latter required a multiplication (i * sizeof(type)) and addition on every access. Modern optimising compilers (GCC, Clang, MSVC) perform strength reduction and loop transformations that make both approaches generate identical machine code, so the choice between pointer traversal and index-based loops is now purely a matter of readability and algorithmic clarity rather than performance.`,
      keyPoints: [
        'Pointer traversal means creating a separate pointer variable `p`, pointing it to the array, and physically changing the address `p` holds using the `++` increment operator.',
        'When you write `p++`, the compiler is smart enough to know the size of the data type. If `p` points to a 4-byte `int`, `p++` moves the address forward by exactly 4 bytes to perfectly land on the next integer.',
        'To know when to stop traversing, we calculate an "end" pointer that points to the memory address exactly one element PAST the end of our array. We can then use a `while(p < end)` loop.',
        'In the early days of C, pointer traversal (`*p++`) was noticeably faster than array indexing (`arr[i]`) because it avoided an addition operation on every loop cycle. Today, modern compilers optimize both syntaxes into the exact same blazing-fast machine code, so you can choose whichever is more readable.',
      ],
      codeExamples: [
        {
          id: 'u2-t4-s2-ex1',
          title: 'Iterating with p++',
          code: '#include <stdio.h>\n\nint main(void) {\n    int arr[4] = {10, 20, 30, 40};\n    int *p = arr; /* Points to start */\n\n    /* Iterate 4 times */\n    for (int i = 0; i < 4; i++) {\n        printf("%d ", *p);\n        p++; /* Move pointer to next element */\n    }\n    printf("\\n");\n    return 0;\n}',
          language: 'c',
          explanation: 'This code completely abandons the integer index `i`. Instead, the loop relies entirely on the pointer `p`. In each iteration, we use `*p` to grab the value at the current location. Then, the crucial step `p++` executes. Because `p` is an `int*`, the compiler automatically adds 4 bytes (the size of an integer) to the memory address stored in `p`, sliding it perfectly onto the next element in the array.',
          expectedOutput: '10 20 30 40 ',
          lineBreakdown: [
            { lineNumber: 9, code: '        printf("%d ", *p);', explanation: 'The `*` dereferences the pointer, fetching the actual integer value living at the address currently stored in `p`.' },
            { lineNumber: 10, code: '        p++;', explanation: 'Advances the pointer to the next element. The compiler automatically scales this increment by the size of the data type (moving 4 bytes for an int).' },
          ],
          relatedTopicIds: [],
        },
        {
          id: 'u2-t4-s2-ex2',
          title: 'Pointer Comparison Loop',
          code: '#include <stdio.h>\n\nint main(void) {\n    int arr[4] = {10, 20, 30, 40};\n    int *p = arr;           /* Start address */\n    int *end = arr + 4;     /* Address just PAST the last element */\n\n    /* Loop as long as p hasn\'t reached the end address */\n    while (p < end) {\n        printf("%d ", *p);\n        p++;\n    }\n    printf("\\n");\n    return 0;\n}',
          language: 'c',
          explanation: 'This is an incredibly powerful and common idiom in professional C code, particularly in the standard library. Instead of counting how many times we loop with an integer `i`, we establish a hard boundary in memory. We calculate the address exactly one slot PAST the end of our array (`end`). Since memory addresses strictly increase as you move forward in RAM, we simply slide our `p` pointer forward until its address reaches the `end` address.',
          expectedOutput: '10 20 30 40 ',
          lineBreakdown: [
            { lineNumber: 6, code: '    int *end = arr + 4;', explanation: 'Calculates the address exactly one element beyond the last valid integer. The C standard specifically guarantees it is legal to compute this "one-past-the-end" address, as long as you never dereference it.' },
            { lineNumber: 9, code: '    while (p < end) {', explanation: 'Memory addresses are just numbers, so we can use standard comparison operators. The loop runs as long as `p` represents a lower memory address than our `end` boundary.' },
          ],
          relatedTopicIds: [],
        },
      ],
      commonMistakes: [
        {
          id: 'u2-t4-s2-cm1',
          title: 'Dereferencing the end pointer',
          wrongCode: 'int *end = arr + n;\nint val = *end; /* Error! */',
          correctCode: 'int *end = arr + n;\nwhile(p < end) { ... }',
          explanation: 'When traversing an array of size `n`, the valid indices are `0` through `n-1`. The address `arr + n` points to memory exactly one element PAST the valid bounds of the array. The C standard specifically allows you to compute and store this out-of-bounds address (to use as an end boundary for loops), but it strictly forbids you from ever dereferencing it to read or write data. That memory belongs to something else.',
          consequence: 'Undefined behavior. Reading it will give you garbage data belonging to some adjacent variable. Writing to it will silently corrupt that adjacent variable, causing a bug that is notoriously difficult to trace.',
        },
      ],
      interviewCallouts: [
        {
          id: 'u2-t4-s2-ic1',
          title: 'Pointer traversal vs Index traversal',
          content: 'In decades past, using pointer traversal (`*p++`) was significantly faster than index traversal (`arr[i]`) because computing `arr + i` required the CPU to perform a fresh multiplication (`i * sizeof(type)`) and addition on every single iteration. Today, however, modern optimizing compilers (like GCC or Clang) will analyze both loops and translate them into the exact same heavily-optimized assembly code. You should choose the syntax that makes the intent of your algorithm clearest.',
          relatedTopicIds: [],
          frequency: 'rare',
        },
      ],
      checkpoints: [
        {
          id: 'u2-t4-s2-cp1',
          title: 'Pointer Iteration',
          description: 'Verify the ability to confidently iterate across contiguous memory using raw pointer manipulation.',
          criteria: [
            'Write a while loop that traverses an array without using integer indices',
            'Explain why the compiler knows exactly how many bytes to jump when executing `p++`',
            'Explain the specific rules regarding the "one-past-the-end" address boundary',
          ],
          topicId: 'u2-t4',
        },
      ],
      revisionCards: [
        {
          id: 'u2-t4-s2-rc1',
          front: 'If `p` is an integer pointer aimed at an array, what precisely happens in memory when you execute `p++`?',
          back: 'The compiler modifies the memory address stored inside `p`, increasing it by the size of the data type (e.g., adding 4 bytes for an int). This mathematically slides the pointer to land perfectly on the next element.',
          topicId: 'u2-t4',
          tags: ['pointers', 'iteration'],
        },
        {
          id: 'u2-t4-s2-rc2',
          front: 'Is it legal in C to compute the address immediately following the end of an array (e.g., `arr + n`)?',
          back: 'Yes, computing the "one-past-the-end" address is explicitly legal in the C standard, primarily so you can use it as a boundary check in while loops. However, dereferencing that address is strictly illegal.',
          topicId: 'u2-t4',
          tags: ['pointers', 'bounds'],
        },
      ],
    },
  ],

  theoryQuestions: [
    {
      id: 'u2-t4-q1',
      type: 'mcq',
      topicId: 'u2-t4',
      difficulty: 'beginner',
      question: 'For an array int arr[5];, which of the following is equivalent to arr[2]?',
      options: ['*(arr + 2)', '*arr + 2', '&arr + 2', 'arr + 2'],
      correctAnswer: '*(arr + 2)',
      explanation: 'Array bracket syntax `arr[2]` is strictly translated by the compiler into `*(arr + 2)`. It takes the base address, steps forward by 2 elements, and dereferences the result to get the value. Adding `2` before dereferencing is crucial.',
      tags: ['arrays', 'pointers', 'equivalence'],
    },
    {
      id: 'u2-t4-q2',
      type: 'predict-output',
      topicId: 'u2-t4',
      difficulty: 'intermediate',
      question: 'What is the output?',
      code: '#include <stdio.h>\nint main(void) {\n    int a[3] = {10, 20, 30};\n    int *p = a;\n    printf("%d ", *p++);\n    printf("%d", *p);\n    return 0;\n}',
      correctAnswer: '10 20',
      explanation: 'The expression `*p++` combines dereferencing and post-incrementing. Because `++` is a postfix operator here, it evaluates to the original address first. The `*` dereferences that original address (printing 10). Only after the entire expression evaluates does `p` increment to point to the next element. The second print confirms `p` now points to 20.',
      tags: ['pointers', 'increment'],
    },
    {
      id: 'u2-t4-q3',
      type: 'true-false',
      topicId: 'u2-t4',
      difficulty: 'beginner',
      question: 'You can assign a new address to an array name (e.g., arr = p;).',
      correctAnswer: false,
      explanation: 'An array name is a non-modifiable lvalue; it represents the fixed memory address where the array was physically allocated by the operating system. You cannot change where an array "points" any more than you can change where a building is located.',
      tags: ['arrays', 'pointers'],
    },
    {
      id: 'u2-t4-q4',
      type: 'spot-bug',
      topicId: 'u2-t4',
      difficulty: 'intermediate',
      question: 'Spot the bug:',
      code: 'int arr[3] = {1, 2, 3};\nfor (int i = 0; i < 3; i++) {\n    printf("%d ", *arr);\n    arr++;\n}',
      correctAnswer: 'arr++ is illegal.',
      explanation: 'The array name `arr` is a constant memory address, not a variable. The `++` operator requires a modifiable variable because it tries to change the value stored within it. You must create a separate pointer variable `int *p = arr;` and increment `p`.',
      tags: ['arrays', 'increment'],
    },
    {
      id: 'u2-t4-q5',
      type: 'mcq',
      topicId: 'u2-t4',
      difficulty: 'advanced',
      question: 'If p is a pointer, what does the expression ++*p do?',
      options: [
        'Increments the pointer to the next element',
        'Dereferences p, then increments the value found there',
        'Increments the pointer, then dereferences it',
        'Compilation error',
      ],
      correctAnswer: 'Dereferences p, then increments the value found there',
      explanation: 'Because both prefix `++` and dereference `*` associate from right to left, `++*p` evaluates as `++(*p)`. It first dereferences `p` to access the actual data (e.g., the number 5), and then applies the prefix increment to that data, changing it to 6. If you wanted to increment the pointer first, you would write `*++p`.',
      tags: ['pointers', 'precedence'],
    },
  ],

  programmingProblems: [
    {
      id: 'u2-t4-new-easy',
      title: 'Array Reverse',
      topicId: 'u2-t4',
      difficulty: 'beginner',
      problemStatement: 'Reverse an array of N elements in place.',
      constraints: ['Do not use a secondary array'],
      sampleInput: '1 2 3',
      sampleOutput: '3 2 1',
      hints: ['Swap elements from both ends moving towards the center'],
      solution: '/* Array reverse implementation */',
      solutionExplanation: 'Swaps index i and N-1-i.',
      dryRun: [],
      tags: ['arrays']
    },
    {
      id: 'u2-t4-new-med',
      title: 'Matrix Diagonal Sum',
      topicId: 'u2-t4',
      difficulty: 'intermediate',
      problemStatement: 'Calculate the sum of the main diagonal of an NxN matrix.',
      constraints: ['Matrix is guaranteed to be square'],
      sampleInput: '1 2\n3 4',
      sampleOutput: '5',
      hints: ['Main diagonal elements have index [i][i]'],
      solution: '/* Diagonal sum implementation */',
      solutionExplanation: 'Iterates and sums arr[i][i].',
      dryRun: [],
      tags: ['matrix']
    },
    {
      id: 'u2-t4-new-hard',
      title: 'Recursive GCD',
      topicId: 'u2-t4',
      difficulty: 'advanced',
      problemStatement: 'Find the Greatest Common Divisor of two numbers using recursion (Euclidean algorithm).',
      constraints: ['Must use recursion'],
      sampleInput: '48 18',
      sampleOutput: '6',
      hints: ['gcd(a, b) = gcd(b, a % b)'],
      solution: '/* Recursive GCD implementation */',
      solutionExplanation: 'Implements Euclidean algorithm recursively.',
      dryRun: [],
      tags: ['recursion']
    },
    {
      id: 'u2-t4-p1',
      title: 'Sum Array using Pointers',
      topicId: 'u2-t4',
      difficulty: 'beginner',
      problemStatement: 'Write a program that initializes an array of 5 integers. Use a pointer and a while loop (comparing the pointer to the end boundary) to calculate the sum of the elements.',
      constraints: ['Do not use integer indices (like arr[i])', 'Use pointer iteration (p++)'],
      sampleInput: '',
      sampleOutput: 'Sum = 15',
      hints: ['int *end = arr + 5;', 'while (p < end)'],
      solution: '#include <stdio.h>\n\nint main(void) {\n    int arr[5] = {1, 2, 3, 4, 5};\n    int sum = 0;\n    \n    int *p = arr;\n    int *end = arr + 5;\n    \n    while (p < end) {\n        sum += *p;\n        p++;\n    }\n    \n    printf("Sum = %d\\n", sum);\n    return 0;\n}',
      solutionExplanation: 'This demonstrates the core paradigm of raw pointer traversal. Instead of an integer tracking our progress, the pointer `p` itself is our progress tracker. It physically slides across memory byte-by-byte. The `end` pointer acts as our finish line, ensuring we stop exactly when we run out of valid memory.',
      dryRun: [
        { step: 1, line: 9, variables: { p: '&arr[0]', end: '&arr[5]', sum: '0' }, output: '', explanation: 'Initialize `p` to the start, and `end` to the one-past-the-end boundary.' },
        { step: 2, line: 11, variables: { '*p': '1', sum: '1' }, output: '', explanation: 'Dereference `p` to read the first integer (1), and add it to `sum`.' },
        { step: 3, line: 12, variables: { p: '&arr[1]' }, output: '', explanation: 'Slide the pointer `p` exactly 4 bytes forward to land on the next integer in memory.' },
      ],
      tags: ['pointers', 'iteration', 'arrays'],
    },
    {
      id: 'u2-t4-p2',
      title: 'Reverse Array using Two Pointers',
      topicId: 'u2-t4',
      difficulty: 'intermediate',
      problemStatement: 'Write a function reverse(int *arr, int n) that reverses an array in-place using two pointers: one starting at the beginning, one starting at the end.',
      constraints: ['Do not use array indices (like arr[i]) inside the loop'],
      sampleInput: '5\n10 20 30 40 50',
      sampleOutput: '50 40 30 20 10 ',
      hints: ['int *left = arr;', 'int *right = arr + n - 1;', 'Loop condition: while (left < right)', 'Swap *left and *right, then left++, right--'],
      solution: '#include <stdio.h>\n\nvoid reverse(int *arr, int n) {\n    int *left = arr;\n    int *right = arr + n - 1;\n    \n    while (left < right) {\n        int temp = *left;\n        *left = *right;\n        *right = temp;\n        \n        left++;\n        right--;\n    }\n}\n\nint main(void) {\n    int data[] = {10, 20, 30, 40, 50};\n    reverse(data, 5);\n    for(int i=0; i<5; i++) printf("%d ", data[i]);\n    return 0;\n}',
      solutionExplanation: 'This is an extremely common professional algorithm known as the "Two Pointer Technique". By placing one pointer at the start and one at the tail, we can walk them toward each other, swapping the physical memory contents at their addresses. When the pointers cross paths, we know we have hit the middle and the array is perfectly reversed. No integer indices or extra arrays are needed.',
      dryRun: [
        { step: 1, line: 4, variables: { '*left': '10', '*right': '50' }, output: '', explanation: 'Pointers initialized. `left` points to the first element, `right` points to the last.' },
        { step: 2, line: 8, variables: { '*left': '50', '*right': '10' }, output: '', explanation: 'The data at the memory addresses is physically swapped using a temporary variable.' },
        { step: 3, line: 12, variables: { left: '&arr[1]', right: '&arr[3]' }, output: '', explanation: '`left` slides forward, `right` slides backward. The loop continues until they meet in the middle.' },
      ],
      tags: ['pointers', 'two-pointers', 'arrays'],
    },
    {
      id: 'u2-t4-p3',
      title: 'String Length using Pointers',
      topicId: 'u2-t4',
      difficulty: 'intermediate',
      problemStatement: 'Strings in C are just arrays of characters ending in a null character \'\\0\'. Write a function my_strlen(char *str) that uses pointer arithmetic to find and return the length of the string.',
      constraints: ['Do not use string.h', 'Do not use an integer counter variable'],
      sampleInput: '"Hello"',
      sampleOutput: '5',
      hints: ['Create a pointer p = str.', 'while (*p != \'\\0\') { p++; }', 'Return the difference between p and str.'],
      solution: '#include <stdio.h>\n\nint my_strlen(char *str) {\n    char *p = str;\n    while (*p != \'\\0\') {\n        p++;\n    }\n    return p - str; /* Pointer arithmetic! */\n}\n\nint main(void) {\n    char msg[] = "Hello";\n    printf("%d\\n", my_strlen(msg));\n    return 0;\n}',
      solutionExplanation: 'This is an exact replication of how the standard library `strlen()` function is implemented under the hood. C strings do not store their length; they just end with a hidden `\\0` null terminator. To find the length, we slide a pointer forward until it hits that null terminator. The profound C trick here is `return p - str;`. Subtracting two pointers calculates the exact number of elements between them, instantly giving us the string length without needing a separate counter variable.',
      dryRun: [
        { step: 1, line: 4, variables: { p: '&msg[0]' }, output: '', explanation: 'The sliding pointer `p` is initialized to point to the start of the string (\'H\').' },
        { step: 2, line: 6, variables: { p: '&msg[5]' }, output: '', explanation: 'The loop slides `p` forward byte by byte. It finally stops when `*p` reveals the hidden \'\\0\' terminator.' },
        { step: 3, line: 8, variables: {}, output: '', explanation: 'Subtracting the base address from the end address yields the exact element count between them: 5.' },
      ],
      tags: ['pointers', 'strings', 'pointer-subtraction'],
    },
  ],
};
