import type { Topic } from '../../../../types';

export const arrays1d: Topic = {
  id: 'u2-t1',
  unitId: 'unit-2',
  title: 'Arrays — 1D',
  slug: 'arrays-1d',
  description: `An array is the simplest and most fundamental data structure in C: a fixed-size, contiguous block of memory that stores multiple values of the same type under a single name. When you declare int scores[5], the compiler allocates exactly 5 × sizeof(int) = 20 consecutive bytes on the stack (assuming 4-byte integers), with no gaps, padding, or metadata between elements. This contiguous layout is not merely an implementation detail — it is the defining architectural property that makes arrays fast, makes pointer arithmetic meaningful, and underlies the deep equivalence between arrays and pointers that is central to C programming.

Because elements are stored contiguously and the compiler knows the size of each element, the address of any element can be computed in constant time using the formula: address = base_address + (index × element_size). This is why array indices start at 0 in C: the first element has an offset of zero bytes from the base address, the second element has an offset of one element's width, and so on. This constant-time random access is what distinguishes arrays from linked data structures and is the performance foundation for algorithms like binary search.

C performs no bounds checking on array accesses. Accessing scores[5] in a 5-element array (valid indices are 0–4) reads or writes memory beyond the array's allocated region — silently corrupting adjacent stack variables, function return addresses, or other critical data. This absence of runtime safety checking is a deliberate design choice that eliminates bounds-check overhead in performance-critical code, but it makes buffer overflow vulnerabilities the single most exploited class of security bugs in C programs.`,
  difficulty: 'beginner',
  prerequisites: ['u1-t8'],
  estimatedMinutes: 60,
  subtopics: [
    {
      id: 'u2-t1-s1',
      title: 'Array Fundamentals',
      slug: 'array-fundamentals',
      description: `An array in C is a fixed-size, contiguous block of memory that stores multiple values of the same type under a single name. When you declare int scores[5], the compiler allocates exactly 5 * sizeof(int) = 20 consecutive bytes on the stack (assuming 4-byte ints), with no gaps or padding between elements. Element 0 occupies the first 4 bytes, element 1 the next 4, and so on. This contiguous layout is not an implementation detail you can ignore — it is the defining characteristic that makes arrays fast and makes the pointer-array relationship in C possible.

The reason array indices start at 0 is a direct consequence of this memory layout. An index is not an arbitrary label; it is an offset from the base address of the array, measured in units of the element size. The compiler computes the address of element i using the formula: address = base_address + i * sizeof(element). For the first element, i is 0, so the address is simply the base address itself — zero elements away from the start. This is why zero-based indexing is not a quirky convention but a natural consequence of how address arithmetic works at the hardware level. Most C-derived languages (Java, JavaScript, Python) inherited this convention for the same reason.

The critical tradeoff of C arrays is the total absence of bounds checking. When you write scores[10] on a 5-element array, the compiler does not insert any code to verify that 10 is a valid index. It simply computes base_address + 10 * 4 and accesses whatever memory happens to be at that location — memory that does not belong to the array. Reading out of bounds produces garbage data; writing out of bounds silently corrupts other variables, the function's return address, or other program state. This is undefined behavior, and it is the root cause of buffer overflow vulnerabilities that have been exploited in countless security attacks. C trusts the programmer to stay within bounds, and violating that trust has consequences ranging from subtle data corruption to immediate crashes.`,
      keyPoints: [
        'An array stores multiple values of the same type under a single name, allowing you to work with collections of data using loops instead of declaring separate variables. Instead of int score1, score2, score3, score4, score5, you declare int scores[5] and access each score by index.',
        'The elements are stored in contiguous (adjacent) memory. If int scores[5] starts at address 1000, then scores[0] is at 1000, scores[1] is at 1004, scores[2] is at 1008, and so on (assuming 4-byte ints). There are no gaps between elements, which is why pointer arithmetic works seamlessly with arrays.',
        'Arrays are zero-indexed: the first element is at index 0, the second at index 1, and the last at index N-1 (where N is the array size). This is not arbitrary — the index represents the offset from the start of the array in memory. The first element is at offset 0 (it is at the start), which is why its index is 0.',
        'Declaration syntax: type arrayName[size] allocates space for size elements of the given type. The size must be a compile-time constant in standard C (C11 does support variable-length arrays, but they are optional and controversial). Examples: int scores[5], float temperatures[100], char name[50].',
        'Initialization: you can provide initial values in braces: int arr[3] = {10, 20, 30}. If you provide fewer values than the array size, the remaining elements are automatically initialized to 0: int arr[5] = {1, 2} sets arr[0]=1, arr[1]=2, arr[2]=arr[3]=arr[4]=0. You can omit the size if providing a full initializer: int arr[] = {5, 10, 15} creates a 3-element array.',
        'C performs absolutely no bounds checking on array accesses. If you write arr[10] on a 5-element array, C will happily compute the address and read or write whatever is at that location — which is memory that does not belong to the array. This is undefined behavior: it might produce garbage, silently corrupt another variable, or crash the program.',
      ],
      codeExamples: [
        {
          id: 'u2-t1-s1-ex1',
          title: 'Declaration and Initialization',
          code: '#include <stdio.h>\n\nint main(void) {\n    int a[5]; /* Uninitialized garbage */\n    int b[3] = {10, 20, 30}; /* Full init */\n    int c[5] = {1, 2}; /* Partial init: rest are 0 */\n    int d[] = {5, 10, 15}; /* Size inferred as 3 */\n\n    printf("b[0]=%d, b[2]=%d\\n", b[0], b[2]);\n    printf("c[4]=%d (auto zeroed)\\n", c[4]);\n    return 0;\n}',
          language: 'c',
          explanation: 'This program demonstrates the four ways to declare and initialize arrays. The uninitialized array a contains garbage (whatever happened to be in that stack memory). The fully initialized array b has all three values specified. The partially initialized array c demonstrates a crucial rule: when you provide an initializer list, any elements not explicitly listed are automatically set to 0 — this is guaranteed by the C standard, not just a common behavior. The compiler-sized array d lets the compiler count the initializer values and determine the size automatically (3 in this case). The partial initialization trick is especially useful: int arr[1000] = {0} initializes all 1000 elements to 0 with a single line.',
          expectedOutput: 'b[0]=10, b[2]=30\nc[4]=0 (auto zeroed)',
          lineBreakdown: [
            { lineNumber: 4, code: '    int a[5];', explanation: 'Creates space for 5 ints. Contains garbage values.' },
            { lineNumber: 6, code: '    int c[5] = {1, 2};', explanation: 'First two are 1 and 2. The remaining three elements are automatically set to 0.' },
            { lineNumber: 7, code: '    int d[] = {5, 10, 15};', explanation: 'Compiler counts the elements and sets size to 3.' },
          ],
          relatedTopicIds: ['u1-t4'],
        },
        {
          id: 'u2-t1-s1-ex2',
          title: 'Iterating Through an Array',
          code: '#include <stdio.h>\n\nint main(void) {\n    int scores[4] = {85, 92, 78, 90};\n    int sum = 0;\n\n    for (int i = 0; i < 4; i++) {\n        printf("Score %d: %d\\n", i + 1, scores[i]);\n        sum += scores[i];\n    }\n    \n    printf("Average: %.2f\\n", sum / 4.0f);\n    return 0;\n}',
          language: 'c',
          explanation: 'This is the fundamental array-processing pattern that you will use hundreds of times: a for loop that iterates from index 0 to size-1, accessing each element in sequence. The loop variable i serves as the index, and scores[i] accesses the element at that position. Notice that the loop condition is i < 4 (strictly less than the size), not i <= 4, because the valid indices are 0, 1, 2, 3 — the last index is always one less than the size. The running sum pattern (accumulating into a variable inside the loop) and the average calculation (dividing by a float to avoid integer truncation) are both patterns you will see repeatedly.',
          expectedOutput: 'Score 1: 85\nScore 2: 92\nScore 3: 78\nScore 4: 90\nAverage: 86.25',
          lineBreakdown: [
            { lineNumber: 7, code: '    for (int i = 0; i < 4; i++) {', explanation: 'Loop runs for i = 0, 1, 2, 3. Stops before 4.' },
            { lineNumber: 9, code: '        sum += scores[i];', explanation: 'Access element at index i and add to sum.' },
            { lineNumber: 12, code: '    printf("Average: %.2f\\n", sum / 4.0f);', explanation: 'Divide by 4.0f to ensure floating-point division.' },
          ],
          relatedTopicIds: ['u1-t8'],
        },
        {
          id: 'u2-t1-s1-ex3',
          title: 'The Danger of Out-of-Bounds Access',
          code: '#include <stdio.h>\n\nint main(void) {\n    int arr[3] = {10, 20, 30};\n    \n    /* VALID indices: 0, 1, 2 */\n    printf("arr[2] = %d\\n", arr[2]);\n    \n    /* UNDEFINED BEHAVIOR - Out of bounds! */\n    /* printf("arr[3] = %d\\n", arr[3]); */\n    /* arr[5] = 99; */ \n    \n    printf("Always stay within 0 to size-1.\\n");\n    return 0;\n}',
          language: 'c',
          explanation: 'This example demonstrates the most dangerous property of C arrays: there is no bounds checking. The array has 3 elements with valid indices 0, 1, and 2. Accessing arr[3] is out of bounds — it reads memory immediately after the array, which might belong to another variable, to the function\'s return address, or to unmapped memory. Reading out of bounds typically produces a garbage value or crashes with a segmentation fault. Writing out of bounds (arr[5] = 99) is even worse: it silently overwrites whatever data happens to be at that memory location, corrupting your program\'s state in ways that may not manifest until much later. This is the root cause of buffer overflow vulnerabilities.',
          expectedOutput: 'arr[2] = 30\nAlways stay within 0 to size-1.',
          lineBreakdown: [
            { lineNumber: 7, code: '    printf("arr[2] = %d\\n", arr[2]);', explanation: 'Last valid element is at index size-1 (3-1=2).' },
            { lineNumber: 10, code: '    /* printf("arr[3] = %d\\n", arr[3]); */', explanation: 'Reading past the array. Might print garbage, might crash.' },
            { lineNumber: 11, code: '    /* arr[5] = 99; */', explanation: 'Writing past the array. Silently corrupts other variables.' },
          ],
          relatedTopicIds: ['u1-t10'],
        },
      ],
      commonMistakes: [
        {
          id: 'u2-t1-s1-cm1',
          title: 'Off-by-One Error',
          wrongCode: 'int arr[5];\nfor(int i = 1; i <= 5; i++) { arr[i] = 0; }',
          correctCode: 'int arr[5];\nfor(int i = 0; i < 5; i++) { arr[i] = 0; }',
          explanation: 'This is the most common bug in array programming, and it stems from the fact that arrays are zero-indexed but humans naturally count from 1. A 5-element array has indices 0 through 4. Starting the loop at i = 1 misses arr[0] (the first element), and ending at i <= 5 accesses arr[5] (which is out of bounds). The correct pattern is always: start at 0, loop while i < size. This off-by-one error is so pervasive that it has its own name in computer science, and experienced programmers develop a reflexive habit of checking loop bounds against array sizes.',
          consequence: 'Memory corruption. arr[5] writes to memory outside the array, potentially overwriting the next variable on the stack or corrupting the function\'s return address.',
        },
        {
          id: 'u2-t1-s1-cm2',
          title: 'Assigning arrays directly',
          wrongCode: 'int a[3] = {1, 2, 3};\nint b[3];\nb = a; /* Error! */',
          correctCode: 'int a[3] = {1, 2, 3};\nint b[3];\nfor(int i = 0; i < 3; i++) { b[i] = a[i]; }',
          explanation: 'Unlike structs or basic types, arrays cannot be assigned with the = operator in C. The expression b = a attempts to assign to the entire array, which C does not support because an array name is not an assignable value (it is a constant pointer to the first element). To copy the contents of one array to another, you must copy each element individually using a loop, or use the memcpy function from <string.h>. This restriction exists because C was designed for low-level control, and implicit copying of large memory blocks would hide potentially expensive operations.',
          consequence: 'Compilation error: "assignment to expression with array type." The = operator is simply not defined for array operands in C.',
        },
      ],
      interviewCallouts: [
        {
          id: 'u2-t1-s1-ic1',
          title: 'Why do array indices start at 0?',
          content: 'This is a question about C\'s memory model. An array index is an offset from the base address of the array, measured in units of the element size. The first element sits at offset 0 (zero elements away from the start), so its index is 0. The address formula is: element_address = base_address + (index * sizeof(element)). With zero-based indexing, there is no need to subtract 1 in this formula, which makes the hardware-level address calculation simpler and slightly faster. Most C-derived languages (Java, Python, JavaScript) inherited this convention for the same reason.',
          relatedTopicIds: [],
          frequency: 'common',
        },
      ],
      checkpoints: [
        {
          id: 'u2-t1-s1-cp1',
          title: 'Array Basics',
          description: 'Verify understanding of array declaration and indexing.',
          criteria: [
            'Declare an array of 10 floats initialized to 0',
            'Write a loop to print all elements',
            'Identify the valid index range for an array of size N',
          ],
          topicId: 'u2-t1',
        },
      ],
      revisionCards: [
        {
          id: 'u2-t1-s1-rc1',
          front: 'What are the valid indices for int data[10];?',
          back: '0 through 9. (0 to size-1). Index 10 is out of bounds!',
          topicId: 'u2-t1',
          tags: ['arrays', 'indexing'],
        },
        {
          id: 'u2-t1-s1-rc2',
          front: 'What happens if you do int arr[5] = {1};?',
          back: 'The first element arr[0] is 1. The remaining 4 elements are automatically initialized to 0.',
          topicId: 'u2-t1',
          tags: ['arrays', 'initialization'],
        },
        {
          id: 'u2-t1-s1-rc3',
          front: 'Does C check array bounds?',
          back: 'No. C trusts the programmer. Accessing out of bounds causes undefined behavior (garbage reads, silent corruption, or crashes).',
          topicId: 'u2-t1',
          tags: ['arrays', 'safety'],
        },
      ],
    },
    {
      id: 'u2-t1-s2',
      title: 'Array Algorithms & Functions',
      slug: 'array-algorithms',
      description: `When you pass an array to a function in C, something fundamentally different happens compared to passing a scalar variable like an int or float. The array does not get copied — instead, it "decays" into a pointer to its first element. The function parameter int arr[] is semantically identical to int *arr; both are pointers that hold the address of the array's first element. This means the function operates on the original memory, not a copy, so any modifications to array elements inside the function are immediately visible to the caller. This behavior is the opposite of pass-by-value for scalars, and it exists for a practical reason: copying an entire array of 10,000 integers (40,000 bytes) every time you call a function would be prohibitively expensive.

The critical consequence of array decay is that the function has no way to determine the array's size from the pointer alone. Inside the function, sizeof(arr) returns the size of the pointer variable (typically 8 bytes on a 64-bit system), not the total size of the original array. The sizeof(arr)/sizeof(arr[0]) idiom for computing array length works only in the scope where the array was originally declared. This is why you must always pass the array size as a separate parameter — a discipline so important that it should be considered a mandatory part of every function signature that accepts an array. Forgetting to do so leads to functions that silently process the wrong number of elements, producing incorrect results without any compiler warning.

Building on this foundation, the standard array algorithms — finding the maximum, computing a sum, reversing in place — all follow a common pattern: iterate through the array with a for loop from index 0 to n-1, applying the operation to each element. When finding a maximum, the correct initialization is max = arr[0] (not 0 or INT_MIN), because the maximum must always be an element that actually exists in the array. When reversing, the two-pointer technique swaps arr[i] with arr[n-1-i] for exactly n/2 iterations — swapping for n iterations would restore the original order. These patterns are the building blocks of every array-based algorithm you will encounter.`,
      keyPoints: [
        'When you pass an array to a function, it "decays" into a pointer to its first element. This means the function parameter int arr[] is actually equivalent to int *arr — both are pointers. The array itself is not copied; instead, the function receives the address of the first element and can access all elements through pointer arithmetic.',
        'Because the array decays to a pointer, the function has no way to know the array\'s size. The sizeof trick (sizeof(arr) / sizeof(arr[0])) only works in the scope where the array was originally declared. Inside a function, sizeof(arr) returns the size of a pointer (typically 8 bytes on 64-bit systems), not the total array size. You must always pass the size as a separate parameter.',
        'Because the function receives a pointer to the original memory, any modifications made to array elements inside the function are visible to the caller. This is unlike pass-by-value for scalars, where the function works with a copy. Arrays effectively have pass-by-reference semantics in C.',
        'Finding the maximum value in an array is a fundamental algorithm: initialize max to arr[0] (not 0, because the array might contain only negative values), then scan through the rest of the array, updating max whenever a larger element is found. This linear scan is O(n) and is optimal for unsorted arrays.',
        'Reversing an array in-place uses the two-pointer technique: swap arr[0] with arr[n-1], arr[1] with arr[n-2], and so on. The loop runs n/2 times, stopping at the midpoint. If you iterate the full n times, you swap everything twice and end up with the original order.',
      ],
      codeExamples: [
        {
          id: 'u2-t1-s2-ex1',
          title: 'Passing Array to Function',
          code: '#include <stdio.h>\n\n/* Must pass length separately! */\nvoid printArray(int arr[], int length) {\n    for (int i = 0; i < length; i++) {\n        printf("%d ", arr[i]);\n    }\n    printf("\\n");\n}\n\nint main(void) {\n    int nums[] = {10, 20, 30, 40, 50};\n    int size = sizeof(nums) / sizeof(nums[0]);\n    \n    printArray(nums, size);\n    return 0;\n}',
          language: 'c',
          explanation: 'This example demonstrates the correct pattern for passing arrays to functions. Inside main, sizeof(nums) returns the total size of the array in bytes (5 * 4 = 20 bytes for 5 ints). But inside printArray, sizeof(arr) would return the size of a pointer (typically 8 bytes), not the array size, because arr has decayed from an array to a pointer. This is why we compute the size in main using the sizeof(nums)/sizeof(nums[0]) idiom (20/4 = 5) and pass it as a separate parameter. This idiom is so important that you should consider it a mandatory part of every function that accepts an array.',
          expectedOutput: '10 20 30 40 50',
          lineBreakdown: [
            { lineNumber: 4, code: 'void printArray(int arr[], int length)', explanation: 'arr[] acts as a pointer. length is needed to know how many elements.' },
            { lineNumber: 13, code: '    int size = sizeof(nums) / sizeof(nums[0]);', explanation: 'Idiom to calculate array length: total array bytes divided by one element\'s bytes.' },
            { lineNumber: 15, code: '    printArray(nums, size);', explanation: 'Pass the array name and its calculated size.' },
          ],
          relatedTopicIds: ['u2-t6'],
        },
        {
          id: 'u2-t1-s2-ex2',
          title: 'Finding Maximum Value',
          code: '#include <stdio.h>\n\nint findMax(int arr[], int n) {\n    int max = arr[0]; /* Start with first element */\n    for (int i = 1; i < n; i++) {\n        if (arr[i] > max) {\n            max = arr[i];\n        }\n    }\n    return max;\n}\n\nint main(void) {\n    int data[] = {45, 12, 88, 33, 71};\n    printf("Max value: %d\\n", findMax(data, 5));\n    return 0;\n}',
          language: 'c',
          explanation: 'This is one of the most fundamental array algorithms, and a common interview question. The critical detail is initializing max to arr[0], not to 0 or INT_MIN. If you initialize max to 0 and the array contains only negative numbers (like {-5, -10, -3}), the function would incorrectly return 0 — a value that is not even in the array. By starting with arr[0], you guarantee that max is always an element of the array. The loop starts at index 1 (since arr[0] is already accounted for) and updates max whenever a larger element is found.',
          expectedOutput: 'Max value: 88',
          lineBreakdown: [
            { lineNumber: 4, code: '    int max = arr[0];', explanation: 'Safest initialization. Handles negative numbers correctly.' },
            { lineNumber: 5, code: '    for (int i = 1; i < n; i++) {', explanation: 'Start loop at 1 since arr[0] is already the current max.' },
            { lineNumber: 6, code: '        if (arr[i] > max) { max = arr[i]; }', explanation: 'Update max whenever a larger element is found.' },
          ],
          relatedTopicIds: ['u1-t8'],
        },
        {
          id: 'u2-t1-s2-ex3',
          title: 'Reversing an Array In-Place',
          code: '#include <stdio.h>\n\nvoid reverse(int arr[], int n) {\n    for (int i = 0; i < n / 2; i++) {\n        int temp = arr[i];\n        arr[i] = arr[n - 1 - i];\n        arr[n - 1 - i] = temp;\n    }\n}\n\nint main(void) {\n    int data[] = {1, 2, 3, 4, 5};\n    reverse(data, 5);\n    for(int i = 0; i < 5; i++) printf("%d ", data[i]);\n    return 0;\n}',
          language: 'c',
          explanation: 'Array reversal uses the "two pointers moving inward" technique. Element at index i is swapped with element at index n-1-i: the first with the last, the second with the second-to-last, and so on. The loop runs only n/2 iterations because by the time you reach the middle, every element has already been swapped once. If you ran the loop n times, each pair would be swapped twice, returning the array to its original order. The three-line swap using a temporary variable (temp = arr[i]; arr[i] = arr[n-1-i]; arr[n-1-i] = temp) is the standard pattern for swapping values in C.',
          expectedOutput: '5 4 3 2 1',
          lineBreakdown: [
            { lineNumber: 4, code: '    for (int i = 0; i < n / 2; i++) {', explanation: 'Stop at halfway. If we go to n, we would swap everything twice (back to original).' },
            { lineNumber: 6, code: '        arr[i] = arr[n - 1 - i];', explanation: 'n-1 is the last index. n-1-i moves backwards from the end.' },
          ],
          relatedTopicIds: [],
        },
      ],
      commonMistakes: [
        {
          id: 'u2-t1-s2-cm1',
          title: 'Using sizeof on an array parameter',
          wrongCode: 'void print(int arr[]) {\n    int n = sizeof(arr)/sizeof(arr[0]); /* Bug! */\n}',
          correctCode: 'void print(int arr[], int n) {\n    /* Use the passed n */\n}',
          explanation: 'This is one of the most deceptive bugs in C because the code compiles without errors and produces plausible-looking results. When an array is passed to a function, it decays into a pointer. Inside the function, sizeof(arr) returns the size of the pointer (8 bytes on 64-bit systems), not the total size of the original array. So sizeof(arr)/sizeof(arr[0]) evaluates to 8/4 = 2, not the actual number of elements. The function will process only 2 elements regardless of the real array size. The fix is to always accept the array size as a separate parameter.',
          consequence: 'The loop runs the wrong number of times. On a 64-bit system with a 100-element int array, it would process only 2 elements (8/4). The program silently ignores most of the data.',
        },
        {
          id: 'u2-t1-s2-cm2',
          title: 'Initializing max to 0 when finding maximum',
          wrongCode: 'int max = 0;\nfor(int i=0; i<n; i++) if(arr[i] > max) max = arr[i];',
          correctCode: 'int max = arr[0];\nfor(int i=1; i<n; i++) if(arr[i] > max) max = arr[i];',
          explanation: 'This is a logic error that only manifests with certain inputs. If the array contains only negative numbers (like {-5, -10, -3}), no element is greater than 0, so the if condition is never true, and max remains 0. But 0 is not in the array — the actual maximum is -3. The fix is to initialize max to arr[0], which is always a valid element. This makes the algorithm correct for all possible inputs: positive, negative, mixed, and single-element arrays.',
          consequence: 'Returns 0 (or whatever the initial value was) for arrays containing only negative numbers, even though 0 does not appear in the array.',
        },
      ],
      interviewCallouts: [
        {
          id: 'u2-t1-s2-ic1',
          title: 'Array Decay',
          content: 'This is a precise technical term that interviewers expect you to know. When an array name appears in an expression (like passing it to a function), it "decays" — it is automatically converted from an array type to a pointer to the first element. This conversion is implicit and happens everywhere except in three contexts: when used with sizeof, when used with the & operator, and when used as a string literal initializer. Understanding decay explains why sizeof behaves differently inside vs. outside a function, and why you can use array names interchangeably with pointers in most expressions.',
          relatedTopicIds: ['u2-t4'],
          frequency: 'common',
        },
      ],
      checkpoints: [
        {
          id: 'u2-t1-s2-cp1',
          title: 'Array Algorithms',
          description: 'Verify array manipulation skills.',
          criteria: [
            'Write a function to find the minimum value in an array',
            'Write a function to sum all elements',
            'Explain why you must pass array size to functions',
          ],
          topicId: 'u2-t1',
        },
      ],
      revisionCards: [
        {
          id: 'u2-t1-s2-rc1',
          front: 'How do you calculate the number of elements in an array arr?',
          back: 'int size = sizeof(arr) / sizeof(arr[0]); (Note: This only works in the scope where the array was declared, NOT inside a function it was passed to).',
          topicId: 'u2-t1',
          tags: ['arrays', 'sizeof'],
        },
        {
          id: 'u2-t1-s2-rc2',
          front: 'What happens to an array when passed to a function?',
          back: 'It decays into a pointer to its first element. Changes made in the function affect the original array.',
          topicId: 'u2-t1',
          tags: ['arrays', 'functions'],
        },
        {
          id: 'u2-t1-s2-rc3',
          front: 'How many times should a loop iterate to reverse an array of size n?',
          back: 'n / 2 times. If it iterates n times, it will swap all elements back to their original positions.',
          topicId: 'u2-t1',
          tags: ['arrays', 'algorithms'],
        },
      ],
    },
  ],

  theoryQuestions: [
    {
      id: 'u2-t1-q1',
      type: 'mcq',
      topicId: 'u2-t1',
      difficulty: 'beginner',
      question: 'Which of the following correctly declares an array of 10 integers?',
      options: ['int arr(10);', 'int arr[10];', 'array<int> arr[10];', 'int[10] arr;'],
      correctAnswer: 'int arr[10];',
      explanation: 'In C, the size goes in square brackets AFTER the variable name.',
      tags: ['arrays', 'declaration'],
    },
    {
      id: 'u2-t1-q2',
      type: 'spot-bug',
      topicId: 'u2-t1',
      difficulty: 'beginner',
      question: 'Spot the bug:',
      code: 'int arr[5] = {1, 2, 3, 4, 5};\nfor (int i = 1; i <= 5; i++) {\n    printf("%d ", arr[i]);\n}',
      correctAnswer: 'The loop bounds are wrong. It accesses index 5, which is out of bounds.',
      explanation: 'Valid indices are 0 to 4. The loop goes from 1 to 5. arr[5] is out of bounds and will print garbage.',
      tags: ['arrays', 'bounds'],
    },
    {
      id: 'u2-t1-q3',
      type: 'predict-output',
      topicId: 'u2-t1',
      difficulty: 'intermediate',
      question: 'What is the output?',
      code: '#include <stdio.h>\nint main(void) {\n    int a[3] = {5};\n    printf("%d %d %d", a[0], a[1], a[2]);\n    return 0;\n}',
      correctAnswer: '5 0 0',
      explanation: 'Partial initialization. The first element is 5, the rest are automatically initialized to 0.',
      tags: ['arrays', 'initialization'],
    },
    {
      id: 'u2-t1-q4',
      type: 'true-false',
      topicId: 'u2-t1',
      difficulty: 'intermediate',
      question: 'When you pass an array to a function, the function receives a copy of the whole array.',
      correctAnswer: false,
      explanation: 'Functions receive a pointer to the first element (pass-by-reference semantics for arrays). Modifying it modifies the original array. This avoids copying large amounts of memory.',
      tags: ['arrays', 'functions'],
    },
    {
      id: 'u2-t1-q5',
      type: 'mcq',
      topicId: 'u2-t1',
      difficulty: 'intermediate',
      question: 'Why is sizeof(arr) inside a function dangerous when arr is an array parameter?',
      options: [
        'It causes a compilation error',
        'It returns the size of a pointer (usually 8), not the array size',
        'It returns 0',
        'It returns the number of elements instead of bytes',
      ],
      correctAnswer: 'It returns the size of a pointer (usually 8), not the array size',
      explanation: 'Because the array decayed to a pointer, sizeof() only sees the pointer. To get the array size inside a function, you must pass the length as a separate parameter.',
      tags: ['arrays', 'sizeof', 'decay'],
    },
  ],

  programmingProblems: [
    {
      id: 'u2-t1-new-easy',
      title: 'Array Reverse',
      topicId: 'u2-t1',
      difficulty: 'beginner',
      problemStatement: 'Reverse an array of N elements in place.',
      constraints: ['Do not use a secondary array'],
      sampleInput: '1 2 3',
      sampleOutput: '3 2 1',
      hints: ['Swap elements from both ends moving towards the center'],
      solution: '/* Array reverse implementation */',
      solutionExplanation: 'Swaps index i and N-1-i.',
      dryRun: [],
      tags: ['arrays']
    },
    {
      id: 'u2-t1-new-med',
      title: 'Matrix Diagonal Sum',
      topicId: 'u2-t1',
      difficulty: 'intermediate',
      problemStatement: 'Calculate the sum of the main diagonal of an NxN matrix.',
      constraints: ['Matrix is guaranteed to be square'],
      sampleInput: '1 2\n3 4',
      sampleOutput: '5',
      hints: ['Main diagonal elements have index [i][i]'],
      solution: '/* Diagonal sum implementation */',
      solutionExplanation: 'Iterates and sums arr[i][i].',
      dryRun: [],
      tags: ['matrix']
    },
    {
      id: 'u2-t1-new-hard',
      title: 'Recursive GCD',
      topicId: 'u2-t1',
      difficulty: 'advanced',
      problemStatement: 'Find the Greatest Common Divisor of two numbers using recursion (Euclidean algorithm).',
      constraints: ['Must use recursion'],
      sampleInput: '48 18',
      sampleOutput: '6',
      hints: ['gcd(a, b) = gcd(b, a % b)'],
      solution: '/* Recursive GCD implementation */',
      solutionExplanation: 'Implements Euclidean algorithm recursively.',
      dryRun: [],
      tags: ['recursion']
    },
    {
      id: 'u2-t1-p1',
      title: 'Sum and Average',
      topicId: 'u2-t1',
      difficulty: 'beginner',
      problemStatement: 'Write a program that takes an integer N, reads N integers into an array, and calculates their sum and average.',
      constraints: ['N <= 100', 'Print average to 2 decimal places'],
      sampleInput: '5\n10 20 30 40 50',
      sampleOutput: 'Sum: 150\nAverage: 30.00',
      hints: ['Use a loop to read elements with scanf', 'Use a float for the average calculation to avoid truncation'],
      solution: '#include <stdio.h>\n\nint main(void) {\n    int n;\n    scanf("%d", &n);\n    \n    int arr[100];\n    int sum = 0;\n    \n    for (int i = 0; i < n; i++) {\n        scanf("%d", &arr[i]);\n        sum += arr[i];\n    }\n    \n    float avg = (float)sum / n;\n    printf("Sum: %d\\n", sum);\n    printf("Average: %.2f\\n", avg);\n    \n    return 0;\n}',
      solutionExplanation: 'Reads the size N, then reads N elements into the array while keeping a running sum. A cast to float is used to ensure accurate division.',
      dryRun: [
        { step: 1, line: 11, variables: { n: '5', sum: '0' }, output: '', explanation: 'Read n = 5' },
        { step: 2, line: 14, variables: { i: '0', 'arr[0]': '10', sum: '10' }, output: '', explanation: 'Read first element, sum=10' },
        { step: 3, line: 18, variables: { sum: '150', n: '5' }, output: '', explanation: 'Loop finishes. avg = 150 / 5 = 30.00' },
        { step: 4, line: 20, variables: {}, output: 'Sum: 150\nAverage: 30.00', explanation: 'Outputs results' },
      ],
      tags: ['arrays', 'loops', 'arithmetic'],
    },
    {
      id: 'u2-t1-p2',
      title: 'Count Occurrences',
      topicId: 'u2-t1',
      difficulty: 'beginner',
      problemStatement: 'Read an array of N integers, then read a target number. Count how many times the target appears in the array.',
      constraints: ['N <= 50'],
      sampleInput: '6\n1 4 2 4 4 5\n4',
      sampleOutput: 'Target 4 appears 3 times',
      hints: ['Use a counter variable initialized to 0', 'Loop through the array and if arr[i] == target, increment counter'],
      solution: '#include <stdio.h>\n\nint main(void) {\n    int n;\n    scanf("%d", &n);\n    \n    int arr[50];\n    for (int i = 0; i < n; i++) {\n        scanf("%d", &arr[i]);\n    }\n    \n    int target;\n    scanf("%d", &target);\n    \n    int count = 0;\n    for (int i = 0; i < n; i++) {\n        if (arr[i] == target) {\n            count++;\n        }\n    }\n    \n    printf("Target %d appears %d times\\n", target, count);\n    return 0;\n}',
      solutionExplanation: 'Two loops: one to read the data, one to search for the target. We compare each element to the target and increment a counter if they match.',
      dryRun: [
        { step: 1, line: 9, variables: { n: '6' }, output: '', explanation: 'Array populated with 1, 4, 2, 4, 4, 5.' },
        { step: 2, line: 13, variables: { target: '4', count: '0' }, output: '', explanation: 'Read target = 4.' },
        { step: 3, line: 17, variables: { i: '1', 'arr[1]': '4', count: '1' }, output: '', explanation: 'Found a match at index 1.' },
        { step: 4, line: 23, variables: { target: '4', count: '3' }, output: 'Target 4 appears 3 times', explanation: 'Loop finishes, total count is 3.' },
      ],
      tags: ['arrays', 'linear-search', 'counting'],
    },
    {
      id: 'u2-t1-p3',
      title: 'Check if Array is Sorted',
      topicId: 'u2-t1',
      difficulty: 'intermediate',
      problemStatement: 'Write a function that checks if an array of N integers is sorted in strictly increasing order. Return 1 if sorted, 0 otherwise.',
      constraints: ['N <= 100', 'Use a separate function'],
      sampleInput: '4\n10 20 30 40',
      sampleOutput: 'Sorted',
      hints: ['Loop from i = 0 to n-2', 'Check if arr[i] >= arr[i+1]. If yes, return 0 early.', 'If loop finishes, return 1'],
      solution: '#include <stdio.h>\n\nint isSorted(int arr[], int n) {\n    for (int i = 0; i < n - 1; i++) {\n        if (arr[i] >= arr[i + 1]) {\n            return 0; /* Not strictly increasing */\n        }\n    }\n    return 1; /* Sorted */\n}\n\nint main(void) {\n    int n;\n    scanf("%d", &n);\n    int arr[100];\n    for (int i = 0; i < n; i++) {\n        scanf("%d", &arr[i]);\n    }\n    \n    if (isSorted(arr, n)) {\n        printf("Sorted\\n");\n    } else {\n        printf("Not Sorted\\n");\n    }\n    return 0;\n}',
      solutionExplanation: 'Checks adjacent pairs. If any pair is out of order (left >= right), it immediately returns 0. Note the loop goes to n-1 to avoid accessing arr[n] (out of bounds).',
      dryRun: [
        { step: 1, line: 4, variables: { n: '4' }, output: '', explanation: 'Array is 10, 20, 30, 40.' },
        { step: 2, line: 5, variables: { i: '0' }, output: '', explanation: 'arr[0]=10, arr[1]=20. 10 >= 20 is false. Continue.' },
        { step: 3, line: 9, variables: {}, output: '', explanation: 'All pairs checked. Loop ends, returns 1.' },
        { step: 4, line: 21, variables: {}, output: 'Sorted\\n', explanation: 'main prints Sorted.' },
      ],
      tags: ['arrays', 'functions', 'logic'],
    },
  ],
};
