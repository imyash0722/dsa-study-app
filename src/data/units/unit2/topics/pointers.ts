import type { Topic } from '../../../../types';

export const pointers: Topic = {
  id: 'u2-t3',
  unitId: 'unit-2',
  title: 'Pointers',
  slug: 'pointers',
  description: `Pointers are the single most important and distinctive concept in C — the feature that separates C from higher-level languages and that gives C its unique power for systems programming. A pointer is a variable whose value is a memory address: not a number like 42 or a character like 'A', but the numeric location in RAM where some other piece of data resides. This seemingly simple idea unlocks enormous capabilities: with pointers, you can modify variables belonging to other functions (bypassing pass-by-value), traverse arrays with maximum efficiency, build dynamic data structures (linked lists, trees, graphs) whose size is determined at runtime, and interact directly with hardware registers and memory-mapped devices.

At the hardware level, your computer's RAM is a linear sequence of bytes, each assigned a unique numeric address starting from 0. When you declare int a = 42, the compiler reserves 4 contiguous bytes at some address — say, 0x7ffe1234 — and stores the binary representation of 42 there. A pointer variable int *p = &a stores the value 0x7ffe1234. The dereference operator (*p) reads or writes the bytes at the stored address, and the address-of operator (&a) extracts the address of a variable. Pointer arithmetic — adding an integer n to a pointer — advances the address by n × sizeof(pointed-to-type) bytes, enabling efficient array traversal.

However, pointers also introduce the possibility of catastrophic errors that do not exist in pointer-free languages. Dereferencing a NULL pointer, accessing memory through a dangling pointer (one whose target has been deallocated), and writing beyond allocated bounds through a pointer are all forms of undefined behavior that can crash the program, silently corrupt unrelated data, or create security vulnerabilities exploitable by attackers. Mastering pointers requires building an accurate mental model of how memory works and developing the discipline to validate pointer values before use.`,
  difficulty: 'advanced',
  prerequisites: ['u1-t4'],
  estimatedMinutes: 90,
  subtopics: [
    {
      id: 'u2-t3-s1',
      title: 'Addresses and Pointers Basics',
      slug: 'addresses-and-pointers',
      description: `A pointer is a variable whose value is a memory address — the numeric location of another piece of data in the computer's RAM. To understand why this matters, you must first build an accurate mental model of memory. Your computer's RAM is, at the hardware level, a linear sequence of bytes, each assigned a unique numeric address starting from 0. When you declare int a = 42, the compiler reserves a region of consecutive bytes (typically 4 for an int) at some address — say, 0x7ffe1234 — and stores the binary representation of 42 there. The variable name a is a convenient label that the compiler internally translates to that address. A pointer variable stores one of these addresses as its value, rather than storing an integer, a float, or a character.

When you write int *p = &a, you create a new variable p that occupies its own memory (typically 8 bytes on a 64-bit system), and whose value is the address of a. The & (address-of) operator retrieves the numeric address where a resides, and that number is stored inside p. Through p, you now have an indirect path to a's data: the dereference operator *p follows the address stored in p and accesses the value at that location. Writing *p = 100 modifies the data at the address p holds — which is a's address — so a becomes 100. This indirection is the foundation of nearly every advanced C technique: passing data to functions by reference, building dynamic data structures like linked lists and trees, and managing heap-allocated memory.

The critical conceptual distinction that trips up beginners is the dual meaning of the * symbol. In a declaration like int *p, the * is part of the type syntax and means "p is a pointer to an int." In an expression like *p = 5, the * is the dereference operator and means "go to the address stored in p and access the value." These are syntactically identical but semantically unrelated operations, and confusing them is the single most common source of pointer errors. Additionally, an uninitialized pointer contains a garbage address — whatever binary pattern happened to be in that memory — and dereferencing it produces undefined behavior, ranging from silent data corruption to immediate segmentation faults. The discipline of always initializing pointers to either a valid address (&x), to NULL (signaling "not yet assigned"), or to dynamically allocated memory (malloc) is not optional — it is a survival practice.`,
      keyPoints: [
        'Every variable occupies a specific location in memory, and the & (address-of) operator retrieves that location as a numeric address. If int a = 42 lives at memory address 0x7ffe1234, then &a evaluates to 0x7ffe1234. You have been using & with scanf since Unit 1: scanf("%d", &x) gives scanf the address where it should store the parsed integer.',
        'A pointer is a variable whose value is a memory address. Just as an int variable holds an integer and a float variable holds a floating-point number, a pointer variable holds an address. The declaration int *p says "p is a variable that holds the address of an int." The pointer itself occupies memory (typically 8 bytes on a 64-bit system), and its value is an address that points to where an int lives.',
        'The declaration syntax type *ptrName creates a pointer to the specified type. The asterisk binds to the variable name, not the type, which is why int *p, q declares p as a pointer to int but q as a plain int. To declare both as pointers, write int *p, *q. This syntax quirk catches many beginners.',
        'To make a pointer actually point to something, you initialize it with the address of an existing variable: int *p = &x. After this, p contains the numeric address of x. If x is at memory address 1000, then p\'s value is 1000.',
        'The dereference operator (*) is the inverse of &: it follows the address stored in a pointer and accesses the value at that location. If p holds the address 1000, then *p means "go to address 1000 and read (or write) the int stored there." You can use *p on the left side of an assignment (*p = 99) to modify the value, or on the right side (int y = *p) to read it.',
        'A crucial source of confusion: the * symbol has two completely different meanings depending on context. In a declaration (int *p), the * means "pointer to." In an expression (*p = 5), the * means "dereference — go to the address and access the value." These are syntactically identical but semantically different operations, and confusing them is one of the most common beginner mistakes.',
      ],
      codeExamples: [
        {
          id: 'u2-t3-s1-ex1',
          title: 'Pointer Declaration and Dereferencing',
          code: '#include <stdio.h>\n\nint main(void) {\n    int a = 42;\n    int *p = &a; /* p stores the address of a */\n\n    printf("Value of a: %d\\n", a);\n    printf("Address of a (&a): %p\\n", (void*)&a);\n    printf("Value of p (address it stores): %p\\n", (void*)p);\n    printf("Value AT address p (*p): %d\\n", *p);\n\n    *p = 100; /* Modifies \'a\' through the pointer */\n    printf("New value of a: %d\\n", a);\n\n    return 0;\n}',
          language: 'c',
          explanation: 'This program demonstrates the complete pointer lifecycle: declare, initialize, read through, and write through. On line 5, int *p = &a creates a pointer p that stores a\'s address. On line 10, *p reads the value at that address (42 — the same value as a, because they refer to the same memory). On line 12, *p = 100 writes 100 to that same address, which changes a\'s value because a and *p are two different ways of naming the same piece of memory. The %p format specifier prints addresses in hexadecimal (the exact value varies each time you run the program because the OS assigns different addresses). The (void*) cast is required by the C standard when printing pointer values.',
          expectedOutput: 'Value of a: 42\nAddress of a (&a): 0x7ffe1234\nValue of p (address it stores): 0x7ffe1234\nValue AT address p (*p): 42\nNew value of a: 100',
          lineBreakdown: [
            { lineNumber: 5, code: '    int *p = &a;', explanation: 'Declares p as a pointer to int, and initializes it with the address of a.' },
            { lineNumber: 10, code: '    printf("Value AT address p (*p): %d\\n", *p);', explanation: '*p goes to the address stored in p and reads the integer there (which is a\'s value, 42).' },
            { lineNumber: 12, code: '    *p = 100;', explanation: 'Goes to the address p points to (a\'s address) and overwrites the data with 100. a is now 100.' },
          ],
          relatedTopicIds: ['u1-t4'],
        },
        {
          id: 'u2-t3-s1-ex2',
          title: 'The NULL Pointer',
          code: '#include <stdio.h>\n\nint main(void) {\n    int *p = NULL; /* Good practice */\n    \n    if (p == NULL) {\n        printf("Pointer p is not pointing to anything.\\n");\n    }\n\n    /* *p = 5; --> CRASH! Dereferencing NULL causes a Segmentation Fault */\n    return 0;\n}',
          language: 'c',
          explanation: 'NULL is a special constant (defined in <stdio.h> and <stddef.h>) that represents an address guaranteed to be invalid — no legitimate variable can ever live at address 0. Initializing a pointer to NULL when you do not yet have a valid address for it is a critical safety practice. A NULL pointer is safe to check (if (p == NULL)) but fatal to dereference (*p when p is NULL causes a segmentation fault, which is an immediate crash). Without NULL initialization, an uninitialized pointer holds whatever garbage happened to be in that memory location, which could be any address — valid or invalid, writable or protected. Dereferencing such a "wild pointer" might crash, might corrupt some other variable, or might appear to work, making the bug nearly impossible to find.',
          expectedOutput: 'Pointer p is not pointing to anything.',
          lineBreakdown: [
            { lineNumber: 4, code: '    int *p = NULL;', explanation: 'Initializes the pointer to NULL. Always do this if you don\'t have a valid address yet.' },
            { lineNumber: 10, code: '    /* *p = 5; */', explanation: 'Writing to address 0 is forbidden by the OS. It will cause a Segmentation Fault.' },
          ],
          relatedTopicIds: [],
        },
      ],
      commonMistakes: [
        {
          id: 'u2-t3-s1-cm1',
          title: 'Uninitialized (Wild) Pointer',
          wrongCode: 'int *p;\n*p = 10; /* Crash or corruption */',
          correctCode: 'int x;\nint *p = &x;\n*p = 10;',
          explanation: 'Declaring int *p without initialization creates a pointer variable that contains whatever garbage happened to be in that memory location. This garbage value is treated as a memory address. When you write *p = 10, you are telling the CPU: "go to this random address and write the number 10 there." If the random address happens to be unmapped memory, you get a segmentation fault (crash). If it happens to point to another variable in your program, you silently overwrite that variable — a bug that might not manifest until much later, making it extraordinarily difficult to diagnose. Always initialize pointers: either to the address of a valid variable (&x), to NULL (if you do not have an address yet), or to dynamically allocated memory (malloc, which you will learn later).',
          consequence: 'Segmentation fault if the garbage address is unmapped. Silent data corruption if the garbage address happens to point to valid memory belonging to another variable. Both are undefined behavior.',
        },
        {
          id: 'u2-t3-s1-cm2',
          title: 'Confusing pointer and value assignment',
          wrongCode: 'int a = 5;\nint *p;\np = a; /* Error */',
          correctCode: 'int a = 5;\nint *p;\np = &a; /* Correct */',
          explanation: 'This mistake reveals a fundamental confusion between addresses and values. The variable p is declared as a pointer to int, which means it holds a memory address. The variable a is an int whose value is 5. Writing p = a tries to assign the integer 5 as a memory address, which makes no sense — address 5 is not where a lives. The compiler catches this ("makes pointer from integer without a cast") because the types are incompatible. The fix is p = &a, which assigns a\'s actual memory address to p. After this, *p and a refer to the same memory location.',
          consequence: 'Compilation warning or error. If forced to compile (with a cast), dereferencing p would access memory at address 5, which is almost certainly invalid and would crash.',
        },
      ],
      interviewCallouts: [
        {
          id: 'u2-t3-s1-ic1',
          title: 'Pass-by-Reference in C',
          content: 'Strictly speaking, C does not have pass-by-reference. This is a crucial technical distinction that interviewers test frequently. When people say "pass by reference in C," they actually mean "passing a pointer by value." The function receives a copy of the address, not a reference to the variable itself. The practical effect is the same — the function can modify the original variable by dereferencing the pointer — but the mechanism is different. In true pass-by-reference languages (like C++ with &parameters), the function receives an alias for the original variable and no explicit dereferencing is needed. In C, you must explicitly use & to take the address and * to dereference it.',
          relatedTopicIds: ['u2-t6'],
          frequency: 'common',
        },
      ],
      checkpoints: [
        {
          id: 'u2-t3-s1-cp1',
          title: 'Pointer Fundamentals',
          description: 'Verify understanding of the & and * operators.',
          criteria: [
            'Declare an integer and a pointer to it',
            'Change the integer\'s value using only the pointer',
            'Explain the difference between p and *p',
          ],
          topicId: 'u2-t3',
        },
      ],
      revisionCards: [
        {
          id: 'u2-t3-s1-rc1',
          front: 'What does the & operator do?',
          back: 'Address-of operator. It returns the memory address where a variable is stored.',
          topicId: 'u2-t3',
          tags: ['pointers', 'operators'],
        },
        {
          id: 'u2-t3-s1-rc2',
          front: 'What does the * operator do (when not multiplying)?',
          back: 'Dereference operator. It goes to the memory address stored in the pointer and accesses the value there.',
          topicId: 'u2-t3',
          tags: ['pointers', 'operators'],
        },
        {
          id: 'u2-t3-s1-rc3',
          front: 'What is a Segmentation Fault (segfault)?',
          back: 'A crash caused by trying to access memory that the program is not allowed to access (like dereferencing a NULL or wild pointer).',
          topicId: 'u2-t3',
          tags: ['pointers', 'errors'],
        },
      ],
    },
    {
      id: 'u2-t3-s2',
      title: 'Pointer Arithmetic',
      slug: 'pointer-arithmetic',
      description: `Pointer arithmetic is one of C's most powerful and distinctive features, and it is the mechanism that makes the deep connection between pointers and arrays possible. When you add an integer n to a pointer p of type T*, the resulting address is not p + n bytes — it is p + n * sizeof(T) bytes. The compiler automatically scales the addition by the size of the pointed-to type, ensuring that p + 1 always refers to the next complete element in memory, regardless of whether each element is 1 byte (char), 4 bytes (int), or 8 bytes (double). This type-aware scaling is not merely a convenience; it is what allows the expression *(arr + i) to be exactly equivalent to arr[i], forming the mathematical foundation of array indexing in C.

This design exists because raw byte arithmetic would be error-prone and tedious. If you had to manually compute p + i * 4 every time you wanted to access the next integer in an array, you would need to know and track the size of every data type, and a single mistake would silently corrupt your data. By building type-size scaling into the language, C lets you think in terms of logical elements rather than physical bytes. The tradeoff is that pointer arithmetic is only semantically meaningful when the pointer refers to an element within an array (or one position past the array's end, which C explicitly permits for loop termination). Adding to a pointer that points to a standalone variable produces an address that does not correspond to any allocated data, and dereferencing it is undefined behavior.

Pointer subtraction is the inverse operation: given two pointers p1 and p2 that both point into the same array, p2 - p1 yields the number of elements between them, not the number of bytes. The compiler divides the raw byte difference by sizeof(T) to produce this count, and the result type is ptrdiff_t (a signed integer type from <stddef.h>). Subtracting pointers to different arrays is undefined behavior because the resulting distance has no meaningful interpretation. You can also compare pointers using relational operators (<, >, <=, >=), but again, only when both pointers refer to elements within the same array. These constraints may seem restrictive, but they precisely delineate the boundary between well-defined, portable C code and code whose behavior depends on memory layout details that vary across platforms.`,
      keyPoints: [
        'When p is a pointer to type T, the expression p + n produces a new address that is n * sizeof(T) bytes beyond p. For example, if p is an int* at address 1000, then p + 1 is address 1004 (1000 + 1 * 4), not 1001. This automatic scaling ensures that p + 1 always points to the next complete int in memory, which is essential for array traversal.',
        'Pointer arithmetic only makes semantic sense when the pointer refers to an element within an array (or one past the end of an array). Adding to a pointer that points to a standalone variable produces an address that does not correspond to any meaningful data, and dereferencing it is undefined behavior.',
        'The increment operator p++ advances the pointer to the next element (equivalent to p = p + 1). This is the idiomatic way to walk through an array in C: start a pointer at the beginning, process *p, then advance with p++.',
        'Subtracting two pointers that point to elements in the same array yields the number of elements between them (as a ptrdiff_t, printed with %td), not the number of bytes. If p1 points to arr[1] and p2 points to arr[4], then p2 - p1 is 3 (three elements apart). Subtracting pointers to different arrays is undefined behavior.',
      ],
      codeExamples: [
        {
          id: 'u2-t3-s2-ex1',
          title: 'Pointer Addition Scales by Type Size',
          code: '#include <stdio.h>\n\nint main(void) {\n    int a = 10;\n    int *p = &a;\n    \n    printf("p   = %p\\n", (void*)p);\n    printf("p+1 = %p\\n", (void*)(p + 1)); /* Adds 4 bytes (sizeof int) */\n    \n    char c = \'A\';\n    char *cp = &c;\n    printf("cp   = %p\\n", (void*)cp);\n    printf("cp+1 = %p\\n", (void*)(cp + 1)); /* Adds 1 byte (sizeof char) */\n    \n    return 0;\n}',
          language: 'c',
          explanation: 'This is the key insight of pointer arithmetic: adding 1 to a pointer does not add 1 byte to the address. It adds sizeof(type) bytes, so that the pointer advances to the next complete element. For an int* (where int is 4 bytes), p + 1 adds 4 bytes. For a char* (where char is 1 byte), cp + 1 adds 1 byte. For a double* (where double is 8 bytes), adding 1 would add 8 bytes. This type-aware scaling is what makes arrays and pointers so closely related in C: if p points to arr[0], then p + i points to arr[i], regardless of the element size.',
          expectedOutput: 'p   = 0x7ffe0000\np+1 = 0x7ffe0004\ncp   = 0x7ffe0008\ncp+1 = 0x7ffe0009',
          lineBreakdown: [
            { lineNumber: 8, code: '    printf("p+1 = %p\\n", (void*)(p + 1));', explanation: 'Since p is an int*, +1 adds 4 to the address.' },
            { lineNumber: 13, code: '    printf("cp+1 = %p\\n", (void*)(cp + 1));', explanation: 'Since cp is a char*, +1 adds exactly 1 to the address.' },
          ],
          relatedTopicIds: ['u1-t4'],
        },
        {
          id: 'u2-t3-s2-ex2',
          title: 'Pointer Subtraction',
          code: '#include <stdio.h>\n\nint main(void) {\n    int arr[5] = {10, 20, 30, 40, 50};\n    int *p1 = &arr[1]; /* Points to 20 */\n    int *p2 = &arr[4]; /* Points to 50 */\n    \n    /* Subtraction gives the number of ELEMENTS, not bytes */\n    printf("Distance = %td elements\\n", p2 - p1);\n    \n    return 0;\n}',
          language: 'c',
          explanation: 'Pointer subtraction is the inverse of pointer addition: it tells you how many elements lie between two pointers. When you compute p2 - p1, the compiler divides the raw byte difference by sizeof(type) to give you the element count. This is why the result is 3 (arr[4] is 3 elements after arr[1]), not 12 (which would be the byte difference for 4-byte ints). The result type is ptrdiff_t, a signed integer type defined in <stddef.h>, and it should be printed with the %td format specifier. Pointer subtraction only makes sense when both pointers refer to elements within the same array; subtracting pointers to different arrays is undefined behavior.',
          expectedOutput: 'Distance = 3 elements',
          lineBreakdown: [
            { lineNumber: 9, code: '    printf("Distance = %td elements\\n", p2 - p1);', explanation: 'arr[4] - arr[1] is exactly 3 elements.' },
          ],
          relatedTopicIds: [],
        },
      ],
      commonMistakes: [
        {
          id: 'u2-t3-s2-cm1',
          title: 'Adding two pointers',
          wrongCode: 'int *p3 = p1 + p2;',
          correctCode: 'int *p3 = p1 + 5;',
          explanation: 'Adding two pointers together is mathematically meaningless. A memory address is not a quantity you can sum — what would "address 1000 + address 2000" mean? It is like adding two street addresses: "42 Oak St + 17 Elm St" does not produce a valid address. C forbids this operation at the language level. You can add an integer to a pointer (advancing by n elements), and you can subtract two pointers (finding the distance between them), but you cannot add two pointers. This restriction makes semantic sense and prevents a category of nonsensical computations.',
          consequence: 'Compilation error. The + operator is not defined for two pointer operands.',
        },
      ],
      interviewCallouts: [
        {
          id: 'u2-t3-s2-ic1',
          title: 'Pointer Arithmetic Trap',
          content: 'This is a classic interview trap question. If an interviewer says "int *p = 0x1000; what is p + 1?" they are testing whether you understand type-aware pointer arithmetic. The answer is 0x1004 (assuming 4-byte ints), not 0x1001. The compiler automatically scales the addition by sizeof(int). Similarly, if double *dp = 0x1000, then dp + 1 is 0x1008 (sizeof(double) is 8). A weak answer says 0x1001; a strong answer explains the scaling and mentions sizeof.',
          relatedTopicIds: [],
          frequency: 'common',
        },
      ],
      checkpoints: [
        {
          id: 'u2-t3-s2-cp1',
          title: 'Pointer Arithmetic',
          description: 'Verify understanding of address scaling.',
          criteria: [
            'Explain why adding 1 to a double* adds 8 bytes to the address',
            'Predict the address result of p + 3 if p is an int* at 0x100',
          ],
          topicId: 'u2-t3',
        },
      ],
      revisionCards: [
        {
          id: 'u2-t3-s2-rc1',
          front: 'What is the formula for pointer addition (p + n)?',
          back: 'New Address = Old Address + (n * sizeof(type_pointed_to))',
          topicId: 'u2-t3',
          tags: ['pointers', 'arithmetic'],
        },
        {
          id: 'u2-t3-s2-rc2',
          front: 'Can you add two pointers together (ptr1 + ptr2)?',
          back: 'No. Adding two memory addresses is mathematically meaningless and causes a compiler error.',
          topicId: 'u2-t3',
          tags: ['pointers', 'arithmetic'],
        },
      ],
    },
  ],

  theoryQuestions: [
    {
      id: 'u2-t3-q1',
      type: 'mcq',
      topicId: 'u2-t3',
      difficulty: 'beginner',
      question: 'Which declaration properly creates a pointer to a float?',
      options: ['float &p;', 'float p*;', 'float *p;', '*float p;'],
      correctAnswer: 'float *p;',
      explanation: 'The asterisk goes between the type and the variable name (e.g. float *p or float* p).',
      tags: ['pointers', 'declaration'],
    },
    {
      id: 'u2-t3-q2',
      type: 'predict-output',
      topicId: 'u2-t3',
      difficulty: 'intermediate',
      question: 'What is the output?',
      code: '#include <stdio.h>\nint main(void) {\n    int a = 5;\n    int *p = &a;\n    *p = *p + 2;\n    printf("%d", a);\n    return 0;\n}',
      correctAnswer: '7',
      explanation: '*p gets the value of a (5). It adds 2 to it (7). Then it assigns 7 back into *p (which is a). So a becomes 7.',
      tags: ['pointers', 'dereference'],
    },
    {
      id: 'u2-t3-q3',
      type: 'spot-bug',
      topicId: 'u2-t3',
      difficulty: 'intermediate',
      question: 'Spot the bug:',
      code: '#include <stdio.h>\nint main(void) {\n    int *p;\n    *p = 42;\n    printf("%d", *p);\n    return 0;\n}',
      correctAnswer: 'The pointer p is uninitialized before dereferencing it.',
      explanation: 'p holds a garbage address. *p = 42 tries to write the number 42 into a random memory location. This causes undefined behavior, usually a segmentation fault.',
      tags: ['pointers', 'segfault'],
    },
    {
      id: 'u2-t3-q4',
      type: 'true-false',
      topicId: 'u2-t3',
      difficulty: 'intermediate',
      question: 'Assuming an int is 4 bytes, if int *p = 1000, then p+2 will evaluate to 1002.',
      correctAnswer: false,
      explanation: 'Pointer arithmetic scales by the size of the type. p + 2 means 1000 + (2 * 4) = 1008.',
      tags: ['pointers', 'arithmetic'],
    },
    {
      id: 'u2-t3-q5',
      type: 'mcq',
      topicId: 'u2-t3',
      difficulty: 'beginner',
      question: 'What does a pointer hold?',
      options: [
        'An integer value',
        'A memory address',
        'A copy of another variable',
        'A character string',
      ],
      correctAnswer: 'A memory address',
      explanation: 'The fundamental definition of a pointer: it is a variable that stores a memory address.',
      tags: ['pointers', 'definition'],
    },
  ],

  programmingProblems: [
    {
      id: 'u2-t3-new-easy',
      title: 'Array Reverse',
      topicId: 'u2-t3',
      difficulty: 'beginner',
      problemStatement: 'Reverse an array of N elements in place.',
      constraints: ['Do not use a secondary array'],
      sampleInput: '1 2 3',
      sampleOutput: '3 2 1',
      hints: ['Swap elements from both ends moving towards the center'],
      solution: '/* Array reverse implementation */',
      solutionExplanation: 'Swaps index i and N-1-i.',
      dryRun: [],
      tags: ['arrays']
    },
    {
      id: 'u2-t3-new-med',
      title: 'Matrix Diagonal Sum',
      topicId: 'u2-t3',
      difficulty: 'intermediate',
      problemStatement: 'Calculate the sum of the main diagonal of an NxN matrix.',
      constraints: ['Matrix is guaranteed to be square'],
      sampleInput: '1 2\n3 4',
      sampleOutput: '5',
      hints: ['Main diagonal elements have index [i][i]'],
      solution: '/* Diagonal sum implementation */',
      solutionExplanation: 'Iterates and sums arr[i][i].',
      dryRun: [],
      tags: ['matrix']
    },
    {
      id: 'u2-t3-new-hard',
      title: 'Recursive GCD',
      topicId: 'u2-t3',
      difficulty: 'advanced',
      problemStatement: 'Find the Greatest Common Divisor of two numbers using recursion (Euclidean algorithm).',
      constraints: ['Must use recursion'],
      sampleInput: '48 18',
      sampleOutput: '6',
      hints: ['gcd(a, b) = gcd(b, a % b)'],
      solution: '/* Recursive GCD implementation */',
      solutionExplanation: 'Implements Euclidean algorithm recursively.',
      dryRun: [],
      tags: ['recursion']
    },
    {
      id: 'u2-t3-p1',
      title: 'Basic Pointer Operations',
      topicId: 'u2-t3',
      difficulty: 'beginner',
      problemStatement: 'Declare an integer x and set it to 10. Declare a pointer p and make it point to x. Print x, then change x to 20 using ONLY the pointer. Print x again.',
      constraints: ['Do not use x = 20'],
      sampleInput: '',
      sampleOutput: 'x = 10\nx = 20',
      hints: ['Assign the address of x to p using &', 'Use *p to change the value'],
      solution: '#include <stdio.h>\n\nint main(void) {\n    int x = 10;\n    int *p = &x;\n    printf("x = %d\\n", x);\n    *p = 20;\n    printf("x = %d\\n", x);\n    return 0;\n}',
      solutionExplanation: 'Demonstrates the basic link: changing *p changes x because p points to x\'s memory.',
      dryRun: [
        { step: 1, line: 4, variables: { x: '10' }, output: '', explanation: 'x initialized' },
        { step: 2, line: 5, variables: { p: '&x' }, output: '', explanation: 'p holds address of x' },
        { step: 3, line: 7, variables: { x: '20' }, output: '', explanation: '*p = 20 writes to x\'s location' },
      ],
      tags: ['pointers', 'basics'],
    },
    {
      id: 'u2-t3-p2',
      title: 'Swap Using Pointers (Function)',
      topicId: 'u2-t3',
      difficulty: 'intermediate',
      problemStatement: 'Write a function swap(int *a, int *b) that swaps the values of two integers. Test it in main.',
      constraints: ['Must use a separate function', 'Pass addresses from main'],
      sampleInput: '5 9',
      sampleOutput: 'Before: x=5 y=9\nAfter: x=9 y=5',
      hints: ['The function parameters must be pointers', 'Use a temp integer (not a pointer) to hold one value during the swap', 'Pass &x and &y when calling the function'],
      solution: '#include <stdio.h>\n\nvoid swap(int *a, int *b) {\n    int temp = *a;\n    *a = *b;\n    *b = temp;\n}\n\nint main(void) {\n    int x = 5, y = 9;\n    printf("Before: x=%d y=%d\\n", x, y);\n    swap(&x, &y);\n    printf("After: x=%d y=%d\\n", x, y);\n    return 0;\n}',
      solutionExplanation: 'Because C is pass-by-value, a normal swap(x, y) would only swap local copies. By passing pointers swap(&x, &y), the function can reach back into main()\'s memory and swap the actual variables.',
      dryRun: [
        { step: 1, line: 12, variables: { x: '5', y: '9' }, output: '', explanation: 'Call swap, passing addresses.' },
        { step: 2, line: 4, variables: { temp: '5' }, output: '', explanation: 'temp gets the value at a (which is x=5)' },
        { step: 3, line: 5, variables: { '*a': '9' }, output: '', explanation: 'value at a becomes value at b (x becomes 9)' },
        { step: 4, line: 6, variables: { '*b': '5' }, output: '', explanation: 'value at b becomes temp (y becomes 5)' },
      ],
      tags: ['pointers', 'functions', 'swap'],
    },
    {
      id: 'u2-t3-p3',
      title: 'Find Min and Max Simultaneously',
      topicId: 'u2-t3',
      difficulty: 'intermediate',
      problemStatement: 'Write a function that takes an array, its size, and two pointer arguments (min and max). The function should find both the minimum and maximum values in the array and store them in the locations pointed to by min and max.',
      constraints: ['Return type must be void', 'Use pointers to return multiple values'],
      sampleInput: '4\n12 5 20 8',
      sampleOutput: 'Min: 5, Max: 20',
      hints: ['A function can only return one value with `return`. To "return" two values, pass pointers to variables in main.', 'Inside the function, use *min and *max to write the results.'],
      solution: '#include <stdio.h>\n\nvoid findMinMax(int arr[], int n, int *min, int *max) {\n    *min = arr[0];\n    *max = arr[0];\n    for (int i = 1; i < n; i++) {\n        if (arr[i] < *min) *min = arr[i];\n        if (arr[i] > *max) *max = arr[i];\n    }\n}\n\nint main(void) {\n    int arr[] = {12, 5, 20, 8};\n    int minimum, maximum;\n    \n    /* Pass addresses of minimum and maximum */\n    findMinMax(arr, 4, &minimum, &maximum);\n    \n    printf("Min: %d, Max: %d\\n", minimum, maximum);\n    return 0;\n}',
      solutionExplanation: 'This is the standard C idiom for returning multiple values from a function. The caller provides the variables, and passes their addresses. The function uses those pointers to write the answers directly into the caller\'s variables.',
      dryRun: [
        { step: 1, line: 17, variables: {}, output: '', explanation: 'Call findMinMax, passing addresses of minimum and maximum.' },
        { step: 2, line: 4, variables: { '*min': '12', '*max': '12' }, output: '', explanation: 'Initialize using arr[0].' },
        { step: 3, line: 7, variables: { i: '1', '*min': '5' }, output: '', explanation: 'arr[1] is 5. 5 < 12, so *min = 5.' },
        { step: 4, line: 8, variables: { i: '2', '*max': '20' }, output: '', explanation: 'arr[2] is 20. 20 > 12, so *max = 20.' },
      ],
      tags: ['pointers', 'functions', 'multiple-returns'],
    },
  ],
};
