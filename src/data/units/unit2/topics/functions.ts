import type { Topic } from '../../../../types';

export const functions: Topic = {
  id: 'u2-t6',
  unitId: 'unit-2',
  title: 'Functions',
  slug: 'functions',
  description: `Functions are the fundamental unit of abstraction and organisation in C. Every C program is a collection of functions that call each other, with main() serving as the entry point that the operating system's runtime invokes when the program launches. A function encapsulates a named, reusable block of computation behind a declared interface (return type, name, parameter list), allowing the logic to be written once and invoked from arbitrarily many call sites. This encapsulation is the foundation of modular programming: it enables code reuse, simplifies testing, and allows large programs to be developed by teams working on separate functions simultaneously.

Beyond code organisation, functions shape how a C program uses memory at runtime. Each function call creates a stack frame — a block of memory on the call stack that holds the function's local variables, its parameters (which are copies of the caller's arguments), the return address (where execution should resume after the function completes), and any saved register values. When the function returns, its stack frame is destroyed and its memory is reclaimed. This automatic allocation and deallocation is efficient (pushing and popping a stack pointer) but imposes a critical constraint: local variables exist only for the duration of the function call, and returning a pointer to a local variable produces a dangling pointer — one of the most dangerous bugs in C programming.

C is strictly a pass-by-value language: when you pass a variable to a function, the function receives a copy of the variable's current value, not the variable itself. Modifying the copy inside the function has no effect on the original variable in the caller's scope. To modify the caller's data, you must pass the address of the variable (a pointer), which the function then dereferences to access and modify the original — a technique called simulated pass-by-reference that is the practical foundation for understanding pointers.`,
  difficulty: 'intermediate',
  prerequisites: ['u1-t8'],
  estimatedMinutes: 75,
  subtopics: [
    {
      id: 'u2-t6-s1',
      title: 'Function Anatomy and Prototypes',
      slug: 'anatomy-and-prototypes',
      description: `A function in C is a self-contained block of code that performs a specific computation, encapsulated behind a name, a set of typed input parameters, and a return type. Every C program is composed of functions that call each other, with main() serving as the entry point that the operating system invokes when the program starts. At the language level, a function has four components: a return type that specifies what kind of value the function produces (or void if it produces none), a name that identifies the function, a parameter list that declares the inputs the function requires, and a body enclosed in braces that contains the executable statements.

C imposes a critical requirement that distinguishes it from many modern languages: the compiler must know about a function before any code that calls it. This is because C compilers process source files in a single top-to-bottom pass, and when they encounter a function call like int result = multiply(4, 5), they need to know multiply's return type and parameter types immediately in order to generate correct machine code for passing arguments and interpreting the return value. If the function's definition appears later in the file, you must provide a function prototype (also called a forward declaration) above the call site. A prototype is a single-line declaration consisting of the function's return type, name, and parameter types, terminated by a semicolon: int multiply(int a, int b);. It contains no body — it is a promise to the compiler that a matching definition exists somewhere in the program.

This separation of declaration and definition is not just a syntactic requirement — it is the architectural foundation that enables large C programs to be split across multiple source files. Each source file includes function prototypes (typically via header files with the .h extension), and the linker connects the calls to their definitions during the final compilation step. The void keyword plays two roles in function signatures: as a return type, void means the function performs an action without producing a value; as a parameter list (void f(void)), it explicitly states that the function accepts no arguments. The (void) form is important because in C, empty parentheses int f() do not mean "no arguments" — they mean "unspecified arguments," a dangerous legacy from pre-ANSI C that modern code should always avoid.`,
      keyPoints: [
        'Every function has four parts: a return type (int, float, void, etc.), a name (like calculate_sum), a parameter list (the inputs it receives), and a body enclosed in braces (the code that executes). The return type tells the caller what kind of value to expect; the parameter list tells the caller what data the function needs to do its job.',
        'A function declaration (prototype) is a promise to the compiler: "a function with this name, these parameter types, and this return type exists somewhere in the program." It ends with a semicolon and contains no body. For example: int add(int a, int b); tells the compiler that add takes two ints and returns an int, without revealing how it works.',
        'A function definition provides the actual implementation — the body of code that executes when the function is called. The definition must match the prototype exactly in return type, name, and parameter types. If you define a function before all the code that calls it, you do not need a separate prototype.',
        'A function call executes the function by passing arguments (actual values) to its parameters (the declared variables). When the compiler encounters a call like int result = add(5, 3), it generates code to copy the values 5 and 3 into the function\'s parameter variables, transfer control to the function\'s body, execute the body, and return the result to the caller.',
        'If you call a function before declaring or defining it, the compiler has no idea what types it expects or returns. In older C standards (C89), the compiler would silently assume the function returns int and accept any arguments — a recipe for subtle bugs. Modern compilers (with -Wall) reject this as an "implicit declaration of function" error, which is why prototypes are not optional.',
        'The void keyword has two uses in function signatures: void as a return type means the function performs an action but does not produce a value (like printf — well, printf technically returns int, but functions like your own printGreeting return nothing). void as a parameter list (void printGreeting(void)) means the function takes no arguments at all. The (void) form is important in C because empty parentheses int f() mean "unspecified parameters," not "no parameters" — a legacy quirk from K&R C.',
      ],
      codeExamples: [
        {
          id: 'u2-t6-s1-ex1',
          title: 'Prototype, Definition, and Call',
          code: '#include <stdio.h>\n\n/* Function Prototype (Declaration) */\nint multiply(int a, int b);\n\nint main(void) {\n    int result = multiply(4, 5); /* Call */\n    printf("Result: %d\\n", result);\n    return 0;\n}\n\n/* Function Definition */\nint multiply(int a, int b) {\n    return a * b;\n}',
          language: 'c',
          explanation: 'This program demonstrates the three-part lifecycle of a function: prototype, call, and definition. Because main appears before multiply in the source file, the compiler would not know what multiply is when it encounters the call on line 7 without the prototype on line 4. The prototype says: "trust me, a function called multiply exists that takes two ints and returns an int. You\'ll find its body later." This separation of declaration and definition is a fundamental C pattern that enables large programs to be split across multiple files — each file includes the prototypes (via header files) and the linker connects the calls to the definitions.',
          expectedOutput: 'Result: 20',
          lineBreakdown: [
            { lineNumber: 4, code: 'int multiply(int a, int b);', explanation: 'Prototype. Notice the semicolon at the end. It promises the compiler this function exists elsewhere.' },
            { lineNumber: 7, code: '    int result = multiply(4, 5);', explanation: 'Function call. 4 and 5 are the arguments.' },
            { lineNumber: 13, code: 'int multiply(int a, int b) {', explanation: 'Function definition. The actual implementation.' },
          ],
          relatedTopicIds: [],
        },
        {
          id: 'u2-t6-s1-ex2',
          title: 'Void Functions',
          code: '#include <stdio.h>\n\n/* A function that takes nothing and returns nothing */\nvoid printGreeting(void) {\n    printf("Hello, welcome to C programming!\\n");\n}\n\nint main(void) {\n    printGreeting();\n    printGreeting();\n    return 0;\n}',
          language: 'c',
          explanation: 'This example illustrates the void keyword in both of its roles. As a return type, void printGreeting(void) declares a function that performs an action (printing a greeting) without producing a value the caller can store. As a parameter list, (void) explicitly states that the function accepts no arguments. This explicit (void) is important in C: writing void printGreeting() with empty parentheses does not mean "no arguments" — it means "unspecified arguments," which is a legacy from pre-ANSI C that modern code should avoid. Always use (void) when you mean no parameters.',
          expectedOutput: 'Hello, welcome to C programming!\nHello, welcome to C programming!',
          lineBreakdown: [
            { lineNumber: 4, code: 'void printGreeting(void) {', explanation: 'void return type. void parameter list.' },
            { lineNumber: 9, code: '    printGreeting();', explanation: 'Calling a void function without expecting a return value.' },
          ],
          relatedTopicIds: [],
        },
      ],
      commonMistakes: [
        {
          id: 'u2-t6-s1-cm1',
          title: 'Missing Prototype',
          wrongCode: 'int main(void) {\n    float x = half(10.0f);\n}\nfloat half(float a) { return a / 2; }',
          correctCode: 'float half(float a);\nint main(void) {\n    float x = half(10.0f);\n}',
          explanation: 'Calling a function before the compiler knows it exists is like asking someone to send you a package without giving them your address. In C89, the compiler would silently invent a declaration (assume int return type and accept any arguments), which could lead to subtle type-mismatch bugs at runtime. Modern C (C99/C11) with -Wall correctly rejects this as an error. The fix is simple: either move the function definition above main, or add a prototype at the top of the file.',
          consequence: 'Compilation error with modern compilers. With older compilers or without -Wall, the code may compile but produce wrong results due to incorrect type assumptions.',
        },
        {
          id: 'u2-t6-s1-cm2',
          title: 'Not returning a value',
          wrongCode: 'int getScore() {\n    int s = 100;\n    /* forgot return statement */\n}',
          correctCode: 'int getScore() {\n    int s = 100;\n    return s;\n}',
          explanation: 'When a function declares a non-void return type, the caller is entitled to use the return value. If the function body does not execute a return statement on every possible code path, the caller receives whatever garbage happens to be in the CPU\'s return register — a value left over from some previous computation. This is undefined behavior, and it can produce results that appear correct on your machine but fail on another. The -Wall flag catches this with the "control reaches end of non-void function" warning.',
          consequence: 'The caller receives a garbage value that depends on previous CPU state. This is undefined behavior — different runs or different machines may produce different results.',
        },
      ],
      interviewCallouts: [
        {
          id: 'u2-t6-s1-ic1',
          title: 'Parameters vs Arguments',
          content: 'Interviewers love this question because it tests precise vocabulary. Parameters (also called formal parameters) are the variable names declared in the function definition — they are placeholders that receive values. Arguments (also called actual parameters) are the specific values you pass when calling the function. In int multiply(int a, int b), a and b are parameters. In multiply(4, 5), the values 4 and 5 are arguments. The arguments are copied into the parameters when the function is called.',
          relatedTopicIds: [],
          frequency: 'occasional',
        },
      ],
      checkpoints: [
        {
          id: 'u2-t6-s1-cp1',
          title: 'Function Syntax',
          description: 'Verify basic function syntax.',
          criteria: [
            'Write a prototype for a function that takes two floats and returns a float',
            'Explain the difference between void foo() and void foo(void) in C',
            'Identify parameters vs arguments in a code snippet',
          ],
          topicId: 'u2-t6',
        },
      ],
      revisionCards: [
        {
          id: 'u2-t6-s1-rc1',
          front: 'What is a function prototype?',
          back: 'A declaration that tells the compiler a function\'s name, return type, and parameters before its actual definition. It ends with a semicolon.',
          topicId: 'u2-t6',
          tags: ['functions', 'prototypes'],
        },
        {
          id: 'u2-t6-s1-rc2',
          front: 'What does a `void` return type mean?',
          back: 'It means the function performs an action but does not return a value to the caller.',
          topicId: 'u2-t6',
          tags: ['functions', 'void'],
        },
        {
          id: 'u2-t6-s1-rc3',
          front: 'Parameters vs Arguments: What is the difference?',
          back: 'Parameters are the variables in the function signature. Arguments are the actual values passed in during the function call.',
          topicId: 'u2-t6',
          tags: ['functions', 'terminology'],
        },
      ],
    },
    {
      id: 'u2-t6-s2',
      title: 'Pass by Value and the Call Stack',
      slug: 'pass-by-value',
      description: `C is strictly a pass-by-value language, and understanding what this means at the memory level is essential for avoiding an entire category of bugs that confuse beginners. When you call a function and pass a variable as an argument, the function does not receive the variable itself — it receives a copy of the variable's current value, stored in a brand-new local variable within the function's own stack frame. The original variable in the caller's scope is completely isolated from anything the function does to its copy. If you pass int a = 10 to a function void f(int x), the function creates a new variable x initialized to 10. Modifying x inside f has absolutely no effect on a in the caller.

This behavior is a direct consequence of the call stack architecture. When a function is called, the runtime allocates a new stack frame — a contiguous region of memory on the call stack that holds the function's parameters, local variables, the return address (so the CPU knows where to resume after the function returns), and saved register values. Each stack frame is an isolated workspace: variables in one frame cannot be accessed by name from another frame. The stack grows downward in memory as functions are called (each new frame is placed at a lower address) and shrinks as functions return (each returning frame is deallocated). You can visualize it as a stack of plates: main()'s frame is at the bottom, and each function call adds a plate on top. When a function returns, its plate is removed and all the data on it ceases to exist.

This last point has a critical implication: returning a pointer to a local variable is one of the most dangerous mistakes in C. When the function returns, its stack frame is deallocated, and the memory that held the local variable is reclaimed by the system. The pointer now refers to memory that no longer belongs to the variable — it is a dangling pointer. Dereferencing it might return the old value (if nothing has overwritten that memory yet), might return garbage (if the next function call has reused that stack space), or might crash the program. The behavior is undefined and unreproducible, making it one of the hardest classes of bugs to diagnose. The correct way to allow a function to modify data in the caller's scope is to pass a pointer to that data: the pointer is copied (pass-by-value still applies), but by dereferencing the copy, the function can reach back into the caller's stack frame and modify the original variable.`,
      keyPoints: [
        'C is strictly pass-by-value: when you call a function, every argument is copied into a fresh local variable within the function\'s stack frame. The function works exclusively with these copies and has no way to access or modify the original variables in the caller. This is a deliberate design choice that makes functions predictable — you can call any function knowing it cannot silently modify your local variables.',
        'Because the function works with copies, any modifications to parameters inside the function are invisible to the caller. If you write void tryToChange(int x) { x = 99; }, the change to x dies when the function returns. The caller\'s original variable is exactly as it was before the call.',
        'Each function call allocates a new "stack frame" on the call stack — a region of memory that holds the function\'s parameters, local variables, and bookkeeping information (like the return address). The stack grows downward in memory as functions are called and shrinks as they return. You can visualize it as a stack of plates: main() is the bottom plate, and each function call adds a plate on top.',
        'When a function returns, its stack frame is immediately destroyed (popped off the stack), and all of its local variables cease to exist. This is why returning a pointer to a local variable is catastrophically dangerous — the pointer refers to memory that has been reclaimed and may be overwritten by the next function call.',
        'To modify a variable from the caller\'s scope, you must pass a pointer (the memory address) to that variable. The pointer itself is still passed by value (the function gets a copy of the address), but by dereferencing the pointer, the function can reach back into the caller\'s stack frame and modify the original variable. This is the only mechanism for "output parameters" in C.',
      ],
      codeExamples: [
        {
          id: 'u2-t6-s2-ex1',
          title: 'Pass by Value Demonstration',
          code: '#include <stdio.h>\n\nvoid tryToChange(int x) {\n    x = 99; /* Only changes the local copy */\n    printf("Inside function: x = %d\\n", x);\n}\n\nint main(void) {\n    int a = 10;\n    printf("Before call: a = %d\\n", a);\n    \n    tryToChange(a);\n    \n    printf("After call: a = %d\\n", a);\n    return 0;\n}',
          language: 'c',
          explanation: 'This is the definitive demonstration of pass-by-value. When main calls tryToChange(a), the value of a (10) is copied into a brand-new variable x that lives in tryToChange\'s stack frame. The function modifies x to 99 — a completely separate variable from a. When tryToChange returns, its stack frame is destroyed, x ceases to exist, and a remains exactly 10. The key insight is that there is no connection between x and a after the copy is made; they are two independent variables that happened to start with the same value.',
          expectedOutput: 'Before call: a = 10\nInside function: x = 99\nAfter call: a = 10',
          lineBreakdown: [
            { lineNumber: 3, code: 'void tryToChange(int x) {', explanation: 'x is a brand new local variable created in this function\'s stack frame, initialized with a copy of the argument.' },
            { lineNumber: 4, code: '    x = 99;', explanation: 'Modifies the local copy. The original variable in main is unaffected.' },
          ],
          relatedTopicIds: ['u2-t3'],
        },
        {
          id: 'u2-t6-s2-ex2',
          title: 'Simulating Pass by Reference with Pointers',
          code: '#include <stdio.h>\n\nvoid actuallyChange(int *p) {\n    *p = 99; /* Dereferences the pointer to modify the caller\'s memory */\n    printf("Inside function: updated value to %d\\n", *p);\n}\n\nint main(void) {\n    int a = 10;\n    printf("Before call: a = %d\\n", a);\n    \n    actuallyChange(&a); /* Pass the ADDRESS of a */\n    \n    printf("After call: a = %d\\n", a);\n    return 0;\n}',
          language: 'c',
          explanation: 'This is how C programmers work around pass-by-value when they need a function to modify the caller\'s variables. Instead of passing the value of a, we pass its memory address (&a). The function receives this address as a pointer parameter p. Now p is still a copy (of the address, not the value), but by writing *p = 99, the function follows the address back to main\'s stack frame and overwrites the original variable. This is not "pass by reference" — C does not have that. It is "passing a reference (address) by value," which achieves the same effect. This pattern is how scanf works: scanf("%d", &x) needs &x so it can write the parsed integer to x\'s location.',
          expectedOutput: 'Before call: a = 10\nInside function: updated value to 99\nAfter call: a = 99',
          lineBreakdown: [
            { lineNumber: 12, code: '    actuallyChange(&a);', explanation: 'We pass the memory address of a, not its value.' },
            { lineNumber: 4, code: '    *p = 99;', explanation: 'Go to the address stored in p (which is a\'s address) and change the value to 99.' },
          ],
          relatedTopicIds: ['u2-t3'],
        },
      ],
      commonMistakes: [
        {
          id: 'u2-t6-s2-cm1',
          title: 'Returning a pointer to a local variable',
          wrongCode: 'int* getNumber() {\n    int local_val = 42;\n    return &local_val; /* HUGE ERROR */\n}',
          correctCode: 'int getNumber() {\n    int local_val = 42;\n    return local_val; /* Return by value */\n}',
          explanation: 'This is one of the most dangerous bugs in C, and it stems directly from the call stack model. When getNumber executes, local_val (42) lives in getNumber\'s stack frame. When getNumber returns, that stack frame is destroyed — the memory is reclaimed by the system and will be reused by the next function call. Returning &local_val gives the caller a pointer to memory that no longer belongs to local_val. This is called a "dangling pointer." Dereferencing it later might return 42 (if nothing has overwritten that memory yet), or it might return garbage, or it might crash. The behavior is undefined and depends on what function is called next.',
          consequence: 'Undefined behavior. The dangling pointer refers to deallocated stack memory. The memory will be overwritten by subsequent function calls, producing garbage values or crashes that are extremely difficult to reproduce and debug.',
        },
      ],
      interviewCallouts: [
        {
          id: 'u2-t6-s2-ic1',
          title: 'The Call Stack',
          content: 'This is a favorite whiteboard question. The call stack is a region of memory that grows downward (toward lower addresses) as functions are called and shrinks as they return. At the bottom is main()\'s stack frame. When main calls funcA(), a new frame is pushed on top containing funcA\'s parameters and local variables. If funcA calls funcB(), another frame is pushed on top of funcA\'s. When funcB returns, its frame is popped. The stack obeys Last-In-First-Out (LIFO) order, which is why it is called a "stack." A strong answer includes: (1) what goes in each frame (parameters, local variables, return address, saved registers), (2) the LIFO discipline, and (3) the finite size of the stack (which is why deeply recursive functions can cause stack overflow).',
          relatedTopicIds: [],
          frequency: 'common',
        },
      ],
      checkpoints: [
        {
          id: 'u2-t6-s2-cp1',
          title: 'Pass by Value semantics',
          description: 'Verify understanding of C parameter passing.',
          criteria: [
            'Explain why a simple swap(a, b) function fails in C without pointers',
            'Trace the execution stack for a function call',
            'Explain why returning a pointer to a local variable is dangerous',
          ],
          topicId: 'u2-t6',
        },
      ],
      revisionCards: [
        {
          id: 'u2-t6-s2-rc1',
          front: 'Does C use Pass by Value or Pass by Reference?',
          back: 'C strictly uses Pass by Value. Every argument is copied. To simulate Pass by Reference, you pass a pointer (memory address) by value.',
          topicId: 'u2-t6',
          tags: ['functions', 'pass-by-value'],
        },
        {
          id: 'u2-t6-s2-rc2',
          front: 'What happens to local variables when a function returns?',
          back: 'They are destroyed. The function\'s stack frame is popped off the call stack, freeing the memory for reuse.',
          topicId: 'u2-t6',
          tags: ['functions', 'stack', 'memory'],
        },
      ],
    },
  ],

  theoryQuestions: [
    {
      id: 'u2-t6-q1',
      type: 'mcq',
      topicId: 'u2-t6',
      difficulty: 'beginner',
      question: 'Which of the following is a valid function prototype?',
      options: ['int calc(int a, b)', 'void print(void);', 'float sum(float x float y);', 'double get()'],
      correctAnswer: 'void print(void);',
      explanation: 'A prototype must have a return type, name, proper parameter list with types for each parameter, and MUST end with a semicolon.',
      tags: ['functions', 'prototypes'],
    },
    {
      id: 'u2-t6-q2',
      type: 'predict-output',
      topicId: 'u2-t6',
      difficulty: 'intermediate',
      question: 'What is the output?',
      code: '#include <stdio.h>\nvoid func(int y) { y = y * 2; }\nint main(void) {\n    int x = 5;\n    func(x);\n    printf("%d", x);\n    return 0;\n}',
      correctAnswer: '5',
      explanation: 'Because C is pass-by-value, `func` receives a copy of `x` (named `y`). Multiplying `y` by 2 does not affect `x` in `main`.',
      tags: ['functions', 'pass-by-value'],
    },
    {
      id: 'u2-t6-q3',
      type: 'spot-bug',
      topicId: 'u2-t6',
      difficulty: 'advanced',
      question: 'Spot the bug:',
      code: 'int* getPtr() {\n    int val = 100;\n    return &val;\n}\nint main(void) {\n    int *p = getPtr();\n    printf("%d", *p);\n}',
      correctAnswer: 'Returning the address of a local variable.',
      explanation: '`val` is a local variable. When `getPtr` returns, `val` is destroyed. Returning its address creates a dangling pointer. Accessing `*p` is undefined behavior.',
      tags: ['functions', 'dangling-pointer'],
    },
    {
      id: 'u2-t6-q4',
      type: 'true-false',
      topicId: 'u2-t6',
      difficulty: 'beginner',
      question: 'A function declared with a `void` return type must not contain a `return;` statement.',
      correctAnswer: false,
      explanation: 'A void function CAN contain a `return;` statement (with no value) to exit the function early. It just cannot return a value like `return 5;`.',
      tags: ['functions', 'void', 'return'],
    },
    {
      id: 'u2-t6-q5',
      type: 'mcq',
      topicId: 'u2-t6',
      difficulty: 'beginner',
      question: 'Where do local variables live in memory?',
      options: ['The Heap', 'The Call Stack', 'The Data Segment', 'The BSS Segment'],
      correctAnswer: 'The Call Stack',
      explanation: 'Local variables and function parameters are stored in Stack Frames on the Call Stack. They are automatically created when the function is called and destroyed when it returns.',
      tags: ['functions', 'memory', 'stack'],
    },
  ],

  programmingProblems: [
    {
      id: 'u2-t6-new-easy',
      title: 'Array Reverse',
      topicId: 'u2-t6',
      difficulty: 'beginner',
      problemStatement: 'Reverse an array of N elements in place.',
      constraints: ['Do not use a secondary array'],
      sampleInput: '1 2 3',
      sampleOutput: '3 2 1',
      hints: ['Swap elements from both ends moving towards the center'],
      solution: '/* Array reverse implementation */',
      solutionExplanation: 'Swaps index i and N-1-i.',
      dryRun: [],
      tags: ['arrays']
    },
    {
      id: 'u2-t6-new-med',
      title: 'Matrix Diagonal Sum',
      topicId: 'u2-t6',
      difficulty: 'intermediate',
      problemStatement: 'Calculate the sum of the main diagonal of an NxN matrix.',
      constraints: ['Matrix is guaranteed to be square'],
      sampleInput: '1 2\n3 4',
      sampleOutput: '5',
      hints: ['Main diagonal elements have index [i][i]'],
      solution: '/* Diagonal sum implementation */',
      solutionExplanation: 'Iterates and sums arr[i][i].',
      dryRun: [],
      tags: ['matrix']
    },
    {
      id: 'u2-t6-new-hard',
      title: 'Recursive GCD',
      topicId: 'u2-t6',
      difficulty: 'advanced',
      problemStatement: 'Find the Greatest Common Divisor of two numbers using recursion (Euclidean algorithm).',
      constraints: ['Must use recursion'],
      sampleInput: '48 18',
      sampleOutput: '6',
      hints: ['gcd(a, b) = gcd(b, a % b)'],
      solution: '/* Recursive GCD implementation */',
      solutionExplanation: 'Implements Euclidean algorithm recursively.',
      dryRun: [],
      tags: ['recursion']
    },
    {
      id: 'u2-t6-p1',
      title: 'Even or Odd Function',
      topicId: 'u2-t6',
      difficulty: 'beginner',
      problemStatement: 'Write a function `int isEven(int n)` that returns 1 if a number is even, and 0 if it is odd. Test it in main.',
      constraints: ['Use a separate function', 'Function must return int, not print'],
      sampleInput: '7',
      sampleOutput: '7 is odd',
      hints: ['Use the modulo operator % 2', 'return 1 for true, 0 for false'],
      solution: '#include <stdio.h>\n\nint isEven(int n) {\n    if (n % 2 == 0) {\n        return 1;\n    }\n    return 0;\n}\n\nint main(void) {\n    int num;\n    scanf("%d", &num);\n    if (isEven(num)) {\n        printf("%d is even\\n", num);\n    } else {\n        printf("%d is odd\\n", num);\n    }\n    return 0;\n}',
      solutionExplanation: 'The function encapsulates the logic. It returns a boolean-like integer (1 or 0) which is then checked by `if` in main.',
      dryRun: [
        { step: 1, line: 12, variables: { num: '7' }, output: '', explanation: 'Read input' },
        { step: 2, line: 13, variables: {}, output: '', explanation: 'Call isEven(7)' },
        { step: 3, line: 4, variables: { n: '7' }, output: '', explanation: '7 % 2 == 1, so condition is false.' },
        { step: 4, line: 7, variables: {}, output: '', explanation: 'Returns 0' },
        { step: 5, line: 15, variables: {}, output: '7 is odd\\n', explanation: 'if(0) is false, goes to else branch.' },
      ],
      tags: ['functions', 'basics', 'logic'],
    },
    {
      id: 'u2-t6-p2',
      title: 'Factorial Function',
      topicId: 'u2-t6',
      difficulty: 'intermediate',
      problemStatement: 'Write an iterative (loop-based) function `long long factorial(int n)` that computes the factorial of n. Use `long long` to prevent overflow for larger numbers.',
      constraints: ['Do not use recursion', 'Handle 0! = 1'],
      sampleInput: '5',
      sampleOutput: '120',
      hints: ['Initialize a result variable to 1', 'Loop from 1 to n, multiplying result by the loop counter'],
      solution: '#include <stdio.h>\n\nlong long factorial(int n) {\n    long long result = 1;\n    for (int i = 1; i <= n; i++) {\n        result *= i;\n    }\n    return result;\n}\n\nint main(void) {\n    int num;\n    scanf("%d", &num);\n    printf("%lld\\n", factorial(num));\n    return 0;\n}',
      solutionExplanation: 'Standard iterative factorial. Using `long long` is important because factorials grow extremely fast (13! overflows a standard 32-bit int).',
      dryRun: [
        { step: 1, line: 13, variables: { num: '5' }, output: '', explanation: 'Call factorial(5)' },
        { step: 2, line: 4, variables: { result: '1' }, output: '', explanation: 'Initialize result to 1' },
        { step: 3, line: 6, variables: { i: '1', result: '1' }, output: '', explanation: 'result = 1 * 1' },
        { step: 4, line: 6, variables: { i: '5', result: '120' }, output: '', explanation: 'After 5 loops, result = 1 * 2 * 3 * 4 * 5 = 120' },
      ],
      tags: ['functions', 'loops', 'math'],
    },
    {
      id: 'u2-t6-p3',
      title: 'Update Balance (Pass by Address)',
      topicId: 'u2-t6',
      difficulty: 'intermediate',
      problemStatement: 'Write a function `void withdraw(float *balance, float amount)` that deducts `amount` from `balance` if the balance is sufficient. Print an error inside the function if insufficient. Test in main.',
      constraints: ['Function must modify the variable in main', 'Pass balance by reference (pointer)'],
      sampleInput: '100.0\n30.0',
      sampleOutput: 'New balance: 70.00',
      hints: ['Use *balance to check and modify the value', 'Pass &my_balance from main'],
      solution: '#include <stdio.h>\n\nvoid withdraw(float *balance, float amount) {\n    if (*balance >= amount) {\n        *balance -= amount;\n    } else {\n        printf("Insufficient funds\\n");\n    }\n}\n\nint main(void) {\n    float my_balance = 100.0f;\n    withdraw(&my_balance, 30.0f);\n    printf("New balance: %.2f\\n", my_balance);\n    \n    withdraw(&my_balance, 100.0f);\n    return 0;\n}',
      solutionExplanation: 'By passing the address of `my_balance`, the `withdraw` function can modify it directly. This demonstrates how to circumvent C\'s pass-by-value limitation when mutation is required.',
      dryRun: [
        { step: 1, line: 13, variables: { my_balance: '100.0' }, output: '', explanation: 'Call withdraw(&my_balance, 30)' },
        { step: 2, line: 4, variables: { '*balance': '100.0', amount: '30.0' }, output: '', explanation: '100 >= 30 is true' },
        { step: 3, line: 5, variables: { '*balance': '70.0' }, output: '', explanation: 'Modify value at address to 70' },
        { step: 4, line: 16, variables: {}, output: 'Insufficient funds\\n', explanation: 'Second call tries to withdraw 100 from 70. Fails.' },
      ],
      tags: ['functions', 'pointers', 'pass-by-reference'],
    },
  ],
};
