import type { Topic } from '../../../../types';

export const operatorsExpressions: Topic = {
  id: 'u1-t7',
  unitId: 'unit-1',
  title: 'Operators and Expressions',
  slug: 'operators-expressions',
  description: `Operators are the fundamental verbs of the C language — they specify the computations and comparisons that transform raw data into meaningful results. C provides a rich operator set spanning arithmetic (+, -, *, /, %), relational comparison (<, >, ==, !=), logical combination (&&, ||, !), bitwise manipulation (&, |, ^, ~, <<, >>), assignment (=, +=, -=), and several special-purpose operators (ternary ?:, sizeof, comma, cast). Unlike mathematics, where operator semantics are universally understood, C operators carry platform-specific behaviours, implicit type conversion rules, and evaluation order subtleties that can produce silently incorrect results if not understood precisely.

The most consequential of these subtleties is C's type promotion system: when operands of different types appear in the same expression, the compiler implicitly converts the narrower type to the wider type before performing the operation. A char is promoted to int, an int to long, and a float to double. This promotion is invisible in the source code but can change the result of a computation — most notably, integer division (5/2 = 2) silently truncates the fractional part, and mixing signed and unsigned types can produce counter-intuitive comparison results because the signed value is implicitly converted to unsigned.

Operator precedence and associativity determine the order in which sub-expressions are evaluated in the absence of explicit parentheses. C's precedence table, inherited from B and ultimately from BCPL, contains several counter-intuitive rankings — most notoriously, bitwise AND (&) has lower precedence than equality (==), meaning that a & b == c is parsed as a & (b == c) rather than (a & b) == c. This specific precedence ordering has been the source of countless bugs throughout C's history. When in doubt, explicit parentheses eliminate ambiguity, improve readability, and cost nothing at runtime.`,
  difficulty: 'beginner',
  prerequisites: ['u1-t5'],
  estimatedMinutes: 60,
  subtopics: [
    {
      id: 'u1-t7-s1', title: 'Arithmetic and Relational Operators', slug: 'arithmetic-relational',
      description: `Arithmetic operators (+, -, *, /, %) perform mathematical computations, and relational operators (<, >, <=, >=, ==, !=) compare values to produce boolean-like integer results. Together, they form the computational and decision-making foundation of every C program. However, C's arithmetic operators carry critical subtleties that differ from mathematical expectations and trip up virtually every beginner.

The most consequential subtlety is integer division. When both operands of the / operator are integers, C performs truncating division: it discards the fractional part entirely, rounding toward zero. Thus 5/2 evaluates to 2, not 2.5, and -7/2 evaluates to -3, not -3.5. The decimal component is silently destroyed with no warning from the compiler. To obtain a floating-point result, at least one operand must be a floating-point type: 5.0/2 or (double)5/2 both yield 2.5. This implicit truncation is the source of countless arithmetic bugs in real-world C code, particularly in averaging calculations (sum/count loses precision unless sum or count is cast to double).

The modulo operator (%) returns the remainder after integer division and is defined only for integer types — applying it to float or double is a compilation error. From C99 onward, the sign of the result matches the sign of the dividend (left operand): -7%3 is -1, and 7%-3 is 1. Relational operators compare two values and produce the integer 1 for true or 0 for false. Because C represents truth values as plain integers, relational expressions can participate directly in arithmetic: int count = (a > b) + (c > d) counts how many of two comparisons are true. This integer-based boolean model is a defining characteristic of C that permeates the language's conditional logic, loop constructs, and macro patterns.`,
      keyPoints: [
        'Addition, subtraction, and multiplication (+, -, *) work exactly as you expect from mathematics. But the division operator (/) has a critical twist: when both operands are integers, C performs integer division, which truncates the result toward zero. So 5/2 gives 2, not 2.5. The decimal part is silently discarded. To get a decimal result, at least one operand must be a floating-point type: 5.0/2 gives 2.5.',
        'The modulo operator (%) gives the remainder after integer division: 7%3 is 1 because 7 divided by 3 is 2 with a remainder of 1. This operator only works with integer types — you cannot use % with float or double. Modulo is surprisingly useful: checking if a number is even (n%2 == 0), wrapping values around a range (index % array_size), and extracting digits (n%10 gives the last digit).',
        'In C89 (the older C standard), the behavior of modulo with negative numbers was implementation-defined, meaning different compilers could give different answers. But from C99 onward, the result of the % operator always has the same sign as the dividend (the left operand): -7%3 is -1, and 7%-3 is 1. This was standardized to make behavior predictable across all platforms.',
        'Relational operators (<, >, <=, >=, ==, !=) compare two values and produce an integer result: 1 if the comparison is true, 0 if it is false. Unlike Python or Java, C has no boolean type in its original standard — it uses plain integers where 0 means false and any non-zero value means true. This integer representation of truth values has deep implications for how conditions, logical operators, and even some clever coding tricks work.',
        'The confusion between = (assignment) and == (equality comparison) is the single most dangerous operator mistake in C. Writing if (x = 5) does not check whether x equals 5 — it assigns 5 to x, and then the condition evaluates to 5 (which is truthy), so the if-body always executes. This bug compiles without error in many cases and silently corrupts your variable while also breaking your logic. Some programmers write if (5 == x) ("Yoda conditions") to prevent this, since 5 = x would be a compiler error.',
      ],
      codeExamples: [
        {
          id: 'u1-t7-s1-ex1', title: 'Integer Division Trap', language: 'c',
          code: '#include <stdio.h>\n\nint main(void) {\n    int a = 5, b = 2;\n    printf("5 / 2   = %d\\n", a / b);\n    printf("5.0 / 2 = %f\\n", 5.0 / 2);\n    printf("5 %% 2  = %d\\n", a % b);\n    printf("-7 %% 3 = %d\\n", -7 % 3);\n    return 0;\n}',
          explanation: 'This program reveals the integer division trap that catches nearly every C beginner. When both operands of / are integers, C truncates the result toward zero — it does not round, it does not give you a float, it simply throws away the decimal part. To get a decimal result, at least one operand must be a floating-point type: 5.0/2 uses floating-point division because 5.0 is a double. The %% in the printf format string is how you print a literal percent sign, since % normally introduces a format specifier.',
          expectedOutput: '5 / 2   = 2\n5.0 / 2 = 2.500000\n5 % 2  = 1\n-7 % 3 = -1',
          lineBreakdown: [
            { lineNumber: 5, code: '    a / b', explanation: 'Both int → integer division. 5/2 = 2 (truncated).' },
            { lineNumber: 6, code: '    5.0 / 2', explanation: '5.0 is double, so 2 is promoted to double. Result: 2.5.' },
            { lineNumber: 8, code: '    -7 % 3', explanation: 'In C99+, result sign matches dividend: -7%3 = -1.' },
          ],
          relatedTopicIds: ['u1-t5'],
        },
        {
          id: 'u1-t7-s1-ex2', title: 'Relational Operators Return 0 or 1', language: 'c',
          code: '#include <stdio.h>\n\nint main(void) {\n    int x = 10, y = 20;\n    printf("x == y: %d\\n", x == y);\n    printf("x != y: %d\\n", x != y);\n    printf("x < y:  %d\\n", x < y);\n    printf("x >= y: %d\\n", x >= y);\n    return 0;\n}',
          explanation: 'In C, there is no dedicated boolean type (at least not until C99 added _Bool, which is rarely used in practice). Instead, relational comparisons produce plain integers: 1 for true, 0 for false. This means you can store, print, and do arithmetic with the results of comparisons. For example, the expression (x < y) evaluates to the integer 1 or 0, and you could write int result = (x < y) + (x != y), which adds two truth values together. Understanding that "true" is just 1 and "false" is just 0 demystifies how conditions work in if statements and loops.',
          expectedOutput: 'x == y: 0\nx != y: 1\nx < y:  1\nx >= y: 0',
          lineBreakdown: [
            { lineNumber: 5, code: '    x == y', explanation: '10 == 20 is false → 0.' },
            { lineNumber: 6, code: '    x != y', explanation: '10 != 20 is true → 1.' },
          ],
          relatedTopicIds: ['u1-t8'],
        },
        {
          id: 'u1-t7-s1-ex3', title: 'Division by Zero — Runtime Error', language: 'c',
          code: '#include <stdio.h>\n\nint main(void) {\n    int a = 10, b = 0;\n    /* int result = a / b;  RUNTIME ERROR: floating point exception */\n    if (b != 0) {\n        printf("%d\\n", a / b);\n    } else {\n        printf("Cannot divide by zero!\\n");\n    }\n    return 0;\n}',
          explanation: 'Integer division by zero is not just wrong — it is catastrophic. Unlike floating-point division by zero (which produces infinity in IEEE 754), integer division by zero causes your program to crash immediately with a "floating-point exception" signal, despite the misleading name. The CPU literally cannot compute the result, so the operating system kills your program. The solution is to always check the denominator before dividing, using a simple if statement as a guard. This defensive programming pattern becomes second nature with practice.',
          expectedOutput: 'Cannot divide by zero!',
          lineBreakdown: [
            { lineNumber: 6, code: '    if (b != 0)', explanation: 'Guard against division by zero. Always do this check.' },
          ],
          relatedTopicIds: ['u1-t10'],
        },
      ],
      commonMistakes: [
        { id: 'u1-t7-s1-cm1', title: 'Using = instead of == in conditions',
          wrongCode: 'if (x = 5) { /* always true! */ }',
          correctCode: 'if (x == 5) { /* comparison */ }',
          explanation: 'This is one of the most insidious bugs in C because the code compiles without any error (and often without even a warning, unless you use -Wall). The statement x = 5 inside the if condition performs an assignment: it stores 5 into x and then evaluates to 5. Since 5 is non-zero, C treats it as true, so the if-body always executes. Meanwhile, x has been silently changed from whatever it was to 5, corrupting your program state. The fix is to use == for comparison. To protect yourself, some experienced C programmers write constants on the left: if (5 == x), because 5 = x would trigger a compiler error.',
          consequence: 'The if-body always executes regardless of the original value of x, and x is silently overwritten with the assigned value. The program produces wrong output but no error messages, making this bug extremely difficult to find through testing alone.' },
        { id: 'u1-t7-s1-cm2', title: 'Expecting decimal result from integer division',
          wrongCode: 'float avg = 95 / 4;  /* gives 23.0 not 23.75 */',
          correctCode: 'float avg = 95.0f / 4;  /* gives 23.75 */',
          explanation: 'The mental model error here is identical to the integer division trap we explored in data types: students assume that because the destination variable avg is a float, the division will automatically produce a decimal result. But C evaluates the right-hand side independently of the left-hand side. Since 95 and 4 are both integer literals, C performs integer division (95/4 = 23), then converts the integer 23 to the float 23.0 for storage. By the time the result reaches avg, the fractional part (0.75) is already lost. The fix is to make at least one operand a float: 95.0f / 4 or (float)95 / 4.',
          consequence: 'Percentage and average calculations silently lose their decimal precision. This is particularly dangerous because the program runs without errors and produces a result that looks plausible but is subtly wrong — 23.00 instead of 23.75.' },
      ],
      interviewCallouts: [
        { id: 'u1-t7-s1-ic1', title: 'Why does 5/2 give 2 in C?',
          content: 'Interviewers love this question because it tests whether you understand type promotion rules. When both operands of / are integers, C performs integer division, truncating toward zero. This is different from Python 3 where / always returns a float. In C, to get a decimal result from integer variables, you must explicitly cast at least one operand: (float)a / b or multiply by 1.0 first (a * 1.0 / b). A strong answer also mentions that the truncation is toward zero, not toward negative infinity — so -7/2 gives -3 in C, not -4.',
          relatedTopicIds: ['u1-t5'], frequency: 'common' },
      ],
      checkpoints: [
        { id: 'u1-t7-s1-cp1', title: 'Arithmetic Operators', description: 'Verify core operator understanding.',
          criteria: ['Explain why 7/2 is 3', 'Know what -7%3 gives in C99', 'Always guard against division by zero'], topicId: 'u1-t7' },
      ],
      revisionCards: [
        { id: 'u1-t7-s1-rc1', front: 'What does 5/2 give in C when both are int?', back: '2 (integer division truncates the decimal part). Use 5.0/2 for 2.5.', topicId: 'u1-t7', tags: ['integer-division'] },
        { id: 'u1-t7-s1-rc2', front: 'What does -7 % 3 give in C99+?', back: '-1. In C99+, the result of % has the same sign as the dividend (-7).', topicId: 'u1-t7', tags: ['modulo'] },
        { id: 'u1-t7-s1-rc3', front: 'What is the difference between = and ==?', back: '= is assignment (stores a value). == is comparison (checks equality, returns 0 or 1).', topicId: 'u1-t7', tags: ['operators', 'common-bug'] },
      ],
    },
    {
      id: 'u1-t7-s2', title: 'Logical, Bitwise, and Special Operators', slug: 'logical-bitwise-special',
      description: `Beyond basic arithmetic and comparison, C provides three additional categories of operators that are essential for systems programming and concise expression writing: logical operators for combining boolean conditions, bitwise operators for manipulating individual bits within integers, and special operators (increment/decrement, ternary, comma) that provide compact shorthand for common patterns.

Logical operators (&&, ||, !) combine boolean-valued expressions with an important optimisation called short-circuit evaluation. Logical AND (&&) evaluates its right operand only if the left operand is true (non-zero), because a false left operand guarantees the entire expression is false regardless of the right side. Logical OR (||) evaluates its right operand only if the left operand is false, because a true left operand guarantees the overall result is true. This is not merely a performance optimisation \u2014 it is a safety mechanism that programmers rely on to guard dangerous operations: the expression if (ptr != NULL && ptr->value > 0) is safe precisely because the dereference ptr->value is never executed when ptr is NULL.

Bitwise operators (&, |, ^, ~, <<, >>) treat integers as sequences of individual bits and perform operations on each bit position independently. Bitwise AND (&) produces 1 only where both inputs have 1 \u2014 used to mask specific bits. Bitwise OR (|) sets bits \u2014 used to enable flags. XOR (^) toggles bits. Left shift (<<) multiplies by powers of 2, and right shift (>>) divides by powers of 2 (with implementation-defined behavior for negative numbers). These operators are the backbone of embedded systems programming, network protocol parsing, and hardware register manipulation. The increment/decrement operators (++/--) and the ternary conditional (condition ? a : b) provide syntactic convenience, but the distinction between prefix (++x, which increments then evaluates) and postfix (x++, which evaluates then increments) is a frequent source of bugs in complex expressions. C's operator precedence table \u2014 a strict hierarchy from postfix operators at the top through assignment at the bottom \u2014 determines evaluation order in compound expressions; when in doubt, explicit parentheses prevent precedence-related bugs and improve readability.`,
      keyPoints: [
        'Logical AND (&&) evaluates to true (1) only if both operands are true. Crucially, it short-circuits: if the left operand is false, C does not even evaluate the right operand, because the result is guaranteed to be false regardless. This is not just an optimization — it is a safety feature you will use constantly, like checking if a pointer is non-NULL before dereferencing it: if (ptr != NULL && ptr->value > 0).',
        'Logical OR (||) evaluates to true (1) if at least one operand is true. It also short-circuits: if the left operand is true, the right operand is never evaluated because the result is already known. Short-circuit evaluation means the order of operands matters — always put the "safety check" on the left side.',
        'Bitwise operators work on individual bits within an integer, treating each bit independently. AND (&) produces 1 only where both inputs have 1. OR (|) produces 1 where either input has 1. XOR (^) produces 1 where exactly one input has 1. NOT (~) flips all bits. Left shift (<<) multiplies by powers of 2, and right shift (>>) divides by powers of 2. These operators are essential for embedded systems, network protocols, and competitive programming.',
        'Pre-increment (++x) and post-increment (x++) both add 1 to x, but they differ in when the increment happens relative to the expression that uses them. Pre-increment increments x first and then returns the new value. Post-increment returns the current value of x and then increments it afterward. In a standalone statement (x++;), there is no difference. But in an expression (int b = x++;), the distinction matters: b gets the old value of x, and x is incremented after.',
        'The ternary operator (condition ? value_if_true : value_if_false) is a compact inline if-else that evaluates to a single value. It is useful for simple conditional assignments: int max = (a > b) ? a : b. However, nested ternary expressions quickly become unreadable, so use them only for simple, single-level conditions.',
        'C has a detailed precedence table that determines which operators evaluate first in a complex expression. The hierarchy, from highest to lowest, is roughly: postfix operators ([], ->, ++) > unary operators (!, ~, ++x, *, &) > arithmetic (*, /, % then +, -) > comparison (<, >, ==, !=) > bitwise (&, ^, |) > logical (&&, ||) > ternary (?:) > assignment (=, +=, etc.). When in doubt, use parentheses — they make your intent explicit and prevent precedence bugs.',
      ],
      codeExamples: [
        {
          id: 'u1-t7-s2-ex1', title: 'Short-Circuit Evaluation', language: 'c',
          code: '#include <stdio.h>\n\nint main(void) {\n    int x = 0;\n    /* && short-circuits: second part NOT evaluated because x is 0 (false) */\n    if (x != 0 && 10 / x > 2) {\n        printf("Safe division\\n");\n    } else {\n        printf("Skipped division (x is 0)\\n");\n    }\n    return 0;\n}',
          explanation: 'Short-circuit evaluation is one of the most powerful and practical features of C\'s logical operators. In this example, if x is 0, then the expression x != 0 evaluates to false. Because the overall expression uses && (AND), C already knows the result must be false (false AND anything is false), so it skips the right operand entirely. This means 10/x is never computed, and the division by zero that would crash the program never happens. This pattern is used constantly in professional C code: check for a valid condition first, then perform the operation that depends on that condition, all in a single && expression.',
          expectedOutput: 'Skipped division (x is 0)',
          lineBreakdown: [
            { lineNumber: 6, code: '    x != 0 && 10 / x > 2', explanation: 'x!=0 is false (0), so && short-circuits. 10/x is never executed — no crash!' },
          ],
          relatedTopicIds: ['u1-t8'],
        },
        {
          id: 'u1-t7-s2-ex2', title: 'Pre vs Post Increment — The Most Misunderstood Topic', language: 'c',
          code: '#include <stdio.h>\n\nint main(void) {\n    int a = 5;\n    int b = ++a;  /* pre: increment a to 6, THEN assign 6 to b */\n    printf("a=%d b=%d\\n", a, b);\n\n    int c = 5;\n    int d = c++;  /* post: assign 5 to d, THEN increment c to 6 */\n    printf("c=%d d=%d\\n", c, d);\n    return 0;\n}',
          explanation: 'The difference between ++x and x++ is the single most frequently misunderstood operator concept in C, and it is a favorite exam question. When used in isolation (as a standalone statement), they are identical: both add 1 to x. But when used inside a larger expression, they differ in when the increment happens. Pre-increment (++a) increments a first and then yields the new value, so b receives 6. Post-increment (c++) yields the current value of c first and then increments it, so d receives 5 while c becomes 6. The key mental model: "pre" means "increment before I look at the value," and "post" means "let me look at the value, then increment."',
          expectedOutput: 'a=6 b=6\nc=6 d=5',
          lineBreakdown: [
            { lineNumber: 5, code: '    int b = ++a;', explanation: 'Pre-increment: a becomes 6 first, then b gets 6. Both are 6.' },
            { lineNumber: 9, code: '    int d = c++;', explanation: 'Post-increment: d gets current c (5), then c becomes 6. d=5, c=6.' },
          ],
          relatedTopicIds: [],
        },
        {
          id: 'u1-t7-s2-ex3', title: 'Ternary Operator and Bitwise Basics', language: 'c',
          code: '#include <stdio.h>\n\nint main(void) {\n    int x = 15;\n    /* Ternary: compact if-else */\n    const char *parity = (x % 2 == 0) ? "even" : "odd";\n    printf("%d is %s\\n", x, parity);\n\n    /* Bitwise: check if a number is power of 2 */\n    int n = 16;\n    int is_pow2 = (n > 0) && ((n & (n - 1)) == 0);\n    printf("%d is power of 2: %s\\n", n, is_pow2 ? "yes" : "no");\n    return 0;\n}',
          explanation: 'The ternary operator provides a concise way to choose between two values based on a condition, without the overhead of a full if-else block. It is particularly useful for assignments: const char *parity = (x % 2 == 0) ? "even" : "odd" is much more readable than a five-line if-else that does the same thing. The bitwise power-of-2 check (n & (n-1) == 0) is a classic trick worth understanding: a power of 2 in binary has exactly one bit set (like 16 = 10000). Subtracting 1 flips all bits below and including the set bit (15 = 01111). ANDing these together produces 0 if and only if there was exactly one bit set in the original number.',
          expectedOutput: '15 is odd\n16 is power of 2: yes',
          lineBreakdown: [
            { lineNumber: 6, code: '    (x % 2 == 0) ? "even" : "odd"', explanation: 'If condition true → "even", else → "odd". Compact one-line if-else.' },
            { lineNumber: 11, code: '    n & (n - 1)', explanation: '16 = 10000, 15 = 01111. 10000 & 01111 = 00000 = 0. Power of 2 has only one bit set.' },
          ],
          relatedTopicIds: [],
        },
      ],
      commonMistakes: [
        { id: 'u1-t7-s2-cm1', title: 'Using i++ and ++i interchangeably in expressions',
          wrongCode: 'int i = 5;\nint a = i++;  /* a = 5, i = 6 */\n/* Student expected a = 6 */',
          correctCode: 'int i = 5;\nint a = ++i;  /* a = 6, i = 6 */\n/* Pre-increment if you need the new value */',
          explanation: 'The confusion between pre-increment and post-increment is one of the most persistent in C because in the most common usage (standalone i++ in a for loop), both behave identically. Students develop the habit of treating them as interchangeable, and this works fine until they use one inside an expression. The mental model to internalize is: post-increment (i++) is like saying "give me the current value, and by the way, add 1 to the variable for later." Pre-increment (++i) is like saying "add 1 first, then give me the new value." If you need the updated value in the expression, use pre-increment.',
          consequence: 'The value used in the expression is one less than the programmer expected, creating an off-by-one error. These are notoriously difficult to debug because the variable itself ends up with the correct value after the statement — the error is only in what value was used within the expression.' },
        { id: 'u1-t7-s2-cm2', title: 'Confusing & (bitwise) with && (logical)',
          wrongCode: 'if (x > 0 & y > 0) { /* bitwise AND, not logical! */ }',
          correctCode: 'if (x > 0 && y > 0) { /* logical AND with short-circuit */ }',
          explanation: '& and && look similar but are fundamentally different operations. The single & is a bitwise AND that operates on individual bits of its operands: it compares each bit position independently and produces 1 only where both inputs have 1. The double && is a logical AND that treats its operands as boolean values (zero = false, non-zero = true) and produces 1 or 0. Critically, && short-circuits (skipping the right operand when the left is false), while & always evaluates both sides. In conditions, you almost always want && for its short-circuit safety.',
          consequence: 'Using & instead of && loses short-circuit evaluation, which can cause crashes if the right side performs an operation that depends on the left side being true (like dereferencing a pointer only after checking it is not NULL). Additionally, bitwise AND on non-boolean values produces unexpected results: (3 & 2) is 2 (not 1), which is truthy but not what you intended.' },
      ],
      interviewCallouts: [
        { id: 'u1-t7-s2-ic1', title: 'What is short-circuit evaluation?',
          content: 'Short-circuit evaluation is both a performance optimization and a safety mechanism. In && (AND), if the left operand evaluates to false, the right operand is guaranteed not to be evaluated. In || (OR), if the left operand evaluates to true, the right operand is skipped. This is used as a critical safety pattern throughout C: the expression if (ptr != NULL && ptr->value > 0) first checks that the pointer is valid, and only if it is will it attempt to dereference it. Without short-circuit evaluation, the dereference would happen even when ptr is NULL, crashing the program. When explaining this in an interview, use a concrete example showing how short-circuit prevents a crash, and mention that it also enables lazy evaluation patterns.',
          relatedTopicIds: [], frequency: 'common' },
      ],
      checkpoints: [
        { id: 'u1-t7-s2-cp1', title: 'Operators Mastery', description: 'Verify advanced operator understanding.',
          criteria: ['Explain pre vs post increment with an example', 'Explain short-circuit evaluation', 'Write a ternary expression', 'Know & vs && difference'], topicId: 'u1-t7' },
      ],
      revisionCards: [
        { id: 'u1-t7-s2-rc1', front: 'What does ++x do vs x++?', back: '++x (pre): increment first, then use new value. x++ (post): use current value, then increment. In i=5; a=i++; → a=5, i=6. In i=5; a=++i; → a=6, i=6.', topicId: 'u1-t7', tags: ['increment'] },
        { id: 'u1-t7-s2-rc2', front: 'What is short-circuit evaluation?', back: '&& stops if left is false. || stops if left is true. The right operand is not evaluated. Used to prevent crashes like: if (b != 0 && a/b > 1).', topicId: 'u1-t7', tags: ['logical', 'short-circuit'] },
        { id: 'u1-t7-s2-rc3', front: 'How to check if n is a power of 2 using bitwise?', back: '(n > 0) && ((n & (n-1)) == 0). Powers of 2 have exactly one bit set. n & (n-1) clears the lowest set bit — if result is 0, only one bit was set.', topicId: 'u1-t7', tags: ['bitwise', 'trick'] },
      ],
    },
  ],

  theoryQuestions: [
    { id: 'u1-t7-q1', type: 'predict-output', topicId: 'u1-t7', difficulty: 'beginner',
      question: 'What does this print?',
      code: '#include <stdio.h>\nint main(void) {\n    int x = 5;\n    printf("%d %d\\n", x++, ++x);\n    return 0;\n}',
      correctAnswer: 'Undefined — the order of evaluation of function arguments is unspecified in C.',
      explanation: 'Modifying x twice without a sequence point between them (in the same printf call) is undefined behavior. Never use ++ on the same variable multiple times in one expression.',
      tags: ['increment', 'UB'] },
    { id: 'u1-t7-q2', type: 'predict-output', topicId: 'u1-t7', difficulty: 'beginner',
      question: 'What does this print?',
      code: '#include <stdio.h>\nint main(void) {\n    int a = 10;\n    int b = a++;\n    int c = ++a;\n    printf("%d %d %d\\n", a, b, c);\n    return 0;\n}',
      correctAnswer: '12 10 12',
      explanation: 'b = a++ → b gets 10, a becomes 11. c = ++a → a becomes 12 first, c gets 12. Final: a=12, b=10, c=12.',
      tags: ['pre-post-increment'] },
    { id: 'u1-t7-q3', type: 'mcq', topicId: 'u1-t7', difficulty: 'beginner',
      question: 'What does the expression (5 > 3) && (2 > 8) evaluate to?',
      options: ['1', '0', '5', 'true'],
      correctAnswer: '0',
      explanation: '5>3 is true (1). 2>8 is false (0). 1 && 0 = 0 (false). Logical AND requires both sides to be true.',
      tags: ['logical', 'AND'] },
    { id: 'u1-t7-q4', type: 'spot-bug', topicId: 'u1-t7', difficulty: 'beginner',
      question: 'Find the bug:',
      code: '#include <stdio.h>\nint main(void) {\n    int x = 10;\n    if (x = 20) {\n        printf("x is 20\\n");\n    }\n    return 0;\n}',
      correctAnswer: 'Line 4 uses = (assignment) instead of == (comparison). x is set to 20 and the condition is always true.',
      explanation: 'x = 20 assigns 20 to x and evaluates to 20 (truthy). It should be x == 20. This always prints "x is 20" AND changes x to 20.',
      tags: ['assignment-vs-comparison'] },
    { id: 'u1-t7-q5', type: 'mcq', topicId: 'u1-t7', difficulty: 'intermediate',
      question: 'What is the value of: 2 + 3 * 4 - 1?',
      options: ['19', '13', '14', '20'],
      correctAnswer: '13',
      explanation: 'Precedence: * first → 3*4=12. Then left-to-right: 2+12-1 = 13. Not (2+3)*(4-1)=15.',
      tags: ['precedence'] },
    { id: 'u1-t7-q6', type: 'true-false', topicId: 'u1-t7', difficulty: 'beginner',
      question: 'In C, the logical AND operator (&&) evaluates both operands even if the first is false.',
      correctAnswer: false,
      explanation: '&& short-circuits: if the left operand is false (0), the right operand is never evaluated. This is a safety feature used to prevent crashes.',
      tags: ['short-circuit'] },
  ],

  programmingProblems: [
    {
      id: 'u1-t7-new-easy',
      title: 'Armstrong Number Check',
      topicId: 'u1-t7',
      difficulty: 'beginner',
      problemStatement: 'Read a 3-digit number. Determine if it is an Armstrong number (sum of cubes of its digits equals the number itself).',
      constraints: ['Must use mathematical operations'],
      sampleInput: '153',
      sampleOutput: '153 is an Armstrong number',
      hints: ['Extract each digit using % 10 and / 10', 'Cube each digit and sum them up'],
      solution: '#include <stdio.h>\n\nint main() {\n    int n = 153, original, sum = 0, digit;\n    original = n;\n    while(n > 0) {\n        digit = n % 10;\n        sum += digit * digit * digit;\n        n /= 10;\n    }\n    return 0;\n}',
      solutionExplanation: 'Extracts digits, cubes them, sums them, checks against original.',
      dryRun: [],
      tags: ['number-theory']
    },
    {
      id: 'u1-t7-new-med',
      title: 'Factorial Calculation',
      topicId: 'u1-t7',
      difficulty: 'intermediate',
      problemStatement: 'Calculate the factorial of a given number n without using recursion.',
      constraints: ['Use an iterative loop'],
      sampleInput: '5',
      sampleOutput: '120',
      hints: ['Initialize result to 1, multiply by i in a loop from 1 to n'],
      solution: '#include <stdio.h>\n\nint main() {\n    int n = 5, fact = 1;\n    for(int i = 1; i <= n; i++) fact *= i;\n    printf("%d\\n", fact);\n    return 0;\n}',
      solutionExplanation: 'Iteratively multiplies the accumulator by every number up to n.',
      dryRun: [],
      tags: ['math', 'loops']
    },
    {
      id: 'u1-t7-new-hard',
      title: 'Diamond Pattern',
      topicId: 'u1-t7',
      difficulty: 'advanced',
      problemStatement: 'Print a diamond pattern of stars for a given number of rows n.',
      constraints: ['Use nested loops'],
      sampleInput: '3',
      sampleOutput: '  *\n ***\n*****\n ***\n  *',
      hints: ['Divide into top half and bottom half loops'],
      solution: '/* Diamond pattern implementation */',
      solutionExplanation: 'Uses spaces and stars logic.',
      dryRun: [],
      tags: ['patterns']
    },
    { id: 'u1-t7-p1', title: 'Check Leap Year', topicId: 'u1-t7', difficulty: 'beginner',
      problemStatement: 'Read a year from the user. Print whether it is a leap year. A year is leap if: divisible by 4 AND (not divisible by 100 OR divisible by 400).',
      constraints: ['Use logical operators', 'Single if condition'], sampleInput: '2024', sampleOutput: '2024 is a leap year',
      hints: ['Condition: (year%4==0 && year%100!=0) || year%400==0', 'Use parentheses carefully'],
      solution: '#include <stdio.h>\n\nint main(void) {\n    int year;\n    scanf("%d", &year);\n    if ((year % 4 == 0 && year % 100 != 0) || year % 400 == 0) {\n        printf("%d is a leap year\\n", year);\n    } else {\n        printf("%d is not a leap year\\n", year);\n    }\n    return 0;\n}',
      solutionExplanation: 'Combines %, ==, !=, &&, || in one condition. 2024: 2024%4==0 (true) && 2024%100!=0 (true) → leap.',
      dryRun: [
        { step: 1, line: 4, variables: { year: '2024' }, output: '', explanation: 'Read year=2024.' },
        { step: 2, line: 6, variables: { year: '2024' }, output: '', explanation: '2024%4=0 ✓, 2024%100=24≠0 ✓ → true && true = true.' },
        { step: 3, line: 7, variables: {}, output: '2024 is a leap year', explanation: 'Condition true → leap year.' },
      ],
      tags: ['logical-operators', 'modulo'] },
    { id: 'u1-t7-p2', title: 'Max of Three Using Ternary', topicId: 'u1-t7', difficulty: 'beginner',
      problemStatement: 'Read three integers and find the maximum using only ternary operators (no if-else).',
      constraints: ['Must use ternary operator', 'No if-else allowed'], sampleInput: '10 25 18', sampleOutput: 'Max = 25',
      hints: ['Find max of first two, then compare with third', 'int max = (a > b) ? a : b; then max = (max > c) ? max : c;'],
      solution: '#include <stdio.h>\n\nint main(void) {\n    int a, b, c;\n    scanf("%d %d %d", &a, &b, &c);\n    int max = (a > b) ? ((a > c) ? a : c) : ((b > c) ? b : c);\n    printf("Max = %d\\n", max);\n    return 0;\n}',
      solutionExplanation: 'Nested ternary: first compare a>b. If true, compare a vs c. If false, compare b vs c. One expression, no if-else.',
      dryRun: [
        { step: 1, line: 5, variables: { a: '10', b: '25', c: '18' }, output: '', explanation: 'Read three numbers.' },
        { step: 2, line: 6, variables: { max: '25' }, output: '', explanation: 'a>b? No → (b>c? 25>18 yes → 25). max=25.' },
      ],
      tags: ['ternary', 'max'] },
    { id: 'u1-t7-p3', title: 'Bit Counter', topicId: 'u1-t7', difficulty: 'intermediate',
      problemStatement: 'Read a positive integer and count how many bits are set to 1 in its binary representation.',
      constraints: ['Use bitwise AND and right shift', 'Print the count'], sampleInput: '13', sampleOutput: 'Number of set bits in 13: 3',
      hints: ['13 in binary = 1101 (three 1-bits)', 'Check last bit with n & 1, then shift right with n >>= 1', 'Loop while n > 0'],
      solution: '#include <stdio.h>\n\nint main(void) {\n    int n;\n    scanf("%d", &n);\n    int original = n, count = 0;\n    while (n > 0) {\n        count += (n & 1);\n        n >>= 1;\n    }\n    printf("Number of set bits in %d: %d\\n", original, count);\n    return 0;\n}',
      solutionExplanation: 'n & 1 checks if the last bit is 1. n >>= 1 shifts all bits right by 1 (divides by 2). Repeat until n is 0.',
      dryRun: [
        { step: 1, line: 7, variables: { n: '13 (1101)', count: '0' }, output: '', explanation: '13 & 1 = 1. count=1. n>>=1 → n=6 (110).' },
        { step: 2, line: 7, variables: { n: '6 (110)', count: '1' }, output: '', explanation: '6 & 1 = 0. count=1. n>>=1 → n=3 (11).' },
        { step: 3, line: 7, variables: { n: '3 (11)', count: '1' }, output: '', explanation: '3 & 1 = 1. count=2. n>>=1 → n=1 (1).' },
        { step: 4, line: 7, variables: { n: '1 (1)', count: '2' }, output: '', explanation: '1 & 1 = 1. count=3. n>>=1 → n=0. Loop ends.' },
      ],
      tags: ['bitwise', 'bit-count'] },
  ],
};
