import type { Topic } from '../../../../types';

export const languageBehaviors: Topic = {
  id: 'u1-t10',
  unitId: 'unit-1',
  title: 'Language Specifications & Behaviors',
  slug: 'language-behaviors',
  description: `The C standard classifies program behaviour into four categories that range from fully deterministic to completely unpredictable, and understanding this classification is arguably the single most important conceptual foundation for writing correct and portable C code. Most high-level languages protect programmers from dangerous operations by raising exceptions (Python), throwing errors (Java), or performing silent conversions (JavaScript). C deliberately does none of these things — instead, it grants the programmer direct access to hardware-level operations and places the full burden of correctness on the programmer's shoulders.

The most consequential category is undefined behaviour (UB): operations for which the C standard imposes literally no requirements on the resulting program behaviour. A program that triggers UB may crash, produce garbage output, silently corrupt unrelated data structures, or appear to work perfectly for years before failing catastrophically under a different compiler version or optimisation level. Modern optimising compilers actively exploit the assumption that UB never occurs, using it as license to eliminate entire code paths, reorder instructions, and perform transformations that are logically valid only if the UB assumption holds.

Between well-defined behaviour (identical results on every platform) and UB lie implementation-defined behaviour (the result depends on the platform but must be documented by the compiler, such as sizeof(int) or the signedness of plain char) and unspecified behaviour (the compiler may choose between valid alternatives without documenting the choice, such as the evaluation order of function arguments). Programmers who understand these categories write code that is correct by construction rather than by coincidence, avoiding the common trap of declaring code "working" simply because it produces the expected output on one particular machine with one particular compiler.`,
  difficulty: 'intermediate',
  prerequisites: ['u1-t7'],
  estimatedMinutes: 40,
  subtopics: [
    {
      id: 'u1-t10-s1', title: 'Undefined, Implementation-Defined, and Unspecified Behavior', slug: 'behavior-categories',
      description: `The C standard categorises program behaviour into four levels that form a spectrum from fully deterministic to completely unpredictable, and understanding this spectrum is essential for writing C code that is genuinely correct rather than merely coincidentally working on one particular machine.

Well-defined behaviour produces identical results on every conforming implementation: 2 + 3 is always 5, array[0] always accesses the first element, and sizeof(char) is always 1. Implementation-defined behaviour produces a result that depends on the specific compiler and platform, but the compiler is required to document its choice: sizeof(int) is typically 4 on modern desktops but could be 2 on embedded 16-bit processors; whether plain char is signed or unsigned varies between ARM (unsigned) and x86 (signed) by default. Code relying on implementation-defined behaviour is correct on a given platform but not portable. Unspecified behaviour allows the compiler to choose between valid alternatives without documenting the choice: the order in which function arguments are evaluated (left-to-right or right-to-left) is unspecified, meaning f(a++, a++) can produce different results across compilers or even across different optimisation levels of the same compiler.

Undefined behaviour (UB) is the most dangerous category: the C standard imposes literally no requirements on what happens when it is triggered. The program may crash, produce garbage output, silently corrupt unrelated data, or appear to work perfectly for years before failing catastrophically under a different compiler version or optimisation flag. Crucially, modern optimising compilers actively exploit the assumption that UB never occurs: if a code path would trigger UB, the compiler may assume that path is unreachable and eliminate it entirely, or may transform surrounding code in ways that are logically correct only under the no-UB assumption. The most common sources of UB that beginners encounter are signed integer overflow (INT_MAX + 1), out-of-bounds array access, NULL pointer dereference, reading uninitialised variables, division by zero, and modifying a variable twice in the same expression without a sequence point (i = i++). Each of these can produce deceptively correct results on one platform while causing spectacular failures on another.`,
      keyPoints: [
        'Undefined Behavior (UB) is the most dangerous category: the C standard imposes literally no requirements on what happens. Your program can crash, produce wrong output, silently corrupt data, or even appear to work perfectly on your machine but fail on your friend\'s. The folklore joke is that UB allows the compiler to "make demons fly out of your nose" — meaning absolutely anything goes. More practically, modern compilers exploit UB for optimization: they assume UB never occurs, and if your code triggers it, the optimizer may transform your program in ways that make no sense to a human reader.',
        'The most common sources of UB that you will encounter in this course are: signed integer overflow (INT_MAX + 1), accessing an array beyond its bounds (arr[5] in a 5-element array), dereferencing a NULL pointer, reading a variable before it has been initialized, dividing by zero, and modifying a variable twice in the same expression without an intervening sequence point (i = i++). Each of these is a ticking time bomb that might produce the expected result on your machine today but explode on a different machine, a different compiler version, or with different optimization flags.',
        'Implementation-Defined Behavior is safer: the result depends on the platform, but the compiler must document what it does. The most common example is sizeof(int), which is 4 bytes on most modern systems but could be 2 bytes on an embedded 16-bit processor. Other examples include whether the char type is signed or unsigned by default, and how negative numbers are represented (virtually all modern systems use two\'s complement, but the standard allows other representations). When you use implementation-defined features, your code is correct but not portable.',
        'Unspecified Behavior is the middle ground: the standard gives the compiler a choice between valid options, but the compiler does not need to document or be consistent about which option it picks. The most important example is the order in which function arguments are evaluated: in printf("%d %d", f(), g()), the compiler may call f() first or g() first, and may make a different choice each time it compiles your code. This is why expressions like printf("%d %d", i++, i++) are undefined — the combination of unspecified evaluation order and multiple modifications of i creates UB.',
        'Why does C have UB at all? The answer is performance. By declaring certain operations as undefined, the C standard gives the compiler permission to assume they never happen. This assumption enables powerful optimizations. For example, because signed overflow is UB, the compiler can assume that x + 1 > x is always true (since the only way it would not be is through overflow, which is UB). This allows the compiler to simplify comparisons, eliminate branches, and generate faster code. Languages like Python, which check for every possible error, pay a performance cost for that safety.',
        'During development, compile with gcc -fsanitize=undefined to enable the Undefined Behavior Sanitizer (UBSan). This instruments your program to detect UB at runtime and print a descriptive error message that tells you exactly what went wrong and where. UBSan adds some runtime overhead, so it is a development tool, not a production setting. But using it routinely will catch UB that testing might miss because UB can produce "correct" results on your machine by coincidence.',
      ],
      codeExamples: [
        {
          id: 'u1-t10-s1-ex1', title: 'Signed Integer Overflow — Undefined Behavior', language: 'c',
          code: '#include <stdio.h>\n#include <limits.h>\n\nint main(void) {\n    int x = INT_MAX;\n    printf("INT_MAX = %d\\n", x);\n    /* x + 1 is UNDEFINED BEHAVIOR for signed int! */\n    /* The compiler may optimize assuming this never happens */\n    printf("INT_MAX + 1 = %d (UB!)\\n", x + 1);\n    return 0;\n}',
          explanation: 'This program demonstrates the most common form of undefined behavior: signed integer overflow. When x equals INT_MAX (2,147,483,647) and you add 1, the mathematical result (2,147,483,648) exceeds the range of a signed 32-bit integer. The C standard declares this undefined, meaning the compiler is not required to produce any particular result. On most systems using two\'s complement arithmetic, the bit pattern wraps around to -2,147,483,648 (INT_MIN), but this is a coincidence of the hardware, not a guarantee. A clever compiler with optimization enabled might see this code and reason: "x + 1 can never overflow because overflow is UB and UB never happens in well-formed programs, so I can assume x + 1 > x and optimize accordingly." This kind of reasoning can cause the compiler to remove code, reorder operations, or make other changes that break your program in subtle ways.',
          expectedOutput: 'INT_MAX = 2147483647\nINT_MAX + 1 = -2147483648 (UB!)',
          lineBreakdown: [
            { lineNumber: 5, code: '    int x = INT_MAX;', explanation: 'x = 2147483647 — the largest signed int.' },
            { lineNumber: 9, code: '    x + 1', explanation: 'UB! The result is unpredictable. It often wraps to -2147483648 but the compiler can assume this never happens.' },
          ],
          relatedTopicIds: ['u1-t6'],
        },
        {
          id: 'u1-t10-s1-ex2', title: 'Array Out of Bounds — Silent Corruption', language: 'c',
          code: '#include <stdio.h>\n\nint main(void) {\n    int arr[5] = {10, 20, 30, 40, 50};\n    /* Accessing arr[5] is UB — no bounds checking in C! */\n    printf("arr[4] = %d (valid)\\n", arr[4]);\n    /* printf("arr[5] = %d (UB!)\\n", arr[5]); */\n    /* Could read garbage, corrupt another variable, or crash */\n    printf("Always check array bounds yourself!\\n");\n    return 0;\n}',
          explanation: 'C performs absolutely no array bounds checking, either at compile time or at runtime. When you write arr[5] in a 5-element array (valid indices 0–4), C happily calculates the memory address as "start of arr + 5 * sizeof(int)" and reads or writes whatever happens to be at that location. This might be another local variable, part of the function\'s return address on the stack, or unmapped memory that triggers a segmentation fault. The terrifying part is that out-of-bounds access often appears to work correctly — the adjacent memory happens to contain zero or some plausible value — and the bug only manifests when the program is run with different data, on a different machine, or after adding unrelated code that changes the stack layout. Buffer overflow vulnerabilities, which have been the root cause of some of the most devastating security breaches in computing history, are all variants of this fundamental issue.',
          expectedOutput: 'arr[4] = 50 (valid)\nAlways check array bounds yourself!',
          lineBreakdown: [
            { lineNumber: 4, code: '    int arr[5] = {...};', explanation: 'Valid indices: 0 to 4. arr[5] is out of bounds.' },
            { lineNumber: 7, code: '    /* arr[5] */', explanation: 'UB: reads memory past the array. Could be another variable, stack frame, or unmapped memory (crash).' },
          ],
          relatedTopicIds: [],
        },
        {
          id: 'u1-t10-s1-ex3', title: 'Using -fsanitize=undefined to Catch UB', language: 'c',
          code: '#include <stdio.h>\n#include <limits.h>\n\nint main(void) {\n    int x = INT_MAX;\n    int y = x + 1;  /* UB: sanitizer will catch this! */\n    printf("%d\\n", y);\n    return 0;\n}\n\n/* Compile with:\n   gcc -std=c11 -fsanitize=undefined -g overflow.c -o overflow\n   \n   Output:\n   overflow.c:6:17: runtime error: signed integer overflow:\n   2147483647 + 1 cannot be represented in type \'int\'\n*/',
          explanation: 'The Undefined Behavior Sanitizer (UBSan) is one of the most valuable tools in your C development toolkit. When you compile with -fsanitize=undefined, GCC inserts small checks before every operation that could trigger UB: before every signed addition (to check for overflow), before every array access (to check bounds, when possible), before every division (to check for zero), and so on. If any check fails at runtime, UBSan prints a detailed error message that tells you the exact file, line number, and nature of the violation. This is dramatically more helpful than the alternative (a mysterious wrong answer or a crash with no useful information). The small runtime overhead is negligible during development and testing.',
          expectedOutput: '-2147483648',
          lineBreakdown: [
            { lineNumber: 6, code: '    int y = x + 1;', explanation: 'UBSan catches this: "signed integer overflow: 2147483647 + 1 cannot be represented in type int".' },
            { lineNumber: 12, code: '    -fsanitize=undefined', explanation: 'GCC flag that instruments the binary to detect UB at runtime.' },
          ],
          relatedTopicIds: ['u1-t3'],
        },
      ],
      commonMistakes: [
        { id: 'u1-t10-s1-cm1', title: 'Modifying a variable twice in one expression',
          wrongCode: 'int i = 5;\nint x = i++ + ++i;  /* UB! */',
          correctCode: 'int i = 5;\ni++;\nint x = i + 1;  /* separate statements */',
          explanation: 'This expression is one of the most commonly asked about in C exams, and the answer is always "undefined behavior." The issue is sequence points: in C, modifying a variable and reading it for a different purpose between the same pair of sequence points is UB. In i++ + ++i, the variable i is modified twice (once by the post-increment, once by the pre-increment) with no sequence point between them. The compiler is free to evaluate these in any order and produce any result. Different compilers genuinely do produce different results for this exact expression, which proves it is not just "theoretically" undefined but practically unpredictable. The fix is trivially simple: break the expression into separate statements where each modification is separated by a semicolon (which is a sequence point).',
          consequence: 'Different compilers, or even the same compiler with different optimization levels, produce different results. There is no "correct" answer — the expression is fundamentally meaningless in C.' },
        { id: 'u1-t10-s1-cm2', title: 'Assuming int is always 4 bytes',
          wrongCode: 'int data[1000000];  /* assumes 4MB, might be 2MB on 16-bit */',
          correctCode: '#include <stdint.h>\nint32_t data[1000000];  /* guaranteed 4 bytes */',
          explanation: 'sizeof(int) is implementation-defined, meaning its value depends on your specific platform and compiler but must be documented. On most 64-bit desktop systems, int is 4 bytes, but on 16-bit embedded systems, it could be 2 bytes. If your program allocates memory based on the assumption that int is always 4 bytes, it will break on platforms where it is not. The stdint.h header solves this by providing types with exact, guaranteed sizes: int32_t is always exactly 32 bits, uint64_t is always exactly 64 bits. Use these types whenever you need a specific size, such as when working with file formats, network protocols, or hardware registers.',
          consequence: 'Data structures have incorrect sizes on platforms where int is not 4 bytes. Array allocations use the wrong amount of memory. Serialized data (written to files or networks) becomes incompatible between platforms.' },
      ],
      interviewCallouts: [
        { id: 'u1-t10-s1-ic1', title: 'What is undefined behavior and why does C have it?',
          content: 'This is one of the most important conceptual questions you will face in a viva, and a strong answer demonstrates genuine understanding of C\'s design philosophy. Undefined behavior exists because C was designed for maximum performance on any hardware. By leaving certain operations undefined, the C standard gives the compiler permission to assume they never happen, which enables aggressive optimization. For example, since signed overflow is UB, the compiler can assume that for any signed int x, the expression x + 1 > x is always true, and it can eliminate an entire branch of code based on that assumption. Languages like Python do not have UB because they check for overflow, bounds violations, and type errors at runtime — but that checking costs performance. C\'s philosophy is "trust the programmer" — if you promise not to trigger UB, the compiler rewards you with faster code. If you break that promise, the compiler feels no obligation to save you.',
          relatedTopicIds: [], frequency: 'common' },
      ],
      checkpoints: [
        { id: 'u1-t10-s1-cp1', title: 'Behavior Categories', description: 'Verify understanding of C behavior categories.',
          criteria: ['Name 3 examples of undefined behavior', 'Explain why signed overflow is UB but unsigned is not', 'Know how to use -fsanitize=undefined', 'Give an example of implementation-defined behavior'], topicId: 'u1-t10' },
      ],
      revisionCards: [
        { id: 'u1-t10-s1-rc1', front: 'What is undefined behavior in C?', back: 'Code for which the C standard imposes no requirements. Anything can happen: crash, garbage output, silent corruption. The compiler can assume it never occurs and optimize accordingly.', topicId: 'u1-t10', tags: ['UB'] },
        { id: 'u1-t10-s1-rc2', front: 'Give 3 examples of undefined behavior.', back: '1) Signed integer overflow. 2) Array out-of-bounds access. 3) Reading an uninitialized variable. Others: NULL dereference, i = i++, division by zero.', topicId: 'u1-t10', tags: ['UB', 'examples'] },
        { id: 'u1-t10-s1-rc3', front: 'What is implementation-defined behavior?', back: 'Behavior that depends on the platform but must be documented by the compiler. Examples: sizeof(int), whether char is signed, right-shift of negative numbers.', topicId: 'u1-t10', tags: ['implementation-defined'] },
        { id: 'u1-t10-s1-rc4', front: 'How to detect UB during development?', back: 'Compile with gcc -fsanitize=undefined. The sanitizer instruments the binary to detect UB at runtime and print descriptive error messages.', topicId: 'u1-t10', tags: ['UBSan', 'debugging'] },
      ],
    },
  ],

  theoryQuestions: [
    { id: 'u1-t10-q1', type: 'mcq', topicId: 'u1-t10', difficulty: 'intermediate',
      question: 'Which of the following is NOT undefined behavior?',
      options: ['Signed integer overflow', 'Unsigned integer overflow', 'Dereferencing a NULL pointer', 'Accessing array out of bounds'],
      correctAnswer: 'Unsigned integer overflow',
      explanation: 'Unsigned overflow is well-defined: it wraps around using modular arithmetic. All other options are undefined behavior.',
      tags: ['UB', 'unsigned'] },
    { id: 'u1-t10-q2', type: 'predict-output', topicId: 'u1-t10', difficulty: 'intermediate',
      question: 'What does this print?',
      code: '#include <stdio.h>\nint main(void) {\n    int i = 3;\n    printf("%d %d\\n", i++, i++);\n    return 0;\n}',
      correctAnswer: 'Undefined — the order of evaluation of printf arguments is unspecified, and modifying i twice is UB.',
      explanation: 'The order in which function arguments are evaluated is unspecified. Modifying i twice without a sequence point is UB. Different compilers produce different results.',
      tags: ['UB', 'sequence-point'] },
    { id: 'u1-t10-q3', type: 'mcq', topicId: 'u1-t10', difficulty: 'beginner',
      question: 'What type of behavior is sizeof(int) being 4 on one system and 2 on another?',
      options: ['Undefined behavior', 'Implementation-defined behavior', 'Unspecified behavior', 'Well-defined behavior'],
      correctAnswer: 'Implementation-defined behavior',
      explanation: 'The size of int depends on the platform, but the compiler must document it. This is implementation-defined — not undefined (anything goes) or unspecified (undocumented choice).',
      tags: ['implementation-defined'] },
    { id: 'u1-t10-q4', type: 'spot-bug', topicId: 'u1-t10', difficulty: 'intermediate',
      question: 'Find the undefined behavior:',
      code: '#include <stdio.h>\nint main(void) {\n    int arr[3] = {1, 2, 3};\n    for (int i = 0; i <= 3; i++) {\n        printf("%d\\n", arr[i]);\n    }\n    return 0;\n}',
      correctAnswer: 'Array out of bounds: arr has 3 elements (indices 0-2) but the loop accesses arr[3]. Should be i < 3.',
      explanation: 'i <= 3 means i goes 0, 1, 2, 3. arr[3] is past the end of the array. This is UB — it may print garbage, crash, or silently corrupt memory.',
      tags: ['array', 'bounds', 'UB'] },
    { id: 'u1-t10-q5', type: 'true-false', topicId: 'u1-t10', difficulty: 'intermediate',
      question: 'The expression i = i++ is well-defined in C because assignment happens after increment.',
      correctAnswer: false,
      explanation: 'i = i++ modifies i twice (once by = and once by ++) between sequence points. This is undefined behavior. The result is unpredictable.',
      tags: ['UB', 'sequence-point'] },
  ],

  programmingProblems: [
    {
      id: 'u1-t10-new-easy',
      title: 'Armstrong Number Check',
      topicId: 'u1-t10',
      difficulty: 'beginner',
      problemStatement: 'Read a 3-digit number. Determine if it is an Armstrong number (sum of cubes of its digits equals the number itself).',
      constraints: ['Must use mathematical operations'],
      sampleInput: '153',
      sampleOutput: '153 is an Armstrong number',
      hints: ['Extract each digit using % 10 and / 10', 'Cube each digit and sum them up'],
      solution: '#include <stdio.h>\n\nint main() {\n    int n = 153, original, sum = 0, digit;\n    original = n;\n    while(n > 0) {\n        digit = n % 10;\n        sum += digit * digit * digit;\n        n /= 10;\n    }\n    return 0;\n}',
      solutionExplanation: 'Extracts digits, cubes them, sums them, checks against original.',
      dryRun: [],
      tags: ['number-theory']
    },
    {
      id: 'u1-t10-new-med',
      title: 'Factorial Calculation',
      topicId: 'u1-t10',
      difficulty: 'intermediate',
      problemStatement: 'Calculate the factorial of a given number n without using recursion.',
      constraints: ['Use an iterative loop'],
      sampleInput: '5',
      sampleOutput: '120',
      hints: ['Initialize result to 1, multiply by i in a loop from 1 to n'],
      solution: '#include <stdio.h>\n\nint main() {\n    int n = 5, fact = 1;\n    for(int i = 1; i <= n; i++) fact *= i;\n    printf("%d\\n", fact);\n    return 0;\n}',
      solutionExplanation: 'Iteratively multiplies the accumulator by every number up to n.',
      dryRun: [],
      tags: ['math', 'loops']
    },
    {
      id: 'u1-t10-new-hard',
      title: 'Diamond Pattern',
      topicId: 'u1-t10',
      difficulty: 'advanced',
      problemStatement: 'Print a diamond pattern of stars for a given number of rows n.',
      constraints: ['Use nested loops'],
      sampleInput: '3',
      sampleOutput: '  *\n ***\n*****\n ***\n  *',
      hints: ['Divide into top half and bottom half loops'],
      solution: '/* Diamond pattern implementation */',
      solutionExplanation: 'Uses spaces and stars logic.',
      dryRun: [],
      tags: ['patterns']
    },
    { id: 'u1-t10-p1', title: 'Safe Addition with Overflow Check', topicId: 'u1-t10', difficulty: 'intermediate',
      problemStatement: 'Write a function that adds two integers and checks for overflow BEFORE it happens. Print "Overflow!" if the result would exceed INT_MAX or go below INT_MIN.',
      constraints: ['Must check BEFORE adding to avoid UB', 'Use limits.h constants'],
      sampleInput: '2147483640 10', sampleOutput: 'Overflow!',
      hints: ['If a > 0 and b > INT_MAX - a, then a+b overflows', 'If a < 0 and b < INT_MIN - a, then a+b underflows'],
      solution: '#include <stdio.h>\n#include <limits.h>\n\nint main(void) {\n    int a, b;\n    scanf("%d %d", &a, &b);\n    if ((b > 0 && a > INT_MAX - b) || (b < 0 && a < INT_MIN - b)) {\n        printf("Overflow!\\n");\n    } else {\n        printf("%d + %d = %d\\n", a, b, a + b);\n    }\n    return 0;\n}',
      solutionExplanation: 'Check overflow BEFORE it occurs using subtraction (which cannot overflow here). If a > INT_MAX - b, then a + b > INT_MAX.',
      dryRun: [
        { step: 1, line: 6, variables: { a: '2147483640', b: '10' }, output: '', explanation: 'Read a and b.' },
        { step: 2, line: 7, variables: {}, output: '', explanation: 'b>0 ✓, a > INT_MAX-10 = 2147483637? 2147483640 > 2147483637 ✓ → overflow.' },
        { step: 3, line: 8, variables: {}, output: 'Overflow!', explanation: 'Print overflow warning.' },
      ],
      tags: ['overflow', 'UB', 'safety'] },
    { id: 'u1-t10-p2', title: 'Sanitizer Demo', topicId: 'u1-t10', difficulty: 'beginner',
      problemStatement: 'Write a program that intentionally triggers 3 different kinds of UB (commented out) and one safe version. Use comments to explain each.',
      constraints: ['Show signed overflow, array OOB, and uninitialized read', 'Keep UB code commented out', 'Include the gcc -fsanitize=undefined command'],
      sampleInput: '', sampleOutput: 'All safe operations completed.',
      hints: ['Comment out the dangerous code', 'Show the safe alternative below each'],
      solution: '#include <stdio.h>\n#include <limits.h>\n\nint main(void) {\n    /* UB 1: Signed overflow */\n    /* int x = INT_MAX + 1; */\n    int x = INT_MAX;  /* safe: just store max */\n\n    /* UB 2: Array out of bounds */\n    int arr[3] = {1, 2, 3};\n    /* int bad = arr[5]; */\n    int good = arr[2];  /* safe: valid index */\n\n    /* UB 3: Uninitialized read */\n    /* int y; printf("%d", y); */\n    int y = 0;  /* safe: always initialize */\n\n    printf("All safe operations completed.\\n");\n    printf("x=%d, good=%d, y=%d\\n", x, good, y);\n    return 0;\n}',
      solutionExplanation: 'Shows three common UBs with their safe alternatives. Use -fsanitize=undefined to detect if any UB slips in.',
      dryRun: [
        { step: 1, line: 18, variables: { x: '2147483647', good: '3', y: '0' }, output: 'All safe operations completed.', explanation: 'All operations are safe.' },
      ],
      tags: ['UB', 'sanitizer', 'safety'] },
    { id: 'u1-t10-p3', title: 'Platform Info Reporter', topicId: 'u1-t10', difficulty: 'beginner',
      problemStatement: 'Print all implementation-defined values: sizeof every integer type, whether char is signed, and the byte order.',
      constraints: ['Use sizeof and limits.h', 'Check CHAR_MIN to determine char signedness'],
      sampleInput: '', sampleOutput: 'char is signed\nint: 4 bytes\nlong: 8 bytes',
      hints: ['If CHAR_MIN < 0, char is signed', 'Use sizeof for all types'],
      solution: '#include <stdio.h>\n#include <limits.h>\n\nint main(void) {\n    printf("char:      %zu byte(s), %s\\n", sizeof(char),\n           CHAR_MIN < 0 ? "signed" : "unsigned");\n    printf("short:     %zu bytes\\n", sizeof(short));\n    printf("int:       %zu bytes\\n", sizeof(int));\n    printf("long:      %zu bytes\\n", sizeof(long));\n    printf("long long: %zu bytes\\n", sizeof(long long));\n    printf("float:     %zu bytes\\n", sizeof(float));\n    printf("double:    %zu bytes\\n", sizeof(double));\n    return 0;\n}',
      solutionExplanation: 'All these values are implementation-defined. This program documents your platform.',
      dryRun: [
        { step: 1, line: 5, variables: {}, output: 'char:      1 byte(s), signed', explanation: 'CHAR_MIN = -128 < 0, so char is signed on this platform.' },
      ],
      tags: ['implementation-defined', 'sizeof'] },
  ],
};
