import type { Topic } from '../../../../types';

export const controlStructures: Topic = {
  id: 'u1-t8',
  unitId: 'unit-1',
  title: 'Control Structures',
  slug: 'control-structures',
  description: `Control structures transform a program from a linear sequence of statements into a dynamic system capable of making decisions and repeating actions based on runtime conditions. Without control flow constructs, a program would execute every instruction exactly once, in order, from top to bottom — useful only for the simplest batch computations. Branching constructs (if, else-if, else, switch) allow the program to evaluate conditions and selectively execute different code paths, while looping constructs (for, while, do-while) allow the program to execute a block of code repeatedly until a termination condition is met.

At the machine-code level, branching compiles to conditional jump instructions (e.g., JE, JNE on x86) that redirect the CPU's instruction pointer based on comparison results stored in the flags register. Loops compile to backward jumps that return execution to the top of the loop body, combined with a conditional check that eventually breaks the cycle. The switch statement may be compiled into a jump table — an array of instruction addresses indexed by the case value — enabling O(1) dispatch rather than sequential condition evaluation. Understanding how these high-level constructs map to low-level machine behaviour provides insight into performance characteristics and helps diagnose control-flow bugs through assembly-level debugging.

Mastering control flow requires not only syntactic knowledge but also disciplined reasoning about edge cases, loop invariants, and termination conditions. Common pitfalls include dangling else ambiguity (where an else binds to the nearest unmatched if), switch fall-through bugs (where a missing break causes execution to cascade into subsequent case labels), off-by-one errors in loop bounds, and infinite loops caused by forgetting to update the loop variable or by writing a condition that is tautologically true.`,
  difficulty: 'beginner',
  prerequisites: ['u1-t7'],
  estimatedMinutes: 70,
  subtopics: [
    {
      id: 'u1-t8-s1', title: 'Branching — if, else, switch', slug: 'branching',
      description: `Branching statements allow a program to select between alternative execution paths based on runtime conditions, transforming a linear sequence of instructions into a decision tree. The if statement is the fundamental branching construct: it evaluates a condition expression and executes the associated block of code only when the expression evaluates to a non-zero value. In C, there is no dedicated boolean type in the original standard (C89/C90) — the language uses plain integers where zero represents false and any non-zero value represents true. This means that if (5) is legal and always true, if (0) is always false, and if (ptr) is the idiomatic way to check whether a pointer is non-NULL.

The else-if ladder extends the basic if to handle multiple mutually exclusive conditions. C evaluates each condition sequentially from top to bottom and executes the body of the first condition that evaluates to true, then skips all remaining else-if and else branches entirely. This top-to-bottom evaluation order means the arrangement of conditions is a design decision with correctness implications: if you test marks >= 60 before marks >= 90, a student scoring 95 matches the first condition and receives the lower grade, never reaching the higher threshold. Structuring conditions from most specific to most general prevents this class of logic error.

The switch statement provides a structurally cleaner alternative to long else-if chains when comparing a single integer or character expression against a set of compile-time constant values. On many architectures, the compiler translates switch into a jump table — an array of instruction addresses indexed by the case value — enabling O(1) dispatch rather than the O(n) sequential evaluation of an else-if chain. However, switch introduces a critical pitfall: fall-through. After executing a matching case label, execution continues into the next case unless explicitly terminated by a break statement. This behavior is sometimes intentional (grouping multiple case labels that share the same handler), but far more often it is an accidental omission that introduces subtle bugs.`,
      keyPoints: [
        'The if statement evaluates its condition (any expression) and executes the body only when the result is non-zero. In C, remember that there is no dedicated boolean type — zero is false, and literally any other value (1, -5, 42, even 0.0001) is true. This means if (5) is legal and always true, and if (0) is legal and always false.',
        'The else-if ladder lets you test multiple conditions in sequence. C evaluates each condition from top to bottom and executes the body of the first one that is true, then skips all remaining else-if and else blocks. This means the order of your conditions matters: if you check marks >= 60 before marks >= 90, every student scoring 90+ would get the grade for 60+ instead.',
        'The switch statement provides a cleaner alternative to long else-if chains when you are comparing a single integer or char expression against specific constant values. Instead of evaluating conditions one by one, switch jumps directly to the matching case label. However, switch has a critical quirk: after executing a case, execution does not automatically stop — it "falls through" into the next case unless you explicitly write break.',
        'Fall-through is switch\'s most notorious feature. Without a break statement at the end of each case, execution continues into the next case regardless of whether it matches. This is sometimes intentional (grouping multiple case labels that share the same action), but is far more often an accidental bug where the programmer simply forgot break. Always include break unless you have a deliberate reason to fall through, and comment your intention when you do.',
        'The default case in a switch handles any value that does not match any case label. Always include default as a safety net — it catches unexpected values and prevents silent failures. Think of it as the else at the end of an if-else ladder.',
        'The "dangling else" problem occurs with nested if statements that are not fully braced. In C, an else always binds to the nearest preceding if that does not already have an else. This can create confusing ambiguity: if (a > 0) if (b > 0) printf("both"); else printf("which if does this else belong to?"); The answer is the inner if, not the outer one. The solution is simple: always use braces { } around if and else bodies, even for single statements.',
      ],
      codeExamples: [
        {
          id: 'u1-t8-s1-ex1', title: 'if / else-if / else Ladder', language: 'c',
          code: '#include <stdio.h>\n\nint main(void) {\n    int marks;\n    printf("Enter marks: ");\n    scanf("%d", &marks);\n    if (marks >= 90) {\n        printf("Grade: S\\n");\n    } else if (marks >= 80) {\n        printf("Grade: A\\n");\n    } else if (marks >= 70) {\n        printf("Grade: B\\n");\n    } else if (marks >= 60) {\n        printf("Grade: C\\n");\n    } else {\n        printf("Grade: F\\n");\n    }\n    return 0;\n}',
          explanation: 'This grading program demonstrates the else-if ladder pattern. The key insight is that conditions are tested sequentially from top to bottom, and only the first true condition executes its body. Once a match is found, all remaining else-if and else blocks are skipped entirely. This means the order of conditions is critical: since each condition implicitly assumes all previous conditions were false, you can write marks >= 80 instead of marks >= 80 && marks < 90 — the else-if structure handles the "less than 90" part automatically because we would not reach this check if marks were 90 or above.',
          expectedOutput: 'Enter marks: Grade: A',
          lineBreakdown: [
            { lineNumber: 7, code: '    if (marks >= 90)', explanation: 'First check. If marks is 85, this is false → move to next.' },
            { lineNumber: 9, code: '    } else if (marks >= 80)', explanation: 'Only checked if previous was false. 85 >= 80 → true → prints Grade: A.' },
          ],
          relatedTopicIds: ['u1-t7'],
        },
        {
          id: 'u1-t8-s1-ex2', title: 'switch with Fall-Through Trap', language: 'c',
          code: '#include <stdio.h>\n\nint main(void) {\n    int day = 3;\n    switch (day) {\n        case 1: printf("Monday\\n"); break;\n        case 2: printf("Tuesday\\n"); break;\n        case 3: printf("Wednesday\\n"); break;\n        case 4: printf("Thursday\\n"); break;\n        case 5: printf("Friday\\n"); break;\n        default: printf("Weekend\\n"); break;\n    }\n    return 0;\n}',
          explanation: 'The switch statement is C\'s answer to "I have one variable and I need to compare it against many specific values." Instead of writing a long chain of if (day == 1) else if (day == 2) else if..., switch lets you list the values as case labels and jump directly to the matching one. The critical rule is that every case must end with break, or execution will "fall through" into the next case. Think of switch as a building elevator: it takes you to the right floor (case), but once you are there, you need to explicitly step off (break), otherwise you ride through every floor below.',
          expectedOutput: 'Wednesday',
          lineBreakdown: [
            { lineNumber: 5, code: '    switch (day)', explanation: 'Evaluates day (3) and jumps to case 3.' },
            { lineNumber: 8, code: '    case 3: ... break;', explanation: 'Matches! Prints Wednesday. break exits the switch.' },
          ],
          relatedTopicIds: [],
        },
        {
          id: 'u1-t8-s1-ex3', title: 'Intentional Fall-Through', language: 'c',
          code: '#include <stdio.h>\n\nint main(void) {\n    char grade = \'B\';\n    switch (grade) {\n        case \'A\':\n        case \'B\':\n            printf("Excellent\\n");\n            break;\n        case \'C\':\n        case \'D\':\n            printf("Average\\n");\n            break;\n        default:\n            printf("Fail\\n");\n            break;\n    }\n    return 0;\n}',
          explanation: 'This example shows the one situation where fall-through is not a bug but a feature. Cases A and B both need to print "Excellent," so case A has no code and no break — it immediately falls through into case B\'s code. Similarly, cases C and D share the "Average" action. This is the only legitimate use of fall-through: grouping multiple case labels that perform the identical action. In all other situations, a missing break is almost certainly a bug. Many coding standards require a comment like /* fall-through */ when you intentionally omit break, to signal that it was not forgotten.',
          expectedOutput: 'Excellent',
          lineBreakdown: [
            { lineNumber: 6, code: '    case \'A\':', explanation: 'No break — falls through to case B code. A and B share the same action.' },
          ],
          relatedTopicIds: [],
        },
      ],
      commonMistakes: [
        { id: 'u1-t8-s1-cm1', title: 'Missing break in switch',
          wrongCode: 'switch (x) {\n    case 1: printf("One\\n");\n    case 2: printf("Two\\n");\n    case 3: printf("Three\\n");\n}',
          correctCode: 'switch (x) {\n    case 1: printf("One\\n"); break;\n    case 2: printf("Two\\n"); break;\n    case 3: printf("Three\\n"); break;\n}',
          explanation: 'This is one of the most common switch bugs, and it is particularly insidious because the code looks perfectly reasonable — each case has a printf, and everything is neatly formatted. But without break at the end of each case, execution does not stop after printing "One." It continues directly into case 2, printing "Two," then into case 3, printing "Three." The output for x=1 would be "One Two Three" instead of just "One." This bug is hard to catch during code review because the indentation makes it look like each case is independent.',
          consequence: 'For x=1, the program prints "One Two Three" instead of just "One". Every case from the matching one onward executes. This produces obviously wrong output but compiles without any warnings, making it a pure logic bug.' },
        { id: 'u1-t8-s1-cm2', title: 'Using == with floating point in conditions',
          wrongCode: 'float x = 0.1f + 0.2f;\nif (x == 0.3f) { printf("Equal\\n"); }',
          correctCode: 'float x = 0.1f + 0.2f;\nif (fabsf(x - 0.3f) < 1e-6f) { printf("Equal\\n"); }',
          explanation: 'This is the floating-point comparison trap applied to control flow. Students write mathematically correct conditions like x == 0.3f expecting them to work, because 0.1 + 0.2 does equal 0.3 in mathematics. But in the computer\'s binary floating-point representation, 0.1 and 0.2 cannot be stored exactly, so their sum is something like 0.30000001192... instead of exactly 0.3. The == comparison fails because these two representations differ in the last few bits. The correct approach is to check whether the two values are "close enough" by comparing their absolute difference against a small tolerance (epsilon).',
          consequence: 'The condition evaluates to false even when the values are mathematically equal, causing the program to take the wrong branch. This bug is especially confusing because printing the values with default precision shows "0.3" and "0.3" — you have to print with 20+ decimal places to see the hidden discrepancy.' },
      ],
      interviewCallouts: [
        { id: 'u1-t8-s1-ic1', title: 'What is switch fall-through?',
          content: 'Fall-through is one of the most commonly tested concepts in vivas and interviews because it reveals whether you understand how switch actually works at the machine level. Without break, execution simply continues to the next line of code, ignoring case labels (which are just jump targets, not barriers). A strong answer explains that fall-through is a deliberate design choice in C that enables grouping cases (like case \'A\': case \'B\': /* shared code */), but that it is almost always a bug when unintentional. Mention that modern compilers like gcc with -Wimplicit-fallthrough can warn about missing breaks, and that many coding standards require explicit /* fall-through */ comments for intentional cases.',
          relatedTopicIds: [], frequency: 'common' },
      ],
      checkpoints: [
        { id: 'u1-t8-s1-cp1', title: 'Branching Mastery', description: 'Verify branching understanding.',
          criteria: ['Write a grade calculator with if-else ladder', 'Write a menu using switch', 'Explain fall-through'], topicId: 'u1-t8' },
      ],
      revisionCards: [
        { id: 'u1-t8-s1-rc1', front: 'What happens in switch without break?', back: 'Fall-through: execution continues into the next case label, executing its code too. This continues until a break or the end of switch.', topicId: 'u1-t8', tags: ['switch', 'fall-through'] },
        { id: 'u1-t8-s1-rc2', front: 'When should you use switch vs if-else?', back: 'switch: when comparing one variable against multiple discrete values (menu choices, day numbers). if-else: when conditions involve ranges, multiple variables, or complex expressions.', topicId: 'u1-t8', tags: ['switch', 'if-else'] },
        { id: 'u1-t8-s1-rc3', front: 'What is the dangling else problem?', back: 'else binds to the nearest unmatched if. Without braces, nested if-else can be ambiguous. Always use braces {} to make your intent clear.', topicId: 'u1-t8', tags: ['dangling-else'] },
      ],
    },
    {
      id: 'u1-t8-s2', title: 'Loops — for, while, do-while', slug: 'loops',
      description: `Loops are the mechanism by which a program executes a block of code repeatedly, either a predetermined number of times or until a runtime condition changes. The for loop consolidates the three components of iteration \u2014 initialisation, condition checking, and update \u2014 into a single syntactic structure: for (init; condition; update) { body }. The execution flow is strictly defined: init executes exactly once, then the cycle of condition-check \u2192 body execution \u2192 update repeats until the condition evaluates to false. The for loop is the natural choice when the number of iterations is known in advance (\"print the first 100 primes\") or when iterating through array indices.

The while loop separates the condition from the body: while (condition) { body }. The condition is evaluated before each iteration, meaning the body may execute zero times if the condition is initially false. This pre-check semantics makes while ideal for situations where the iteration count cannot be predicted: reading input until end-of-file, polling a hardware register, or searching an unsorted dataset. The do-while loop inverts this relationship: do { body } while (condition); \u2014 it executes the body first and checks the condition afterward, guaranteeing at least one execution. This post-check semantics is specifically designed for input validation scenarios where you must perform an action (prompt the user) before you can evaluate whether to repeat it.

The break and continue statements provide fine-grained control over loop execution. break immediately terminates the innermost enclosing loop and transfers control to the first statement following it. continue skips the remainder of the current iteration and jumps to the loop's update step (in for loops) or condition check (in while/do-while loops). Infinite loops \u2014 for(;;) or while(1) \u2014 are loops that never terminate on their own and are exited exclusively via break when an internal condition is met. Far from being errors, they are the standard architecture for event loops, server main loops, and interactive menu systems. Nested loops multiply their iteration counts: an outer loop running n times containing an inner loop running m times produces n \u00d7 m total body executions, giving rise to O(n\u00b2) time complexity \u2014 the programmer's first encounter with algorithmic performance analysis.`,
      keyPoints: [
        'The for loop packs initialization, condition checking, and update into a single line: for (init; condition; update) { body }. Execution flows as: init once → check condition → if true, execute body → execute update → check condition again → repeat. All three parts are optional: omitting the condition creates an infinite loop for(;;), and you can put complex expressions in any part. The for loop is the natural choice when you know the iteration count in advance.',
        'The while loop is simpler in structure: while (condition) { body }. It checks the condition before each iteration, which means the body might execute zero times if the condition is initially false. This makes while ideal for situations where you cannot predict the number of iterations: reading input until end-of-file, searching for a value in unsorted data, or waiting for a condition to change.',
        'The do-while loop has a subtle but important distinction: do { body } while (condition);. It executes the body first and checks the condition afterward, guaranteeing at least one execution. This is perfect for input validation: you want to ask the user for input at least once, then repeat only if the input was invalid. Note the semicolon after the while condition — forgetting it is a syntax error, and it is the only loop that requires one.',
        'The break statement immediately exits the innermost loop it is inside, jumping to the first statement after the loop. The continue statement skips the rest of the current iteration and jumps to the loop\'s condition check (for while/do-while) or update step (for for loops). Both are tools for early exit and selective skipping, but overusing them can make loop logic harder to follow.',
        'Infinite loops (for(;;) or while(1)) are loops that never terminate on their own. They are not bugs when used intentionally — they are common in event loops, server programs, and interactive menus. You exit them with break when a specific condition is met inside the body. However, accidentally creating an infinite loop (by forgetting to update the loop variable or writing a condition that is always true) is one of the most common beginner mistakes.',
        'Nested loops execute the inner loop completely for each iteration of the outer loop. If the outer loop runs n times and the inner loop runs m times, the body executes n × m times total. This is your first encounter with algorithmic complexity: nested loops often produce O(n²) performance, which can be problematically slow for large inputs. Understanding this cost is the beginning of computational thinking.',
      ],
      codeExamples: [
        {
          id: 'u1-t8-s2-ex1', title: 'Three Loop Types Compared', language: 'c',
          code: '#include <stdio.h>\n\nint main(void) {\n    /* for: when you know how many iterations */\n    printf("for:      ");\n    for (int i = 1; i <= 5; i++) {\n        printf("%d ", i);\n    }\n    printf("\\n");\n\n    /* while: check before each iteration */\n    printf("while:    ");\n    int j = 1;\n    while (j <= 5) {\n        printf("%d ", j);\n        j++;\n    }\n    printf("\\n");\n\n    /* do-while: always runs at least once */\n    printf("do-while: ");\n    int k = 1;\n    do {\n        printf("%d ", k);\n        k++;\n    } while (k <= 5);\n    printf("\\n");\n    return 0;\n}',
          explanation: 'This example puts all three loop types side by side doing the same task, so you can see how each one structures the same logic differently. The for loop is the most compact because it packages init, condition, and update into a single line. The while loop separates these concerns, requiring you to initialize before the loop and update inside the body. The do-while loop is similar to while but checks the condition after the body, which becomes important when the initial state might not satisfy the condition. In this example, all three produce identical output, but in real programs, the choice of loop type communicates your intent to the reader.',
          expectedOutput: 'for:      1 2 3 4 5\nwhile:    1 2 3 4 5\ndo-while: 1 2 3 4 5',
          lineBreakdown: [
            { lineNumber: 6, code: '    for (int i = 1; i <= 5; i++)', explanation: 'init: i=1. condition: i<=5. update: i++. All in one line.' },
            { lineNumber: 14, code: '    while (j <= 5)', explanation: 'Condition checked before body. If j>5 initially, body never runs.' },
            { lineNumber: 23, code: '    do { ... } while (k <= 5);', explanation: 'Body runs first, then condition. Note semicolon after while().' },
          ],
          relatedTopicIds: [],
        },
        {
          id: 'u1-t8-s2-ex2', title: 'break and continue', language: 'c',
          code: '#include <stdio.h>\n\nint main(void) {\n    /* break: exit loop early */\n    for (int i = 1; i <= 10; i++) {\n        if (i == 6) break;\n        printf("%d ", i);\n    }\n    printf("\\n");\n\n    /* continue: skip to next iteration */\n    for (int i = 1; i <= 10; i++) {\n        if (i % 2 == 0) continue;  /* skip even numbers */\n        printf("%d ", i);\n    }\n    printf("\\n");\n    return 0;\n}',
          explanation: 'break and continue give you fine-grained control over loop execution. The break statement is like an emergency exit — the moment it executes, the loop is over, and the program continues with the first statement after the loop. The continue statement is like a "skip" button — it abandons the rest of the current iteration and jumps back to the loop\'s condition check (or update step in a for loop). Both are useful, but both can make code harder to read if overused. A loop with many breaks and continues scattered throughout is a sign that the logic should be restructured.',
          expectedOutput: '1 2 3 4 5\n1 3 5 7 9',
          lineBreakdown: [
            { lineNumber: 6, code: '    if (i == 6) break;', explanation: 'When i reaches 6, exit the loop. Only 1-5 are printed.' },
            { lineNumber: 13, code: '    if (i % 2 == 0) continue;', explanation: 'Even numbers skip the printf and go to next iteration.' },
          ],
          relatedTopicIds: [],
        },
        {
          id: 'u1-t8-s2-ex3', title: 'Nested Loops — Multiplication Table', language: 'c',
          code: '#include <stdio.h>\n\nint main(void) {\n    for (int i = 1; i <= 5; i++) {\n        for (int j = 1; j <= 5; j++) {\n            printf("%4d", i * j);\n        }\n        printf("\\n");\n    }\n    return 0;\n}',
          explanation: 'Nested loops are your first taste of how algorithms scale. The outer loop runs 5 times, and for each of those iterations, the inner loop runs 5 times, producing 25 total iterations. If you changed both loops to run n times, you would have n² iterations — this is called O(n²) complexity. For n=10, that is 100 iterations (fast). For n=1000, that is 1,000,000 iterations (still fast). For n=1,000,000, that is 1,000,000,000,000 iterations (impossibly slow). Understanding this exponential scaling is why nested loops are both powerful and dangerous.',
          expectedOutput: '   1   2   3   4   5\n   2   4   6   8  10\n   3   6   9  12  15\n   4   8  12  16  20\n   5  10  15  20  25',
          lineBreakdown: [
            { lineNumber: 4, code: '    for (int i = 1; ...)', explanation: 'Outer loop: controls which row (1-5).' },
            { lineNumber: 5, code: '    for (int j = 1; ...)', explanation: 'Inner loop: prints columns for current row. Runs 5 times per i.' },
            { lineNumber: 6, code: '    printf("%4d", i * j);', explanation: '%4d pads to 4 chars wide for alignment.' },
          ],
          relatedTopicIds: [],
        },
      ],
      commonMistakes: [
        { id: 'u1-t8-s2-cm1', title: 'Off-by-one error in loops',
          wrongCode: 'for (int i = 1; i < 5; i++) { /* runs 4 times, not 5 */ }',
          correctCode: 'for (int i = 1; i <= 5; i++) { /* runs 5 times */ }',
          explanation: 'Off-by-one errors are the most common bugs in programming, period. They happen because the boundary between < and <= (or starting from 0 vs 1) determines whether your loop executes n times or n-1 times. The fix is to always trace through the boundary values mentally: if i starts at 1 and the condition is i < 5, then i takes values 1, 2, 3, 4 (four iterations). If the condition is i <= 5, then i takes values 1, 2, 3, 4, 5 (five iterations). Developing the habit of checking both the first and last iteration prevents this class of bugs entirely.',
          consequence: 'The loop processes one fewer (or one more) element than intended. This can mean missing the last item in a list, printing one too few rows in a pattern, or computing an incorrect sum. The error is always exactly 1, which can be hard to notice in casual testing.' },
        { id: 'u1-t8-s2-cm2', title: 'Semicolon after for() creating empty loop',
          wrongCode: 'for (int i = 0; i < 5; i++); /* empty loop! */\n    printf("%d\\n", i);  /* runs once, i=5 */',
          correctCode: 'for (int i = 0; i < 5; i++) {\n    printf("%d\\n", i);\n}',
          explanation: 'This is one of the sneakiest bugs in C because it is nearly invisible to the human eye. A semicolon immediately after for() creates an empty statement as the loop body. The loop runs all its iterations, doing nothing each time, and then the indented code below it executes once (not inside the loop at all). The indentation is misleading — it looks like the printf is inside the loop, but C ignores whitespace and indentation. Only the structure of semicolons and braces determines what is inside the loop. The solution is to always use braces { } for loop bodies, even for single statements.',
          consequence: 'The loop runs all its iterations with an empty body (doing nothing). The code that looks like the loop body runs exactly once after the loop finishes. The output is completely wrong, but there are no compiler errors or warnings. This bug can waste hours of debugging time.' },
      ],
      interviewCallouts: [
        { id: 'u1-t8-s2-ic1', title: 'When should you use do-while over while?',
          content: 'The key insight that impresses interviewers is explaining the practical scenario. The do-while loop is specifically designed for situations where you need to execute the body at least once before checking whether to continue. The canonical example is input validation: do { printf("Enter a positive number: "); scanf("%d", &n); } while (n <= 0); This guarantees the prompt appears at least once. With a while loop, you would need to either duplicate the scanf before the loop or use a flag variable, both of which are uglier solutions. Another common use is menu-driven programs: display the menu at least once, then loop based on the user\'s choice.',
          relatedTopicIds: [], frequency: 'occasional' },
      ],
      checkpoints: [
        { id: 'u1-t8-s2-cp1', title: 'Loops Mastery', description: 'Verify loop understanding.',
          criteria: ['Write a for loop counting 1 to 10', 'Write a while loop for input validation', 'Use break and continue correctly', 'Write a nested loop pattern (triangle of stars)'], topicId: 'u1-t8' },
      ],
      revisionCards: [
        { id: 'u1-t8-s2-rc1', front: 'What is the difference between while and do-while?', back: 'while checks condition BEFORE the body (may run 0 times). do-while executes the body FIRST, then checks (always runs at least once).', topicId: 'u1-t8', tags: ['while', 'do-while'] },
        { id: 'u1-t8-s2-rc2', front: 'What does break do inside a nested loop?', back: 'break only exits the INNERMOST loop. The outer loop continues. To exit all loops, use a flag variable or goto (rarely).', topicId: 'u1-t8', tags: ['break', 'nested'] },
        { id: 'u1-t8-s2-rc3', front: 'What happens if you put ; after for()?', back: 'The semicolon becomes the loop body (empty statement). The loop runs all iterations doing nothing. The code after it runs once. Very common silent bug.', topicId: 'u1-t8', tags: ['semicolon-bug'] },
      ],
    },
  ],

  theoryQuestions: [
    { id: 'u1-t8-q1', type: 'predict-output', topicId: 'u1-t8', difficulty: 'beginner',
      question: 'What does this print?',
      code: '#include <stdio.h>\nint main(void) {\n    for (int i = 0; i < 5; i++) {\n        if (i == 3) continue;\n        printf("%d ", i);\n    }\n    return 0;\n}',
      correctAnswer: '0 1 2 4',
      explanation: 'continue skips the rest of the body for i=3. So 3 is not printed. All other values (0,1,2,4) are printed.',
      tags: ['continue'] },
    { id: 'u1-t8-q2', type: 'predict-output', topicId: 'u1-t8', difficulty: 'beginner',
      question: 'What does this print?',
      code: '#include <stdio.h>\nint main(void) {\n    int x = 2;\n    switch (x) {\n        case 1: printf("A ");\n        case 2: printf("B ");\n        case 3: printf("C ");\n        default: printf("D ");\n    }\n    return 0;\n}',
      correctAnswer: 'B C D',
      explanation: 'x matches case 2. No break anywhere, so execution falls through case 3 and default. Output: B C D.',
      tags: ['switch', 'fall-through'] },
    { id: 'u1-t8-q3', type: 'spot-bug', topicId: 'u1-t8', difficulty: 'beginner',
      question: 'Find the bug:',
      code: '#include <stdio.h>\nint main(void) {\n    int sum = 0;\n    for (int i = 1; i <= 100; i++);\n        sum += i;\n    printf("Sum = %d\\n", sum);\n    return 0;\n}',
      correctAnswer: 'Semicolon after for() on line 4 makes the loop body empty. sum += i runs once after the loop, and i is out of scope.',
      explanation: 'The ; after for() creates an empty loop body. The loop runs 100 times doing nothing. Then sum += i tries to use i which is out of scope (compile error). Even if i were in scope, it would add 101 once.',
      tags: ['semicolon-bug'] },
    { id: 'u1-t8-q4', type: 'mcq', topicId: 'u1-t8', difficulty: 'beginner',
      question: 'Which loop guarantees at least one execution of the body?',
      options: ['for', 'while', 'do-while', 'All of them'],
      correctAnswer: 'do-while',
      explanation: 'do-while executes the body first, then checks the condition. Even if the condition is initially false, the body runs once. for and while check before the body.',
      tags: ['do-while'] },
    { id: 'u1-t8-q5', type: 'true-false', topicId: 'u1-t8', difficulty: 'beginner',
      question: 'break inside a nested loop exits ALL enclosing loops.',
      correctAnswer: false,
      explanation: 'break only exits the innermost enclosing loop. The outer loop continues normally. To exit all loops, use a flag variable checked by each loop.',
      tags: ['break', 'nested'] },
  ],

  programmingProblems: [
    {
      id: 'u1-t8-new-easy',
      title: 'Armstrong Number Check',
      topicId: 'u1-t8',
      difficulty: 'beginner',
      problemStatement: 'Read a 3-digit number. Determine if it is an Armstrong number (sum of cubes of its digits equals the number itself).',
      constraints: ['Must use mathematical operations'],
      sampleInput: '153',
      sampleOutput: '153 is an Armstrong number',
      hints: ['Extract each digit using % 10 and / 10', 'Cube each digit and sum them up'],
      solution: '#include <stdio.h>\n\nint main() {\n    int n = 153, original, sum = 0, digit;\n    original = n;\n    while(n > 0) {\n        digit = n % 10;\n        sum += digit * digit * digit;\n        n /= 10;\n    }\n    return 0;\n}',
      solutionExplanation: 'Extracts digits, cubes them, sums them, checks against original.',
      dryRun: [],
      tags: ['number-theory']
    },
    {
      id: 'u1-t8-new-med',
      title: 'Factorial Calculation',
      topicId: 'u1-t8',
      difficulty: 'intermediate',
      problemStatement: 'Calculate the factorial of a given number n without using recursion.',
      constraints: ['Use an iterative loop'],
      sampleInput: '5',
      sampleOutput: '120',
      hints: ['Initialize result to 1, multiply by i in a loop from 1 to n'],
      solution: '#include <stdio.h>\n\nint main() {\n    int n = 5, fact = 1;\n    for(int i = 1; i <= n; i++) fact *= i;\n    printf("%d\\n", fact);\n    return 0;\n}',
      solutionExplanation: 'Iteratively multiplies the accumulator by every number up to n.',
      dryRun: [],
      tags: ['math', 'loops']
    },
    {
      id: 'u1-t8-new-hard',
      title: 'Diamond Pattern',
      topicId: 'u1-t8',
      difficulty: 'advanced',
      problemStatement: 'Print a diamond pattern of stars for a given number of rows n.',
      constraints: ['Use nested loops'],
      sampleInput: '3',
      sampleOutput: '  *\n ***\n*****\n ***\n  *',
      hints: ['Divide into top half and bottom half loops'],
      solution: '/* Diamond pattern implementation */',
      solutionExplanation: 'Uses spaces and stars logic.',
      dryRun: [],
      tags: ['patterns']
    },
    { id: 'u1-t8-p1', title: 'Simple Calculator (switch)', topicId: 'u1-t8', difficulty: 'beginner',
      problemStatement: 'Read two numbers and an operator (+, -, *, /) from the user. Perform the operation using switch and print the result. Handle division by zero.',
      constraints: ['Use switch for operator selection', 'Handle division by zero'], sampleInput: '10 + 5', sampleOutput: '10 + 5 = 15',
      hints: ['Read operator as char', 'Use case \'+\': etc.', 'Check denominator before dividing'],
      solution: '#include <stdio.h>\n\nint main(void) {\n    double a, b;\n    char op;\n    scanf("%lf %c %lf", &a, &op, &b);\n    switch (op) {\n        case \'+\': printf("%.2f %c %.2f = %.2f\\n", a, op, b, a + b); break;\n        case \'-\': printf("%.2f %c %.2f = %.2f\\n", a, op, b, a - b); break;\n        case \'*\': printf("%.2f %c %.2f = %.2f\\n", a, op, b, a * b); break;\n        case \'/\':\n            if (b == 0) printf("Error: division by zero\\n");\n            else printf("%.2f %c %.2f = %.2f\\n", a, op, b, a / b);\n            break;\n        default: printf("Unknown operator\\n"); break;\n    }\n    return 0;\n}',
      solutionExplanation: 'switch on char operator. Each case performs the operation. Division case checks for zero. default handles invalid operators.',
      dryRun: [
        { step: 1, line: 6, variables: { a: '10', op: '+', b: '5' }, output: '', explanation: 'Read: a=10, op=+, b=5.' },
        { step: 2, line: 8, variables: {}, output: '10.00 + 5.00 = 15.00', explanation: 'case +: a+b = 15. Print and break.' },
      ],
      tags: ['switch', 'calculator'] },
    { id: 'u1-t8-p2', title: 'Print Right Triangle Pattern', topicId: 'u1-t8', difficulty: 'beginner',
      problemStatement: 'Read n from user. Print a right triangle of * with n rows.',
      constraints: ['Use nested loops', 'Row i has i stars'], sampleInput: '5', sampleOutput: '*\n**\n***\n****\n*****',
      hints: ['Outer loop: rows 1 to n', 'Inner loop: print i stars for row i'],
      solution: '#include <stdio.h>\n\nint main(void) {\n    int n;\n    scanf("%d", &n);\n    for (int i = 1; i <= n; i++) {\n        for (int j = 1; j <= i; j++) {\n            printf("*");\n        }\n        printf("\\n");\n    }\n    return 0;\n}',
      solutionExplanation: 'Outer loop controls rows (1 to n). Inner loop prints i stars for row i. After inner loop, newline moves to next row.',
      dryRun: [
        { step: 1, line: 6, variables: { i: '1' }, output: '*\\n', explanation: 'Row 1: inner loop runs 1 time → 1 star.' },
        { step: 2, line: 6, variables: { i: '2' }, output: '**\\n', explanation: 'Row 2: inner loop runs 2 times → 2 stars.' },
        { step: 3, line: 6, variables: { i: '5' }, output: '*****\\n', explanation: 'Row 5: inner loop runs 5 times → 5 stars.' },
      ],
      tags: ['nested-loops', 'pattern'] },
    { id: 'u1-t8-p3', title: 'Sum of Digits', topicId: 'u1-t8', difficulty: 'intermediate',
      problemStatement: 'Read a positive integer and find the sum of its digits. Example: 1234 → 1+2+3+4 = 10.',
      constraints: ['Use a while loop', 'Use % 10 to extract last digit', 'Use / 10 to remove last digit'],
      sampleInput: '1234', sampleOutput: 'Sum of digits = 10',
      hints: ['digit = n % 10 gives last digit', 'n = n / 10 removes last digit', 'Loop while n > 0'],
      solution: '#include <stdio.h>\n\nint main(void) {\n    int n, sum = 0;\n    scanf("%d", &n);\n    int original = n;\n    while (n > 0) {\n        sum += n % 10;\n        n /= 10;\n    }\n    printf("Sum of digits = %d\\n", sum);\n    return 0;\n}',
      solutionExplanation: 'n%10 extracts the last digit. n/=10 removes it. Repeat until n is 0. This is a classic loop-with-arithmetic pattern.',
      dryRun: [
        { step: 1, line: 7, variables: { n: '1234', sum: '0' }, output: '', explanation: '1234%10=4. sum=4. n=123.' },
        { step: 2, line: 7, variables: { n: '123', sum: '4' }, output: '', explanation: '123%10=3. sum=7. n=12.' },
        { step: 3, line: 7, variables: { n: '12', sum: '7' }, output: '', explanation: '12%10=2. sum=9. n=1.' },
        { step: 4, line: 7, variables: { n: '1', sum: '9' }, output: '', explanation: '1%10=1. sum=10. n=0. Loop ends.' },
      ],
      tags: ['while', 'digit-extraction'] },
  ],
};
