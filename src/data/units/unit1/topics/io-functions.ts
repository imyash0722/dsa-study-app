import type { Topic } from '../../../../types';

export const ioFunctions: Topic = {
  id: 'u1-t9',
  unitId: 'unit-1',
  title: 'Input/Output Functions',
  slug: 'io-functions',
  description: `The printf and scanf functions are the primary formatted I/O interface in C, and together they illustrate one of the language's most distinctive design patterns: the format-string protocol, in which a template string containing embedded type specifiers drives the conversion between human-readable text and binary data. printf converts program data (integers, floats, strings) into formatted text written to standard output, while scanf performs the inverse operation, parsing text from standard input and converting it into binary values stored in program variables. Both functions rely on variadic argument passing (accepting a variable number of arguments) and format specifiers (%d, %f, %s, %c) to determine how each argument should be interpreted.

Understanding these functions deeply — not merely their syntax but their internal mechanics — prevents an entire category of bugs. printf extracts variadic arguments from the stack as raw bytes and interprets them according to the format specifier; a mismatch between specifier and argument type (e.g., passing a float where %d expects an int) causes printf to read the wrong number of bytes and interpret them under the wrong encoding, producing garbage output or undefined behavior. scanf introduces an additional hazard: because C is pass-by-value, you must pass the address of each target variable (&x) so that scanf can write the converted value to the correct memory location. Forgetting the address-of operator causes scanf to interpret the variable's current value as a memory address and write to an arbitrary location — a classic undefined-behavior bug that typically manifests as a segmentation fault.

Modern compilers with -Wall or -Wformat enabled can detect many format-string mismatches at compile time, but the programmer bears ultimate responsibility for ensuring type agreement between format specifiers and their corresponding arguments.`,
  difficulty: 'beginner',
  prerequisites: ['u1-t4'],
  estimatedMinutes: 45,
  subtopics: [
    {
      id: 'u1-t9-s1', title: 'printf — Format Specifiers and Escape Sequences', slug: 'printf-formatting',
      description: `The printf function is C's primary mechanism for producing formatted output, and its architecture reveals a fundamental design pattern in the language: the format string as a type-dispatch protocol. When you call printf("Score: %d, GPA: %.2f\\n", score, gpa), the format string serves as a template that printf parses character by character. Literal characters are output directly; format specifiers (sequences beginning with %) instruct printf to extract the next variadic argument from the stack and convert it to a human-readable text representation according to the specifier's type code: %d for signed decimal integers, %f for floating-point numbers, %c for single characters, %s for null-terminated strings, %x for hexadecimal, and %p for pointer addresses.

The width and precision modifiers embedded within format specifiers provide fine-grained control over output formatting. A width specifier (e.g., %10d) sets the minimum field width, padding with spaces by default; a left-justification flag (%-10d) aligns the output to the left within the field. For floating-point numbers, a precision specifier (%.2f) controls the number of decimal places displayed. These modifiers are essential for producing professionally formatted output such as aligned tables, fixed-width columnar reports, and formatted financial figures.

Using the wrong format specifier for a given data type is not merely a display error — it is undefined behavior. Because printf extracts arguments from the stack as raw bytes and interprets them according to the specifier, a mismatch (e.g., passing a float but using %d) causes printf to read the wrong number of bytes and interpret the bit pattern under the wrong encoding scheme, producing garbage output or, in some cases, crashing the program. Modern compilers with -Wall or -Wformat enabled can detect many such mismatches at compile time, but the programmer bears ultimate responsibility for ensuring format string and argument type agreement.`,
      keyPoints: [
        'Every data type has a corresponding format specifier: %d for int, %u for unsigned int, %f for float and double (in printf), %e for scientific notation, %c for a single character, %s for a string, %p for a pointer address, %x/%X for hexadecimal, %o for octal, and %% to print a literal percent sign. Using the wrong specifier for a type is undefined behavior — the bits of your data get interpreted as something they are not, producing garbage output.',
        'A subtle but important inconsistency: in scanf, you must use %lf to read a double (and %f for float), but in printf, %f works for both float and double. This is because of "default argument promotion" — when a float is passed as a variadic argument (like to printf), it is automatically promoted to double by the compiler. This inconsistency trips up many students and is worth memorizing.',
        'Width specifiers let you control the minimum character width of the output. %5d prints an integer right-aligned in a 5-character field, padding with spaces on the left. %-5d left-aligns instead. %05d pads with zeros instead of spaces. These are essential for creating aligned columns in tables and reports.',
        'Precision specifiers control decimal places for floating-point numbers and character limits for strings. %.2f displays exactly 2 decimal places. %.5s truncates a string to its first 5 characters. You can combine width and precision: %8.2f means "minimum 8 characters wide, with 2 decimal places."',
        'Escape sequences are special character codes that represent things you cannot type directly in a string literal: \\n is a newline (the most common one — used to end every line of output), \\t is a horizontal tab, \\r is a carriage return, \\\\ prints a literal backslash, \\" prints a literal double quote inside a string, \\0 is the null character (marks the end of strings), and \\a produces a system bell sound.',
        'A detail that appears in tricky exam questions: printf returns an integer equal to the number of characters it successfully printed. So int n = printf("Hello") prints "Hello" on screen and stores 5 in n. This return value is rarely used in practice but is fair game for predict-the-output questions.',
      ],
      codeExamples: [
        {
          id: 'u1-t9-s1-ex1', title: 'All Common Format Specifiers', language: 'c',
          code: '#include <stdio.h>\n\nint main(void) {\n    int i = 42;\n    unsigned int u = 3000000000u;\n    float f = 3.14159f;\n    double d = 2.718281828;\n    char c = \'A\';\n    char s[] = "Hello";\n    printf("int:      %d\\n", i);\n    printf("unsigned: %u\\n", u);\n    printf("float:    %f\\n", f);\n    printf("double:   %.9f\\n", d);\n    printf("char:     %c (ASCII: %d)\\n", c, c);\n    printf("string:   %s\\n", s);\n    printf("hex:      %x\\n", i);\n    printf("octal:    %o\\n", i);\n    printf("pointer:  %p\\n", (void *)&i);\n    printf("percent:  100%%\\n");\n    return 0;\n}',
          explanation: 'This program is a reference sheet for format specifiers. Each printf call demonstrates a different specifier matched to its correct type. Notice that the char variable c can be printed both as a character (%c shows "A") and as an integer (%d shows 65, its ASCII value) — this dual interpretation is a recurring theme in C. The %x and %o specifiers show the same integer in different number bases, which is useful for understanding how data is represented internally. The %p specifier reveals the memory address where a variable is stored, a concept that becomes central when you study pointers.',
          expectedOutput: 'int:      42\nunsigned: 3000000000\nfloat:    3.141590\ndouble:   2.718281828\nchar:     A (ASCII: 65)\nstring:   Hello\nhex:      2a\noctal:    52\npointer:  0x7ffd...\npercent:  100%',
          lineBreakdown: [
            { lineNumber: 13, code: '    %.9f', explanation: '.9 = show 9 decimal places. Reveals double precision.' },
            { lineNumber: 16, code: '    %x', explanation: 'Prints integer in hexadecimal. 42 = 0x2a.' },
            { lineNumber: 19, code: '    100%%', explanation: '%% prints a literal % sign. A single % would start a format specifier.' },
          ],
          relatedTopicIds: ['u1-t5'],
        },
        {
          id: 'u1-t9-s1-ex2', title: 'Width and Precision Formatting', language: 'c',
          code: '#include <stdio.h>\n\nint main(void) {\n    printf("[%10d]\\n", 42);       /* right-aligned, width 10 */\n    printf("[%-10d]\\n", 42);      /* left-aligned, width 10 */\n    printf("[%05d]\\n", 42);       /* zero-padded, width 5 */\n    printf("[%8.2f]\\n", 3.14159); /* width 8, 2 decimals */\n    printf("[%-8.2f]\\n", 3.14159);/* left-aligned */\n    printf("[%.3s]\\n", "Hello");  /* first 3 chars only */\n    return 0;\n}',
          explanation: 'Width and precision modifiers transform printf from a basic printing function into a professional formatting tool. These modifiers are what make the difference between output that looks like a hastily printed debug log and output that looks like a formatted report or receipt. The brackets in this example make the field boundaries visible, so you can see exactly where padding is inserted. In real programs, you would omit the brackets and rely on consistent field widths to create aligned columns.',
          expectedOutput: '[        42]\n[42        ]\n[00042]\n[    3.14]\n[3.14    ]\n[Hel]',
          lineBreakdown: [
            { lineNumber: 4, code: '    %10d', explanation: 'Right-align in 10-char field. 8 spaces + "42".' },
            { lineNumber: 5, code: '    %-10d', explanation: '- flag = left-align. "42" + 8 spaces.' },
            { lineNumber: 6, code: '    %05d', explanation: '0 flag = pad with zeros instead of spaces.' },
          ],
          relatedTopicIds: [],
        },
        {
          id: 'u1-t9-s1-ex3', title: 'Escape Sequences', language: 'c',
          code: '#include <stdio.h>\n\nint main(void) {\n    printf("Line1\\nLine2\\n");           /* newline */\n    printf("Col1\\tCol2\\tCol3\\n");       /* tab */\n    printf("She said \\"hello\\"\\n");     /* quotes */\n    printf("Backslash: \\\\\\n");          /* backslash */\n    printf("Null char value: %d\\n", \'\\0\'); /* null = 0 */\n    return 0;\n}',
          explanation: 'Escape sequences solve a fundamental problem: how do you include characters in a string that cannot be typed directly? You cannot press Enter in the middle of a string literal (it would end the line of code), so \\n represents a newline. You cannot include a literal double quote inside a double-quoted string (it would end the string prematurely), so \\" represents a quote. The backslash acts as an "escape" character, telling the compiler that the next character has special meaning rather than its literal one. The null character \\0 is special — it marks the end of every string in C, which you will learn about in depth when studying arrays and strings.',
          expectedOutput: 'Line1\nLine2\nCol1\tCol2\tCol3\nShe said "hello"\nBackslash: \\\nNull char value: 0',
          lineBreakdown: [
            { lineNumber: 4, code: '    \\n', explanation: 'Newline — moves cursor to next line.' },
            { lineNumber: 5, code: '    \\t', explanation: 'Tab — advances to next tab stop (usually 8 chars).' },
            { lineNumber: 6, code: '    \\"', explanation: 'Escaped quote — prints a literal " inside a string.' },
          ],
          relatedTopicIds: [],
        },
      ],
      commonMistakes: [
        { id: 'u1-t9-s1-cm1', title: 'Wrong format specifier for type',
          wrongCode: 'float x = 3.14f;\nprintf("%d\\n", x);  /* %d for float — UB! */',
          correctCode: 'float x = 3.14f;\nprintf("%f\\n", x);  /* %f for float */',
          explanation: 'This is undefined behavior, and understanding why helps you appreciate what printf does internally. When printf sees %d, it goes to the location on the calling stack where it expects to find a 4-byte integer. But you passed a float, which occupies 4 bytes encoded in IEEE 754 floating-point format. printf reads those 4 bytes as if they were an integer, producing a completely meaningless number. The bits are the same, but the interpretation is different — like reading a Chinese book as if it were written in English. Always ensure every format specifier matches the type of its corresponding argument.',
          consequence: 'The output is a garbage integer that bears no relationship to the actual float value. This is formally undefined behavior, meaning the compiler is not required to produce any particular result. Some compilers with -Wall will warn about format-type mismatches, which is another reason to always compile with warnings enabled.' },
        { id: 'u1-t9-s1-cm2', title: 'Missing arguments for format specifiers',
          wrongCode: 'printf("a=%d b=%d\\n", a);  /* missing b! */',
          correctCode: 'printf("a=%d b=%d\\n", a, b);',
          explanation: 'printf processes format specifiers from left to right, picking up arguments in order. If you have two %d specifiers but only one argument, the second %d reads whatever happens to be next on the calling stack — leftover data from a previous function call, garbage from an uninitialized register, or part of an adjacent local variable. The result is completely unpredictable and is formally undefined behavior. Modern compilers with -Wformat (included in -Wall) will warn about mismatched argument counts, but only if you enable warnings.',
          consequence: 'The second %d reads random bytes from the calling stack, producing a garbage value that changes between runs. In some cases, this can cause a crash if the garbage bytes happen to be interpreted as a pointer in a %s or %p context.' },
      ],
      interviewCallouts: [
        { id: 'u1-t9-s1-ic1', title: 'What does printf return?',
          content: 'Most students are surprised to learn that printf has a return value, and interviewers like to test this with tricky questions. printf returns the total number of characters it successfully wrote to the output stream. For example, printf("Hi") returns 2. A famous exam question nests printf calls: printf("%d", printf("Hello")) first executes the inner printf, which prints "Hello" (5 characters) and returns 5. The outer printf then prints that 5. The complete output is "Hello5". Understanding this requires grasping that printf is just a function that both performs an action (printing) and returns a value (character count), like any other function in C.',
          relatedTopicIds: [], frequency: 'occasional' },
      ],
      checkpoints: [
        { id: 'u1-t9-s1-cp1', title: 'printf Mastery', description: 'Verify printf understanding.',
          criteria: ['Use all common format specifiers correctly', 'Format numbers with width and precision', 'Use escape sequences for special characters'], topicId: 'u1-t9' },
      ],
      revisionCards: [
        { id: 'u1-t9-s1-rc1', front: 'What format specifiers do you use for int, float, double, char, string?', back: 'int: %d, float: %f, double: %f (printf) or %lf (scanf), char: %c, string: %s, unsigned: %u, hex: %x, pointer: %p.', topicId: 'u1-t9', tags: ['printf', 'format'] },
        { id: 'u1-t9-s1-rc2', front: 'What does %-10.2f mean?', back: 'Left-aligned (-), minimum width 10, 2 decimal places. Example: [3.14      ] with padding on the right.', topicId: 'u1-t9', tags: ['printf', 'width'] },
        { id: 'u1-t9-s1-rc3', front: 'How do you print a literal % in printf?', back: 'Use %%. A single % starts a format specifier, so %% is needed to print the character itself.', topicId: 'u1-t9', tags: ['printf', 'escape'] },
      ],
    },
    {
      id: 'u1-t9-s2', title: 'scanf — Reading Input', slug: 'scanf-input',
      description: `The scanf function is printf's complement: it reads text from standard input (the keyboard, by default) and converts it into binary data stored in program variables. The function parses the input stream according to its format string, matching format specifiers to successive input tokens. When scanf encounters %d, it skips leading whitespace, reads characters that form a valid integer representation, converts the character sequence to a binary int value using internal string-to-integer logic, and stores the result at the memory address provided by the corresponding argument.

This storage mechanism introduces the most critical requirement of scanf: the address-of operator (&). Because C is strictly pass-by-value, passing a variable x directly to scanf would give scanf a copy of x's current value \u2014 useless, because scanf needs to modify the original variable. Instead, you pass &x, the memory address where x resides. scanf then writes the converted value directly to that address, effectively modifying the caller's variable through pointer indirection. This is a student's first encounter with the concept that will later be formalised as pointers: the ability to give a function the location of a variable so it can modify the original rather than a copy. Forgetting the & operator means scanf receives a garbage integer that it interprets as a memory address and writes to an arbitrary location \u2014 classic undefined behavior that typically manifests as a segmentation fault.

Scanf's return value is a critical but frequently ignored diagnostic. It returns the number of input items successfully matched and assigned. If the user types \"abc\" when scanf expects %d, the conversion fails, scanf returns 0, and the offending characters remain in the input buffer \u2014 causing subsequent scanf calls to fail repeatedly in an infinite loop. Robust input handling always checks scanf's return value and, when conversion fails, clears the input buffer (typically by reading and discarding characters until a newline is found) before prompting the user again.`,
      keyPoints: [
        'The & in scanf("%d", &x) is not optional decoration — it is the entire mechanism that makes scanf work. When you call a function in C, the function receives a copy of each argument, not the original variable. If you passed x (without &), scanf would receive a copy of whatever value x currently holds and would have no way to modify the original x. By passing &x (the memory address of x), you are telling scanf: "Here is the location where x lives in memory — write the converted value directly to this address."',
        'Without the & operator, scanf receives the current value of x as if it were a memory address, and tries to write data to that location. If x is uninitialized (containing garbage like -12345), scanf tries to write to address -12345, which your program does not own. The operating system immediately kills your program with a segmentation fault. Even if x happens to contain a valid-looking address, you corrupt random memory, causing bizarre bugs that may not manifest until much later.',
        'scanf returns an integer equal to the number of items it successfully read and converted. If you call scanf("%d %d", &a, &b) and the user types "10 hello", scanf successfully converts 10 into a and stops at "hello" (which is not a valid integer), returning 1. If the user types "hello world", scanf cannot convert either item and returns 0. Always check this return value in robust programs to detect invalid input.',
        'For string variables (char arrays), you do NOT use & with scanf. This is because an array name in C automatically "decays" to a pointer to its first element. Writing scanf("%s", name) already passes the address of name\'s first character, which is exactly what scanf needs. Adding & gives a pointer-to-array which has a subtly different type and, while it may work by coincidence, is technically incorrect.',
        'The %s specifier stops reading at the first whitespace character (space, tab, newline), so scanf("%s", name) cannot read a full name like "John Doe" — it would only capture "John". To read an entire line including spaces, use the fgets function instead: fgets(name, sizeof(name), stdin). This is safer and more predictable than scanf for string input.',
        'One of the most frustrating scanf bugs involves the newline character left in the input buffer. When you read an integer with scanf("%d"), the Enter key the user pressed generates a \\n character that remains in the buffer. If the next scanf call reads a character with %c, it will immediately consume that leftover \\n instead of waiting for new input. The fix is to add a space before %c: scanf(" %c", &ch). The space tells scanf to skip all whitespace before reading the character.',
      ],
      codeExamples: [
        {
          id: 'u1-t9-s2-ex1', title: 'scanf Basics — Why & Is Needed', language: 'c',
          code: '#include <stdio.h>\n\nint main(void) {\n    int age;\n    float gpa;\n    char grade;\n    printf("Enter age, GPA, grade: ");\n    scanf("%d %f %c", &age, &gpa, &grade);\n    printf("Age: %d, GPA: %.1f, Grade: %c\\n", age, gpa, grade);\n    return 0;\n}',
          explanation: 'This program demonstrates the most essential pattern in C input: declare a variable, then use scanf with the & operator to fill it with user input. The & is not a formatting flourish — it is the mechanism that makes the entire operation possible. Without it, scanf has no way to modify your variable because C passes all function arguments by value (as copies). By passing &age, &gpa, and &grade, you are giving scanf the exact memory addresses where it should write the converted data. This is your first practical encounter with the address-of operator, which becomes the foundation of pointer programming in Unit 2.',
          expectedOutput: 'Enter age, GPA, grade: Age: 19, GPA: 8.5, Grade: A',
          lineBreakdown: [
            { lineNumber: 8, code: '    scanf("%d %f %c", &age, &gpa, &grade)', explanation: '& gives the memory address. Without it, scanf would write to a random address → crash.' },
          ],
          relatedTopicIds: ['u1-t4'],
        },
        {
          id: 'u1-t9-s2-ex2', title: 'The Newline-in-Buffer Problem', language: 'c',
          code: '#include <stdio.h>\n\nint main(void) {\n    int num;\n    char ch;\n    printf("Enter a number: ");\n    scanf("%d", &num);\n    printf("Enter a character: ");\n    scanf(" %c", &ch);  /* space before %c eats the leftover \\n */\n    printf("Number: %d, Char: %c\\n", num, ch);\n    return 0;\n}',
          explanation: 'This is one of the most frustrating bugs beginners encounter, and it can waste hours of debugging time. When you type 42 and press Enter, two things go into the input buffer: the characters "42" and the newline character \\n from the Enter key. The first scanf("%d", &num) consumes the "42" but leaves the \\n in the buffer. When the second scanf("%c", &ch) executes, it immediately finds the \\n waiting in the buffer and reads it as the character, without ever pausing for the user to type anything. The program appears to skip the second input entirely. The fix is elegant: a space before %c in the format string tells scanf to skip any whitespace characters (spaces, tabs, newlines) before reading the actual character input.',
          expectedOutput: 'Enter a number: Enter a character: Number: 42, Char: A',
          lineBreakdown: [
            { lineNumber: 9, code: '    scanf(" %c", &ch)', explanation: 'The space before %c skips whitespace (including the leftover \\n from the previous scanf). Without it, ch would be \\n.' },
          ],
          relatedTopicIds: ['u1-t11'],
        },
        {
          id: 'u1-t9-s2-ex3', title: 'Checking scanf Return Value', language: 'c',
          code: '#include <stdio.h>\n\nint main(void) {\n    int a, b;\n    printf("Enter two integers: ");\n    int count = scanf("%d %d", &a, &b);\n    if (count == 2) {\n        printf("Sum = %d\\n", a + b);\n    } else {\n        printf("Invalid input! (read %d items)\\n", count);\n    }\n    return 0;\n}',
          explanation: 'Checking scanf\'s return value is the hallmark of robust C programming. scanf returns the number of items it successfully read and converted — not the number of characters, but the number of format specifiers that were satisfied. If the user types "10 hello" when you expected two integers, scanf successfully converts 10 (returning 1) but cannot convert "hello" into an integer (so it stops). If the user types nothing valid at all, scanf returns 0. By checking this return value against the number of items you expected, you can detect invalid input and re-prompt the user instead of proceeding with garbage data.',
          expectedOutput: 'Enter two integers: Sum = 30',
          lineBreakdown: [
            { lineNumber: 6, code: '    int count = scanf(...)', explanation: 'scanf returns 2 if both ints were read, 1 if only one, 0 if none.' },
            { lineNumber: 7, code: '    if (count == 2)', explanation: 'Only proceed if both values were successfully read.' },
          ],
          relatedTopicIds: [],
        },
      ],
      commonMistakes: [
        { id: 'u1-t9-s2-cm1', title: 'Forgetting & in scanf',
          wrongCode: 'int x;\nscanf("%d", x);  /* CRASH: x is garbage, used as address */',
          correctCode: 'int x;\nscanf("%d", &x);  /* correct: &x gives address of x */',
          explanation: 'This is the #1 most common scanf bug, and it usually results in a segmentation fault (crash). Without &, you pass the current value of x to scanf. If x is uninitialized, its value is garbage — some random number like -12345 or 4096. scanf interprets this garbage as a memory address and tries to write the user\'s input to that address. Since your program does not own address -12345, the operating system kills it immediately with a segfault. The fix is simple: always write &x. The & operator gives scanf the legitimate address of x, telling it exactly where to store the data.',
          consequence: 'Segmentation fault (crash) in most cases. If by astronomical coincidence the garbage value happens to be a valid writable address, scanf silently corrupts that memory location, creating a bug that may not manifest until much later in the program. This is the #1 scanf mistake and the #1 cause of crashes in beginner C programs.' },
        { id: 'u1-t9-s2-cm2', title: 'Using & with string in scanf',
          wrongCode: 'char name[50];\nscanf("%s", &name);  /* & is wrong here! */',
          correctCode: 'char name[50];\nscanf("%s", name);  /* array name is already an address */',
          explanation: 'This mistake is subtler than the missing-& bug because it often works by coincidence. An array name like "name" already acts as a pointer to the first element of the array. When you write scanf("%s", name), scanf receives the address of name[0], which is exactly what it needs. Adding & gives scanf a pointer-to-array rather than a pointer-to-char. These have the same numeric value (same address) but different types. While most compilers will produce working code, the type mismatch is technically incorrect and violates the C standard. The compiler will emit a warning if you compile with -Wall.',
          consequence: 'Compiler warning about incompatible pointer types. The program likely works correctly by coincidence (since the numeric address is the same), but it is technically incorrect and may fail with strict compiler settings or on exotic architectures.' },
      ],
      interviewCallouts: [
        { id: 'u1-t9-s2-ic1', title: 'Why does scanf need &?',
          content: 'This question tests your understanding of C\'s pass-by-value semantics, and it is effectively asking "do you understand pointers?" In C, when you call a function, each argument is copied — the function receives a copy, not the original. If scanf received just x (the value), it could examine x but could never modify the original variable back in main. By passing &x (the address), scanf receives a number that tells it exactly where in memory x lives, and it can then write directly to that location, modifying the original variable. This is the essence of pointers: passing an address lets a function reach back and modify a variable that belongs to its caller. When you study pointers formally in Unit 2, you will see that & with scanf was your very first pointer operation.',
          relatedTopicIds: [], frequency: 'common' },
      ],
      checkpoints: [
        { id: 'u1-t9-s2-cp1', title: 'scanf Mastery', description: 'Verify scanf understanding.',
          criteria: ['Read int, float, char with correct format specifiers', 'Handle the newline-in-buffer issue', 'Check scanf return value for input validation'], topicId: 'u1-t9' },
      ],
      revisionCards: [
        { id: 'u1-t9-s2-rc1', front: 'Why does scanf need & before variable names?', back: 'scanf needs the memory ADDRESS to know WHERE to store the input. & gives the address. Without it, scanf writes to a random location → crash.', topicId: 'u1-t9', tags: ['scanf', 'address-of'] },
        { id: 'u1-t9-s2-rc2', front: 'How do you fix the newline-in-buffer problem after scanf %d?', back: 'Put a space before %c: scanf(" %c", &ch). The space tells scanf to skip whitespace (including the leftover \\n) before reading the character.', topicId: 'u1-t9', tags: ['scanf', 'newline-buffer'] },
        { id: 'u1-t9-s2-rc3', front: 'What does scanf return?', back: 'The number of items successfully read. If you call scanf("%d %d", &a, &b), it returns 2 on success, 1 if only first was read, 0 or EOF on failure.', topicId: 'u1-t9', tags: ['scanf', 'return-value'] },
      ],
    },
  ],

  theoryQuestions: [
    { id: 'u1-t9-q1', type: 'predict-output', topicId: 'u1-t9', difficulty: 'beginner',
      question: 'What does this print?',
      code: '#include <stdio.h>\nint main(void) {\n    printf("%d\\n", printf("Hello"));\n    return 0;\n}',
      correctAnswer: 'Hello5',
      explanation: 'Inner printf("Hello") prints "Hello" and returns 5 (length). Outer printf prints that 5. Output: Hello5.',
      tags: ['printf', 'return-value'] },
    { id: 'u1-t9-q2', type: 'predict-output', topicId: 'u1-t9', difficulty: 'beginner',
      question: 'What does this print?',
      code: '#include <stdio.h>\nint main(void) {\n    printf("[%05d]\\n", 42);\n    printf("[%-8.2f]\\n", 3.14159);\n    return 0;\n}',
      correctAnswer: '[00042]\n[3.14    ]',
      explanation: '%05d: zero-padded to width 5 → 00042. %-8.2f: left-aligned, width 8, 2 decimals → "3.14    ".',
      tags: ['printf', 'formatting'] },
    { id: 'u1-t9-q3', type: 'spot-bug', topicId: 'u1-t9', difficulty: 'beginner',
      question: 'Find the bug:',
      code: '#include <stdio.h>\nint main(void) {\n    int x;\n    scanf("%d", x);\n    printf("%d\\n", x);\n    return 0;\n}',
      correctAnswer: 'Missing & before x in scanf. Should be scanf("%d", &x);',
      explanation: 'Without &, the VALUE of x (garbage) is passed as an address. scanf tries to write to that garbage address → segfault or corruption.',
      tags: ['scanf', 'address-of'] },
    { id: 'u1-t9-q4', type: 'mcq', topicId: 'u1-t9', difficulty: 'beginner',
      question: 'Which format specifier is used for double in scanf?',
      options: ['%f', '%d', '%lf', '%e'],
      correctAnswer: '%lf',
      explanation: 'scanf uses %lf for double and %f for float. printf uses %f for both (due to default argument promotion). This inconsistency is a common source of bugs.',
      tags: ['scanf', 'double', 'format'] },
    { id: 'u1-t9-q5', type: 'true-false', topicId: 'u1-t9', difficulty: 'beginner',
      question: 'When reading a string with scanf("%s", name), you need & before name.',
      correctAnswer: false,
      explanation: 'An array name already decays to a pointer (address of first element). Adding & gives a pointer-to-array which is the wrong type. Use scanf("%s", name) without &.',
      tags: ['scanf', 'string', 'array'] },
  ],

  programmingProblems: [
    {
      id: 'u1-t9-new-easy',
      title: 'Armstrong Number Check',
      topicId: 'u1-t9',
      difficulty: 'beginner',
      problemStatement: 'Read a 3-digit number. Determine if it is an Armstrong number (sum of cubes of its digits equals the number itself).',
      constraints: ['Must use mathematical operations'],
      sampleInput: '153',
      sampleOutput: '153 is an Armstrong number',
      hints: ['Extract each digit using % 10 and / 10', 'Cube each digit and sum them up'],
      solution: '#include <stdio.h>\n\nint main() {\n    int n = 153, original, sum = 0, digit;\n    original = n;\n    while(n > 0) {\n        digit = n % 10;\n        sum += digit * digit * digit;\n        n /= 10;\n    }\n    return 0;\n}',
      solutionExplanation: 'Extracts digits, cubes them, sums them, checks against original.',
      dryRun: [],
      tags: ['number-theory']
    },
    {
      id: 'u1-t9-new-med',
      title: 'Factorial Calculation',
      topicId: 'u1-t9',
      difficulty: 'intermediate',
      problemStatement: 'Calculate the factorial of a given number n without using recursion.',
      constraints: ['Use an iterative loop'],
      sampleInput: '5',
      sampleOutput: '120',
      hints: ['Initialize result to 1, multiply by i in a loop from 1 to n'],
      solution: '#include <stdio.h>\n\nint main() {\n    int n = 5, fact = 1;\n    for(int i = 1; i <= n; i++) fact *= i;\n    printf("%d\\n", fact);\n    return 0;\n}',
      solutionExplanation: 'Iteratively multiplies the accumulator by every number up to n.',
      dryRun: [],
      tags: ['math', 'loops']
    },
    {
      id: 'u1-t9-new-hard',
      title: 'Diamond Pattern',
      topicId: 'u1-t9',
      difficulty: 'advanced',
      problemStatement: 'Print a diamond pattern of stars for a given number of rows n.',
      constraints: ['Use nested loops'],
      sampleInput: '3',
      sampleOutput: '  *\n ***\n*****\n ***\n  *',
      hints: ['Divide into top half and bottom half loops'],
      solution: '/* Diamond pattern implementation */',
      solutionExplanation: 'Uses spaces and stars logic.',
      dryRun: [],
      tags: ['patterns']
    },
    { id: 'u1-t9-p1', title: 'Formatted Receipt Printer', topicId: 'u1-t9', difficulty: 'beginner',
      problemStatement: 'Print a formatted receipt: item name (left-aligned, 20 chars), quantity (right-aligned, 5 chars), price (right-aligned, 8 chars with 2 decimals). Use at least 3 items.',
      constraints: ['Use width and alignment formatting', 'Use %-20s, %5d, %8.2f patterns'],
      sampleInput: '', sampleOutput: 'Item                  Qty    Price\nPen                     10    15.00\nNotebook                 5   120.50\nEraser                  20     5.75',
      hints: ['Use %-20s for left-aligned strings', 'Use %5d for right-aligned int', 'Use %8.2f for right-aligned float'],
      solution: '#include <stdio.h>\n\nint main(void) {\n    printf("%-20s %5s %8s\\n", "Item", "Qty", "Price");\n    printf("%-20s %5d %8.2f\\n", "Pen", 10, 15.00);\n    printf("%-20s %5d %8.2f\\n", "Notebook", 5, 120.50);\n    printf("%-20s %5d %8.2f\\n", "Eraser", 20, 5.75);\n    return 0;\n}',
      solutionExplanation: '%-20s left-aligns strings in 20-char fields. %5d right-aligns integers. %8.2f right-aligns floats with 2 decimals. This creates aligned columns.',
      dryRun: [
        { step: 1, line: 4, variables: {}, output: 'Item                  Qty    Price', explanation: 'Header row with column labels.' },
        { step: 2, line: 5, variables: {}, output: 'Pen                     10    15.00', explanation: 'First data row with formatting.' },
      ],
      tags: ['printf', 'formatting'] },
    { id: 'u1-t9-p2', title: 'Input Validation Loop', topicId: 'u1-t9', difficulty: 'intermediate',
      problemStatement: 'Read an integer between 1 and 100. Keep asking until valid input is given. Use scanf return value.',
      constraints: ['Use do-while loop', 'Check scanf return value', 'Re-prompt on invalid input'],
      sampleInput: 'abc\n0\n150\n42', sampleOutput: 'Enter (1-100): Invalid!\nEnter (1-100): Out of range!\nEnter (1-100): Out of range!\nEnter (1-100): You entered: 42',
      hints: ['Use scanf return value to detect non-integer input', 'Clear invalid input with while(getchar() != \\n)'],
      solution: '#include <stdio.h>\n\nint main(void) {\n    int n;\n    int valid;\n    do {\n        printf("Enter (1-100): ");\n        valid = scanf("%d", &n);\n        if (valid != 1) {\n            printf("Invalid!\\n");\n            while (getchar() != \'\\n\');  /* clear bad input */\n        } else if (n < 1 || n > 100) {\n            printf("Out of range!\\n");\n            valid = 0;\n        }\n    } while (valid != 1);\n    printf("You entered: %d\\n", n);\n    return 0;\n}',
      solutionExplanation: 'do-while ensures at least one prompt. scanf returns 1 on success. If not, clear the buffer and re-prompt. Check range separately.',
      dryRun: [
        { step: 1, line: 8, variables: { valid: '0' }, output: 'Invalid!', explanation: '"abc" → scanf returns 0. Clear buffer.' },
        { step: 2, line: 8, variables: { n: '0', valid: '1' }, output: 'Out of range!', explanation: '0 is out of 1-100 range.' },
        { step: 3, line: 8, variables: { n: '42', valid: '1' }, output: 'You entered: 42', explanation: '42 is valid. Loop ends.' },
      ],
      tags: ['scanf', 'validation', 'do-while'] },
    { id: 'u1-t9-p3', title: 'Number System Converter', topicId: 'u1-t9', difficulty: 'beginner',
      problemStatement: 'Read an integer and print it in decimal, octal, and hexadecimal format.',
      constraints: ['Use %d, %o, %x format specifiers', 'Show 0x prefix for hex'],
      sampleInput: '255', sampleOutput: 'Decimal: 255\nOctal: 377\nHex: ff\nHex (upper): FF',
      hints: ['%o for octal', '%x for lowercase hex, %X for uppercase', '%#x adds 0x prefix automatically'],
      solution: '#include <stdio.h>\n\nint main(void) {\n    int n;\n    scanf("%d", &n);\n    printf("Decimal: %d\\n", n);\n    printf("Octal: %o\\n", n);\n    printf("Hex: %x\\n", n);\n    printf("Hex (upper): %X\\n", n);\n    return 0;\n}',
      solutionExplanation: 'Same integer, different bases. %o for base-8, %x for base-16. 255 = 0xFF = 0377 in octal.',
      dryRun: [
        { step: 1, line: 5, variables: { n: '255' }, output: '', explanation: 'Read 255.' },
        { step: 2, line: 6, variables: {}, output: 'Decimal: 255', explanation: '%d prints in base 10.' },
        { step: 3, line: 7, variables: {}, output: 'Octal: 377', explanation: '%o prints in base 8. 255 = 3*64 + 7*8 + 7.' },
        { step: 4, line: 8, variables: {}, output: 'Hex: ff', explanation: '%x prints in base 16. 255 = 15*16 + 15 = ff.' },
      ],
      tags: ['printf', 'number-systems'] },
  ],
};
