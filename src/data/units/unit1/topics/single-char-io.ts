import type { Topic } from '../../../../types';

export const singleCharIo: Topic = {
  id: 'u1-t11',
  unitId: 'unit-1',
  title: 'Single Character Input and Output',
  slug: 'single-char-io',
  description: `Character-at-a-time I/O, provided by the getchar() and putchar() functions, is the lowest-level text processing interface in the C standard library. While printf and scanf operate on formatted data — parsing and producing structured representations of integers, floats, and strings — getchar and putchar operate on individual bytes, giving the programmer complete control over how text is consumed, transformed, and emitted. This granularity is the foundation of the Unix text-processing philosophy: classic utilities like cat (copy input to output), wc (count words), and grep (pattern matching) are all built on character-at-a-time reading loops.

Understanding getchar requires confronting two critical design decisions. First, the input buffer: on most systems, characters typed at the keyboard are not delivered to the program immediately; instead, the terminal driver accumulates them in a line buffer and delivers the entire line (including the terminating newline character) to the program only when the user presses Enter. This line-buffered behaviour means that getchar blocks until a complete line is available, then returns characters from the buffer one at a time on successive calls. Second, the EOF sentinel: getchar returns the special value EOF (typically defined as -1) when no more input is available, and to ensure that EOF is distinguishable from every valid byte value (0–255), getchar returns int rather than char.

This int return type is not a minor implementation detail — it is a correctness requirement that prevents a pernicious bug. If getchar returned char and the platform defines char as unsigned (which is implementation-defined), then EOF (-1) would be silently converted to 255, making it indistinguishable from the legitimate character with ASCII code 255. Storing getchar's result in a char variable is therefore a logic error that can cause programs to loop infinitely or terminate prematurely, depending on the platform's char signedness.`,
  difficulty: 'beginner',
  prerequisites: ['u1-t9'],
  estimatedMinutes: 30,
  subtopics: [
    {
      id: 'u1-t11-s1', title: 'getchar, putchar, and EOF', slug: 'getchar-putchar-eof',
      description: 'The getchar/putchar pair represents the most fundamental I/O pattern in C: reading and writing one character at a time. This granularity gives you complete control over text processing, but it requires understanding two critical concepts. First, the input buffer: characters typed on the keyboard are not delivered to your program one at a time as you press keys. Instead, they accumulate in a buffer until you press Enter, at which point the entire line (including the newline character) becomes available. Second, the EOF sentinel: getchar returns the special value EOF (typically -1) when there is no more input to read. Because EOF must be distinguishable from any valid character byte (0–255), getchar returns int, not char — a design decision that trips up beginners but is essential for correctness.',
      keyPoints: [
        'getchar() reads a single character from standard input (the keyboard, or redirected input) and returns it as an int. The fact that it returns int — not char — is one of the most important details in C I/O. If getchar returned char and your platform defines char as unsigned (which is implementation-defined), then EOF (-1) would be silently converted to 255, making it indistinguishable from a valid character with that code. Using int gives getchar the range to represent all 256 possible byte values (0–255) plus the special EOF sentinel (-1).',
        'putchar(c) writes a single character to standard output. It is simpler and slightly faster than printf("%c", c) because it does not need to parse a format string. When processing text character by character, putchar is the idiomatic choice for output, just as getchar is for input.',
        'EOF is a macro defined in <stdio.h>, typically with the value -1. It does not represent a character that appears in the input stream; rather, it is a signal from the operating system that no more input is available. On a terminal, you send EOF by pressing Ctrl+D on Linux/Mac or Ctrl+Z followed by Enter on Windows. When reading from a file via input redirection (./program < file.txt), EOF is signaled automatically when the file ends.',
        'The critical reason getchar returns int rather than char is that the return type must accommodate 257 distinct values: 256 possible byte values (0–255) plus EOF (-1). A char type can hold only 256 values. If char is signed (-128 to 127), then byte values 128–255 collide with negative numbers. If char is unsigned (0–255), then -1 wraps to 255, colliding with the character with ASCII code 255. Only int has enough range to represent all possibilities without ambiguity.',
        'You can send EOF to your program from the terminal by pressing Ctrl+D (on Linux and macOS) or Ctrl+Z followed by Enter (on Windows). When testing programs that read until EOF, this is how you signal that you are done typing. When using input redirection (./program < input.txt), EOF is sent automatically when the end of the file is reached.',
        'getc(stdin) is functionally equivalent to getchar() — they both read a single character from standard input. The difference is that getc takes a FILE* argument, so it can read from any file stream, not just stdin. Similarly, putc(c, stdout) is equivalent to putchar(c) but can write to any file stream. You will use getc and putc when you study file I/O in Unit 4.',
      ],
      codeExamples: [
        {
          id: 'u1-t11-s1-ex1', title: 'Read and Echo Characters Until EOF', language: 'c',
          code: '#include <stdio.h>\n\nint main(void) {\n    int ch;  /* int, not char! Must hold EOF */\n    printf("Type text (Ctrl+D to end):\\n");\n    while ((ch = getchar()) != EOF) {\n        putchar(ch);\n    }\n    printf("\\nDone.\\n");\n    return 0;\n}',
          explanation: 'This is the canonical character-reading pattern in C, and every C programmer should be able to write it from memory. The key details are: (1) ch must be declared as int, not char, to hold the EOF value without ambiguity. (2) The assignment and comparison are combined in the while condition: (ch = getchar()) assigns the return value of getchar to ch, and then != EOF checks whether the input has ended. The parentheses around the assignment are critical — without them, operator precedence would cause the expression to be interpreted as ch = (getchar() != EOF), which assigns 0 or 1 to ch instead of the actual character. This pattern appears in countless real-world C programs, from text filters to file processors.',
          expectedOutput: 'Type text (Ctrl+D to end):\nHello\nHello\nDone.',
          lineBreakdown: [
            { lineNumber: 4, code: '    int ch;', explanation: 'MUST be int, not char. If char and char is unsigned, EOF (-1) becomes 255 and the loop never ends.' },
            { lineNumber: 6, code: '    (ch = getchar()) != EOF', explanation: 'Read a char, assign to ch, then compare with EOF. Parentheses are critical — without them, ch = (getchar() != EOF) gives 0 or 1.' },
          ],
          relatedTopicIds: ['u1-t9'],
        },
        {
          id: 'u1-t11-s1-ex2', title: 'Count Characters, Words, Lines', language: 'c',
          code: '#include <stdio.h>\n\nint main(void) {\n    int ch, chars = 0, words = 0, lines = 0;\n    int in_word = 0;\n    printf("Enter text (Ctrl+D to end):\\n");\n    while ((ch = getchar()) != EOF) {\n        chars++;\n        if (ch == \'\\n\') lines++;\n        if (ch == \' \' || ch == \'\\n\' || ch == \'\\t\') {\n            in_word = 0;\n        } else if (!in_word) {\n            in_word = 1;\n            words++;\n        }\n    }\n    printf("\\nChars: %d, Words: %d, Lines: %d\\n", chars, words, lines);\n    return 0;\n}',
          explanation: 'This is a simplified version of the classic Unix wc (word count) command, and it demonstrates how powerful character-at-a-time processing can be. The program maintains a state variable in_word that tracks whether the current position is inside a word or between words. The logic is beautifully simple: when you encounter a whitespace character (space, newline, or tab), you are between words (in_word = 0). When you encounter a non-whitespace character while between words (!in_word), you have found the start of a new word, so you increment the word count and set in_word = 1. This state-tracking approach generalizes to many text-processing problems: tokenizing, parsing, and pattern matching all follow similar structures.',
          expectedOutput: 'Enter text (Ctrl+D to end):\nhello world\n\nChars: 12, Words: 2, Lines: 1',
          lineBreakdown: [
            { lineNumber: 5, code: '    int in_word = 0;', explanation: 'State flag: are we currently inside a word? Starts false.' },
            { lineNumber: 10, code: '    ch == \' \' || ...', explanation: 'Whitespace characters end a word. in_word resets to 0.' },
            { lineNumber: 12, code: '    } else if (!in_word)', explanation: 'Non-whitespace after whitespace = start of a new word. Increment count.' },
          ],
          relatedTopicIds: [],
        },
        {
          id: 'u1-t11-s1-ex3', title: 'The Newline-in-Buffer Pitfall', language: 'c',
          code: '#include <stdio.h>\n\nint main(void) {\n    int num;\n    char ch;\n    printf("Enter a number: ");\n    scanf("%d", &num);\n    /* \\n from Enter is still in the buffer! */\n\n    /* BAD: getchar() reads the leftover \\n */\n    /* ch = getchar(); -- would give \\n, not user input */\n\n    /* FIX: consume the leftover \\n first */\n    while (getchar() != \'\\n\');  /* flush buffer */\n\n    printf("Enter a character: ");\n    ch = getchar();\n    printf("Number: %d, Char: %c\\n", num, ch);\n    return 0;\n}',
          explanation: 'This program demonstrates the most practical solution to the newline-in-buffer problem, which is one of the most frustrating bugs in C I/O programming. When you call scanf("%d", &num) and the user types "42" and presses Enter, scanf reads the digits "42" but leaves the newline character (\\n from the Enter key) in the input buffer. If you then call getchar(), it immediately returns that leftover \\n without waiting for the user to type anything. The fix shown here uses a while loop that calls getchar() repeatedly, discarding characters until it finds and consumes the newline. After this flush, the next getchar() call will wait for fresh input from the user. An alternative fix (shown in the scanf topic) is to use scanf(" %c", &ch) with a leading space, which tells scanf to skip whitespace before reading.',
          expectedOutput: 'Enter a number: Enter a character: Number: 42, Char: A',
          lineBreakdown: [
            { lineNumber: 14, code: '    while (getchar() != \'\\n\');', explanation: 'Reads and discards characters until \\n is consumed. Clears the buffer.' },
            { lineNumber: 17, code: '    ch = getchar();', explanation: 'Now getchar() waits for fresh input from the user.' },
          ],
          relatedTopicIds: ['u1-t9'],
        },
      ],
      commonMistakes: [
        { id: 'u1-t11-s1-cm1', title: 'Using char instead of int for getchar()',
          wrongCode: 'char ch;\nwhile ((ch = getchar()) != EOF) { ... }',
          correctCode: 'int ch;\nwhile ((ch = getchar()) != EOF) { ... }',
          explanation: 'This is one of the most dangerous bugs in C because it can cause an infinite loop that consumes 100% CPU and never terminates. Whether char is signed or unsigned is implementation-defined: on some platforms it is signed (-128 to 127) and on others it is unsigned (0 to 255). If char is unsigned, then when getchar() returns EOF (-1), assigning it to an unsigned char converts -1 to 255 via unsigned wraparound. The comparison ch != EOF then compares 255 with -1, which is never equal, so the loop runs forever. On platforms where char is signed, a different problem arises: the character with ASCII value 127 or higher might accidentally match EOF. The solution is simple and universal: always declare the variable that receives getchar()\'s return as int.',
          consequence: 'On platforms where char is unsigned, the loop never terminates because EOF (-1) is silently converted to 255, which is a valid character value and can never equal -1. The program enters an infinite loop that consumes CPU forever.' },
        { id: 'u1-t11-s1-cm2', title: 'Not flushing buffer after scanf',
          wrongCode: 'scanf("%d", &n);\nchar c = getchar();  /* reads \\n, not new input! */',
          correctCode: 'scanf("%d", &n);\nwhile (getchar() != \'\\n\');  /* flush */\nchar c = getchar();  /* now reads fresh input */',
          explanation: 'This is the same newline-in-buffer problem discussed in the I/O Functions topic, but here it manifests with getchar instead of scanf("%c"). After scanf("%d", &n) reads the integer, the \\n from the Enter key remains in stdin\'s buffer. When getchar() is called next, it does not pause for the user to type — it immediately finds the leftover \\n and returns it. The user never gets a chance to enter a character because getchar is satisfied by the buffered newline. The fix is to consume all characters up to and including the newline before calling getchar for fresh input. The while(getchar() != \'\\n\') idiom reads and discards characters one at a time until it finds the newline, clearing the buffer completely.',
          consequence: 'getchar() returns the newline character (ASCII value 10) immediately, without waiting for the user to type. The program appears to skip the input prompt entirely, which is baffling to beginners who do not understand the buffer model.' },
      ],
      interviewCallouts: [
        { id: 'u1-t11-s1-ic1', title: 'Why does getchar() return int instead of char?',
          content: 'This question probes whether you understand the fundamental constraint of data representation: every value a function can return must be distinguishable from every other possible return value. getchar must be able to return any of the 256 possible byte values (0 through 255) that a character might have, AND it must also be able to return the special sentinel value EOF to signal "no more input." That is 257 distinct values. A char type can represent at most 256 values (either 0–255 if unsigned, or -128–127 if signed). In either case, at least one valid character value would collide with EOF, making it impossible to tell whether the input has ended or a particular character was read. An int, with its 4 bytes and billions of possible values, has more than enough room for all 256 characters plus EOF. This is a clean example of how C\'s low-level nature forces you to think about data representation in ways that higher-level languages hide.',
          relatedTopicIds: [], frequency: 'common' },
      ],
      checkpoints: [
        { id: 'u1-t11-s1-cp1', title: 'Character I/O Mastery', description: 'Verify character I/O understanding.',
          criteria: ['Write a read-until-EOF loop with correct int type', 'Explain why getchar returns int not char', 'Handle the newline-in-buffer problem', 'Know Ctrl+D (Linux) sends EOF'], topicId: 'u1-t11' },
      ],
      revisionCards: [
        { id: 'u1-t11-s1-rc1', front: 'Why must getchar() be stored in int, not char?', back: 'getchar() returns int because it must represent all 256 possible characters PLUS EOF (-1). If stored in unsigned char, EOF (-1) becomes 255 and is indistinguishable from a valid character.', topicId: 'u1-t11', tags: ['getchar', 'int', 'EOF'] },
        { id: 'u1-t11-s1-rc2', front: 'What is EOF and what is its value?', back: 'EOF is a macro (usually -1) from <stdio.h> that signals end of input. On terminal: Ctrl+D (Linux/Mac) or Ctrl+Z (Windows) sends EOF.', topicId: 'u1-t11', tags: ['EOF'] },
        { id: 'u1-t11-s1-rc3', front: 'How do you fix the newline-in-buffer problem after scanf?', back: 'Use while(getchar() != \'\\n\'); to consume the leftover newline before calling getchar(). Or use scanf(" %c", &ch) with a leading space.', topicId: 'u1-t11', tags: ['buffer', 'newline', 'scanf'] },
      ],
    },
  ],

  theoryQuestions: [
    { id: 'u1-t11-q1', type: 'mcq', topicId: 'u1-t11', difficulty: 'beginner',
      question: 'What type should you use to store the return value of getchar()?',
      options: ['char', 'unsigned char', 'int', 'short'],
      correctAnswer: 'int',
      explanation: 'getchar() returns int to accommodate all 256 character values plus EOF (-1). Using char may cause infinite loops if char is unsigned.',
      tags: ['getchar', 'type'] },
    { id: 'u1-t11-q2', type: 'predict-output', topicId: 'u1-t11', difficulty: 'beginner',
      question: 'What does putchar(65) print?',
      correctAnswer: 'A',
      explanation: 'putchar takes an int and prints the corresponding ASCII character. 65 is the ASCII value of \'A\'. Same as printf("%c", 65).',
      tags: ['putchar', 'ASCII'] },
    { id: 'u1-t11-q3', type: 'spot-bug', topicId: 'u1-t11', difficulty: 'intermediate',
      question: 'Find the bug:',
      code: '#include <stdio.h>\nint main(void) {\n    char ch;\n    while ((ch = getchar()) != EOF) {\n        putchar(ch);\n    }\n    return 0;\n}',
      correctAnswer: 'ch is char, not int. If char is unsigned, EOF (-1) wraps to 255 and the loop never ends.',
      explanation: 'getchar() returns int. Storing in char may lose the EOF signal. On platforms where char is unsigned (0-255), -1 becomes 255, which is a valid character. The comparison ch != EOF is then always true.',
      tags: ['getchar', 'char-vs-int', 'infinite-loop'] },
    { id: 'u1-t11-q4', type: 'mcq', topicId: 'u1-t11', difficulty: 'beginner',
      question: 'What is the typical value of EOF in C?',
      options: ['0', '-1', '255', 'NULL'],
      correctAnswer: '-1',
      explanation: 'EOF is typically defined as -1 in <stdio.h>. It is NOT 0 (which is the null character \\0) or 255 (which is a valid unsigned char value).',
      tags: ['EOF', 'value'] },
    { id: 'u1-t11-q5', type: 'true-false', topicId: 'u1-t11', difficulty: 'beginner',
      question: 'getchar() and getc(stdin) are functionally equivalent.',
      correctAnswer: true,
      explanation: 'getchar() is defined as getc(stdin). The only difference is that getc can take any FILE* argument (for reading from files), while getchar always reads from stdin.',
      tags: ['getchar', 'getc'] },
  ],

  programmingProblems: [
    {
      id: 'u1-t11-new-easy',
      title: 'Armstrong Number Check',
      topicId: 'u1-t11',
      difficulty: 'beginner',
      problemStatement: 'Read a 3-digit number. Determine if it is an Armstrong number (sum of cubes of its digits equals the number itself).',
      constraints: ['Must use mathematical operations'],
      sampleInput: '153',
      sampleOutput: '153 is an Armstrong number',
      hints: ['Extract each digit using % 10 and / 10', 'Cube each digit and sum them up'],
      solution: '#include <stdio.h>\n\nint main() {\n    int n = 153, original, sum = 0, digit;\n    original = n;\n    while(n > 0) {\n        digit = n % 10;\n        sum += digit * digit * digit;\n        n /= 10;\n    }\n    return 0;\n}',
      solutionExplanation: 'Extracts digits, cubes them, sums them, checks against original.',
      dryRun: [],
      tags: ['number-theory']
    },
    {
      id: 'u1-t11-new-med',
      title: 'Factorial Calculation',
      topicId: 'u1-t11',
      difficulty: 'intermediate',
      problemStatement: 'Calculate the factorial of a given number n without using recursion.',
      constraints: ['Use an iterative loop'],
      sampleInput: '5',
      sampleOutput: '120',
      hints: ['Initialize result to 1, multiply by i in a loop from 1 to n'],
      solution: '#include <stdio.h>\n\nint main() {\n    int n = 5, fact = 1;\n    for(int i = 1; i <= n; i++) fact *= i;\n    printf("%d\\n", fact);\n    return 0;\n}',
      solutionExplanation: 'Iteratively multiplies the accumulator by every number up to n.',
      dryRun: [],
      tags: ['math', 'loops']
    },
    {
      id: 'u1-t11-new-hard',
      title: 'Diamond Pattern',
      topicId: 'u1-t11',
      difficulty: 'advanced',
      problemStatement: 'Print a diamond pattern of stars for a given number of rows n.',
      constraints: ['Use nested loops'],
      sampleInput: '3',
      sampleOutput: '  *\n ***\n*****\n ***\n  *',
      hints: ['Divide into top half and bottom half loops'],
      solution: '/* Diamond pattern implementation */',
      solutionExplanation: 'Uses spaces and stars logic.',
      dryRun: [],
      tags: ['patterns']
    },
    { id: 'u1-t11-p1', title: 'Character Type Classifier', topicId: 'u1-t11', difficulty: 'beginner',
      problemStatement: 'Read characters one at a time until EOF. Count how many are uppercase, lowercase, digits, and other characters. Print the counts.',
      constraints: ['Use getchar() in a loop', 'Use character comparisons, not library functions'],
      sampleInput: 'Hello World 123!', sampleOutput: 'Upper: 2, Lower: 8, Digits: 3, Other: 3',
      hints: ['Uppercase: c >= \'A\' && c <= \'Z\'', 'Lowercase: c >= \'a\' && c <= \'z\'', 'Digit: c >= \'0\' && c <= \'9\''],
      solution: '#include <stdio.h>\n\nint main(void) {\n    int ch, upper = 0, lower = 0, digits = 0, other = 0;\n    while ((ch = getchar()) != EOF) {\n        if (ch >= \'A\' && ch <= \'Z\') upper++;\n        else if (ch >= \'a\' && ch <= \'z\') lower++;\n        else if (ch >= \'0\' && ch <= \'9\') digits++;\n        else other++;\n    }\n    printf("Upper: %d, Lower: %d, Digits: %d, Other: %d\\n",\n           upper, lower, digits, other);\n    return 0;\n}',
      solutionExplanation: 'Processes input character by character. Uses ASCII ranges to classify each character. No library functions needed — pure character arithmetic.',
      dryRun: [
        { step: 1, line: 5, variables: { ch: 'H (72)', upper: '1' }, output: '', explanation: 'H is uppercase. upper=1.' },
        { step: 2, line: 5, variables: { ch: 'e (101)', lower: '1' }, output: '', explanation: 'e is lowercase. lower=1.' },
      ],
      tags: ['getchar', 'classification'] },
    { id: 'u1-t11-p2', title: 'Caesar Cipher (Shift by 3)', topicId: 'u1-t11', difficulty: 'intermediate',
      problemStatement: 'Read text until EOF. Encrypt each letter by shifting it 3 positions forward in the alphabet (wrapping Z→C). Leave non-letters unchanged.',
      constraints: ['Use getchar/putchar', 'Handle uppercase and lowercase', 'Wrap around: X→A, Y→B, Z→C'],
      sampleInput: 'Hello, World!', sampleOutput: 'Khoor, Zruog!',
      hints: ['For uppercase: ((ch - \'A\' + 3) % 26) + \'A\'', 'Non-letters: putchar unchanged'],
      solution: '#include <stdio.h>\n\nint main(void) {\n    int ch;\n    while ((ch = getchar()) != EOF) {\n        if (ch >= \'A\' && ch <= \'Z\') {\n            putchar((ch - \'A\' + 3) % 26 + \'A\');\n        } else if (ch >= \'a\' && ch <= \'z\') {\n            putchar((ch - \'a\' + 3) % 26 + \'a\');\n        } else {\n            putchar(ch);\n        }\n    }\n    return 0;\n}',
      solutionExplanation: 'Subtract base (\'A\' or \'a\') to get 0-25, add shift, modulo 26 for wraparound, add base back. Classic cipher using character arithmetic.',
      dryRun: [
        { step: 1, line: 5, variables: { ch: 'H (72)' }, output: 'K', explanation: 'H-A=7, 7+3=10, 10%26=10, 10+A=75=K.' },
        { step: 2, line: 5, variables: { ch: 'e (101)' }, output: 'h', explanation: 'e-a=4, 4+3=7, 7%26=7, 7+a=104=h.' },
      ],
      tags: ['getchar', 'putchar', 'cipher'] },
    { id: 'u1-t11-p3', title: 'Line Reverser', topicId: 'u1-t11', difficulty: 'intermediate',
      problemStatement: 'Read a single line of text (until newline) using getchar, store it in a char array, then print it in reverse.',
      constraints: ['Use getchar to read one char at a time', 'Store in array, then print backwards', 'Max line length: 1000'],
      sampleInput: 'Hello', sampleOutput: 'olleH',
      hints: ['Read chars into array until \\n', 'Print from last index to 0'],
      solution: '#include <stdio.h>\n\nint main(void) {\n    char line[1000];\n    int len = 0, ch;\n    while ((ch = getchar()) != \'\\n\' && ch != EOF && len < 999) {\n        line[len++] = ch;\n    }\n    for (int i = len - 1; i >= 0; i--) {\n        putchar(line[i]);\n    }\n    putchar(\'\\n\');\n    return 0;\n}',
      solutionExplanation: 'Read character by character into an array, tracking length. Then iterate backwards, printing each character with putchar.',
      dryRun: [
        { step: 1, line: 6, variables: { len: '5', line: 'Hello' }, output: '', explanation: 'Read H, e, l, l, o into array. len=5.' },
        { step: 2, line: 9, variables: { i: '4' }, output: 'o', explanation: 'Print from index 4 backwards.' },
        { step: 3, line: 9, variables: { i: '0' }, output: 'olleH', explanation: 'All chars printed in reverse.' },
      ],
      tags: ['getchar', 'array', 'reverse'] },
  ],
};
