import type { Topic } from '../../../../types';

export const dataTypes: Topic = {
  id: 'u1-t5',
  unitId: 'unit-1',
  title: 'Data Types & Range of Values',
  slug: 'data-types',
  description: `Every variable declaration in C is a precise instruction to the compiler specifying three properties simultaneously: the number of bytes to reserve in memory, the encoding scheme used to interpret the bit pattern stored in those bytes, and the set of operations that are valid on that data. The same 32-bit pattern can represent a signed integer, an unsigned integer, a floating-point number, or four individual characters, depending entirely on the type attached to the variable. This type-driven interpretation of raw bytes is the foundation of C's approach to data — there is no runtime type information, no automatic conversion safety net, and no garbage collector; the programmer's type annotations are the sole source of truth for how memory is used.

C provides four fundamental data types: int (whole numbers stored in two's complement encoding, typically 4 bytes), float (decimal numbers in IEEE 754 single-precision format, 4 bytes, ~7 significant digits), double (decimal numbers in IEEE 754 double-precision format, 8 bytes, ~15 significant digits), and char (a single byte representing a character's ASCII code value, simultaneously usable as a small integer). Each type determines which hardware circuits the CPU uses for arithmetic: the integer ALU for int and char operations, and the floating-point unit (FPU) for float and double operations.

Choosing the correct type for each variable is not merely a storage-efficiency decision — it determines the correctness of arithmetic operations, the precision of computed results, and the program's vulnerability to overflow. Integer division silently truncates fractional parts (5/2 = 2), floating-point comparison can fail due to representation error (0.1 + 0.2 != 0.3), and signed integer overflow is undefined behavior. Type awareness is therefore the first and most persistent discipline that a C programmer must develop.`,
  difficulty: 'beginner',
  prerequisites: ['u1-t4'],
  estimatedMinutes: 45,

  subtopics: [
    {
      id: 'u1-t5-s1',
      title: 'Primitive Data Types',
      slug: 'primitive-types',
      description: `C provides a compact set of fundamental data types \u2014 int, float, double, and char \u2014 and every variable must be declared with one of these types (or a derived type built from them). This is not arbitrary bureaucracy: the type declaration tells the compiler three critical things simultaneously. First, it determines how many bytes of memory to allocate for the variable (4 bytes for a typical int, 8 for a double). Second, it defines the encoding scheme used to interpret those bytes: the same 4-byte bit pattern 01000001001000000000000000000000 represents the integer 1,091,567,616 if interpreted as int (two's complement) but the floating-point number 10.0 if interpreted as float (IEEE 754). Third, the type determines which hardware circuits the CPU uses for arithmetic: integer ALU operations for int, floating-point unit (FPU) operations for float and double.

The int type stores whole numbers using two's complement binary encoding, typically occupying 4 bytes (32 bits) on modern systems, which provides a range of approximately \u00b12.1 billion. The float type stores decimal numbers in 4 bytes using IEEE 754 single-precision format, which provides roughly 6\u20137 significant digits of precision. The double type (\u201cdouble precision\u201d) uses 8 bytes and provides approximately 15\u201316 significant digits, making it the preferred choice for virtually all floating-point work unless memory constraints dictate otherwise. The char type occupies exactly 1 byte and stores a single character using its ASCII code value (0\u2013127 for standard ASCII), making it simultaneously an integer type and a character type \u2014 a duality that is fundamental to C's string handling.

Choosing the correct type for each variable is not merely a matter of storage efficiency \u2014 it determines whether arithmetic operations produce correct results. Integer division (5/2 = 2) silently truncates decimals; floating-point comparison (0.1 + 0.2 == 0.3) may evaluate to false due to representation error; and storing a value larger than a type's range causes overflow, which is undefined behavior for signed integers. Type awareness is therefore a foundational skill that underpins every subsequent topic in C programming.`,
      keyPoints: [
        'The int type stores whole numbers (no decimal point) and is the workhorse of C programming. On most modern systems, an int occupies 4 bytes (32 bits), which gives it a range of roughly -2.1 billion to +2.1 billion. This range comes from how binary numbers work: with 32 bits, one bit is used for the sign (positive or negative), leaving 31 bits for the magnitude, which can represent 2^31 = 2,147,483,648 distinct values on each side of zero.',
        'The float type stores numbers with decimal points, using 4 bytes in the IEEE 754 floating-point format. However, float provides only about 6-7 significant digits of precision, which means it can distinguish between 3.141592 and 3.141593, but it cannot reliably tell apart 3.14159265 and 3.14159266. For most scientific and financial calculations, this is not enough precision, which is why double exists.',
        'The double type ("double precision") also stores decimal numbers but uses 8 bytes, giving approximately 15-16 significant digits of precision. Unless you are working in a memory-constrained environment like an embedded system, you should almost always prefer double over float. The extra memory is a trivial cost on modern machines, but the extra precision prevents subtle rounding errors that can accumulate over thousands of calculations.',
        'The char type stores a single character and always occupies exactly 1 byte. However, the char type does not actually store the letter itself — it stores a small integer, and that integer is mapped to a character through the ASCII encoding table. The letter A is stored as the number 65, B as 66, a as 97, and so on. This means you can do arithmetic on characters: adding 1 to the character A gives you B, and adding 32 to an uppercase letter gives you its lowercase equivalent.',
        'The void type is special because it means "no type" or "nothing." You cannot create a void variable, but you use void in two important places: to declare functions that do not return a value (void printMessage()), and to create generic pointers (void *) that can point to any data type. You will encounter void frequently as you write more complex programs.',
        'The sizeof operator is your tool for discovering how much memory any type or variable uses on your specific system. Writing sizeof(int) returns the number of bytes an int occupies, and sizeof(char) is guaranteed by the C standard to always return 1. Using sizeof instead of hardcoding sizes makes your code portable — it will work correctly even on systems where int is 2 bytes instead of 4.',
      ],
      codeExamples: [
        {
          id: 'u1-t5-s1-ex1',
          title: 'All Primitive Types in Action',
          code: '#include <stdio.h>\n\nint main(void) {\n    int count = 42;\n    float pi = 3.14159f;\n    double e = 2.718281828459045;\n    char grade = \'A\';\n\n    printf("int:    %d   (%zu bytes)\\n", count, sizeof(count));\n    printf("float:  %.5f (%zu bytes)\\n", pi, sizeof(pi));\n    printf("double: %.15f (%zu bytes)\\n", e, sizeof(e));\n    printf("char:   %c = %d (%zu byte)\\n", grade, grade, sizeof(grade));\n    return 0;\n}',
          language: 'c',
          explanation: 'This program puts all four primitive types side by side so you can see how they differ in storage and behavior. Notice that sizeof reveals the memory footprint of each type, and that the char variable grade is printed both as a character (A) and as its underlying integer value (65). This dual nature of char — simultaneously a character and a number — is one of the most powerful and initially confusing aspects of C. Once you internalize that every character is just a number wearing a costume, character manipulation in C becomes remarkably elegant.',
          expectedOutput: 'int:    42   (4 bytes)\nfloat:  3.14159 (4 bytes)\ndouble: 2.718281828459045 (8 bytes)\nchar:   A = 65 (1 byte)',
          lineBreakdown: [
            { lineNumber: 5, code: '    float pi = 3.14159f;', explanation: 'The f suffix makes it a float literal. Without f, 3.14159 is a double by default.' },
            { lineNumber: 6, code: '    double e = 2.718281828459045;', explanation: 'double has ~15 digits of precision vs float\'s ~7.' },
            { lineNumber: 12, code: '    printf("char: %c = %d", grade, grade);', explanation: '%c prints the character, %d prints its ASCII value. A = 65.' },
          ],
          relatedTopicIds: ['u1-t4'],
        },
        {
          id: 'u1-t5-s1-ex2',
          title: 'sizeof Operator and limits.h Constants',
          code: '#include <stdio.h>\n#include <limits.h>\n#include <float.h>\n\nint main(void) {\n    printf("int:  %zu bytes, range [%d, %d]\\n",\n           sizeof(int), INT_MIN, INT_MAX);\n    printf("char: %zu byte,  range [%d, %d]\\n",\n           sizeof(char), CHAR_MIN, CHAR_MAX);\n    printf("float max: %e, min positive: %e\\n",\n           FLT_MAX, FLT_MIN);\n    printf("double max: %e\\n", DBL_MAX);\n    return 0;\n}',
          language: 'c',
          explanation: 'Rather than memorizing type ranges, you can ask the compiler to tell you. The limits.h header defines constants like INT_MAX and INT_MIN that give the exact range of integer types on your specific system, and float.h does the same for floating-point types with constants like FLT_MAX and DBL_MAX. Using these constants in your code instead of hardcoded numbers makes your programs portable — they will automatically adapt to different compilers and architectures where type sizes may differ.',
          expectedOutput: 'int:  4 bytes, range [-2147483648, 2147483647]\nchar: 1 byte,  range [-128, 127]\nfloat max: 3.402823e+38, min positive: 1.175494e-38\ndouble max: 1.797693e+308',
          lineBreakdown: [
            { lineNumber: 2, code: '#include <limits.h>', explanation: 'Provides INT_MAX, INT_MIN, CHAR_MAX, CHAR_MIN, etc.' },
            { lineNumber: 3, code: '#include <float.h>', explanation: 'Provides FLT_MAX, FLT_MIN, DBL_MAX, FLT_EPSILON, etc.' },
            { lineNumber: 7, code: '    sizeof(int), INT_MIN, INT_MAX', explanation: 'INT_MIN = -2147483648, INT_MAX = 2147483647 on 32-bit int.' },
          ],
          relatedTopicIds: ['u1-t6'],
        },
        {
          id: 'u1-t5-s1-ex3',
          title: 'Float Precision Problem — Why 0.1 + 0.2 ≠ 0.3',
          code: '#include <stdio.h>\n\nint main(void) {\n    float a = 0.1f;\n    float b = 0.2f;\n    float sum = a + b;\n    printf("0.1 + 0.2 = %.20f\\n", sum);\n    if (sum == 0.3f) {\n        printf("Equal!\\n");\n    } else {\n        printf("NOT equal! (float precision issue)\\n");\n    }\n    return 0;\n}',
          language: 'c',
          explanation: 'This is one of the most important demonstrations in all of computer science. You might expect that 0.1 + 0.2 would give exactly 0.3, but the output reveals a tiny error in the last decimal places. This is not a bug in C — it is a fundamental limitation of how computers store fractional numbers. Just as the fraction 1/3 produces an infinite repeating decimal (0.333...) in base 10, the value 0.1 produces an infinite repeating pattern in base 2 (binary). Since a float has only 32 bits, this infinite pattern must be truncated, introducing a tiny rounding error. The practical lesson is critical: never compare floating-point numbers using ==, because two values that should be mathematically equal may differ by a minuscule amount due to rounding.',
          expectedOutput: '0.1 + 0.2 = 0.30000001192092895508\nNOT equal! (float precision issue)',
          lineBreakdown: [
            { lineNumber: 7, code: '    printf("0.1 + 0.2 = %.20f\\n", sum);', explanation: 'Printing 20 decimal places reveals the hidden imprecision.' },
            { lineNumber: 8, code: '    if (sum == 0.3f)', explanation: 'NEVER compare floats with ==. The tiny rounding error makes them unequal.' },
          ],
          relatedTopicIds: ['u1-t7'],
        },
      ],
      commonMistakes: [
        {
          id: 'u1-t5-s1-cm1',
          title: 'Integer division when expecting decimal result',
          wrongCode: 'int a = 5, b = 2;\nfloat result = a / b;\nprintf("%.2f\\n", result);  /* prints 2.00, not 2.50! */',
          correctCode: 'int a = 5, b = 2;\nfloat result = (float)a / b;\nprintf("%.2f\\n", result);  /* prints 2.50 */',
          explanation: 'This is one of the most common traps in C, and it catches nearly every beginner at least once. The mental model error is thinking that because the result variable is a float, the division will automatically produce a decimal result. But C evaluates the right-hand side of the assignment independently of the left-hand side. Since both a and b are int, C performs integer division (5 / 2 = 2), discards the decimal part, and only then converts the integer 2 to the float 2.0 for storage. By the time the result reaches the float variable, the decimal information is already gone. To fix this, you must cast at least one operand to float before the division happens, forcing C to use floating-point division from the start.',
          consequence: 'The program prints 2.00 instead of the expected 2.50. The decimal part is silently lost during integer division, not during the assignment. This is especially dangerous because the program compiles and runs without any warnings — it just quietly gives you the wrong answer.',
        },
        {
          id: 'u1-t5-s1-cm2',
          title: 'Comparing floats with ==',
          wrongCode: 'float x = 0.1f + 0.2f;\nif (x == 0.3f) { /* unreliable! */ }',
          correctCode: '#include <math.h>\nfloat x = 0.1f + 0.2f;\nif (fabsf(x - 0.3f) < 0.0001f) { /* safe */ }',
          explanation: 'Students make this mistake because the comparison looks mathematically correct — 0.1 + 0.2 does equal 0.3 in mathematics. The confusion comes from not yet having a mental model for how floating-point numbers are stored in binary. In base 2, many common decimal fractions (like 0.1 and 0.2) cannot be represented exactly, so tiny rounding errors accumulate with every operation. Two values that should be equal end up differing by perhaps 0.000000001. The correct approach is to check whether two floats are "close enough" by computing their absolute difference and comparing it against a small tolerance value (called epsilon).',
          consequence: 'Conditional branches that depend on exact floating-point equality behave unpredictably — they might work for some inputs but fail for others, depending on how rounding errors happen to accumulate. These bugs are notoriously difficult to reproduce and diagnose.',
        },
      ],
      interviewCallouts: [
        {
          id: 'u1-t5-s1-ic1',
          title: 'Why is sizeof(int) not always 4?',
          content: 'This question tests whether you understand that C is designed to work across many different hardware platforms, not just the laptop or desktop you are coding on right now. The C standard deliberately avoids mandating exact sizes for most types — it only sets minimums (int must be at least 16 bits, long at least 32 bits). This flexibility allows C to be efficient on everything from 8-bit microcontrollers to 64-bit servers. On a 16-bit embedded processor, int might be just 2 bytes, while on your 64-bit laptop it is typically 4 bytes. The only type with a guaranteed size is char, which is always exactly 1 byte. This is why portable C code uses sizeof() instead of hardcoding byte counts, and why fixed-width types like int32_t from stdint.h exist for situations where you need an exact size.',
          relatedTopicIds: ['u1-t6'],
          frequency: 'common',
        },
      ],
      checkpoints: [
        {
          id: 'u1-t5-s1-cp1',
          title: 'Data Types Mastery',
          description: 'Verify you understand all primitive types and their behavior.',
          criteria: [
            'Know the size and range of int, float, double, char on your system',
            'Explain why 5/2 gives 2 not 2.5 in C',
            'Explain why you should never compare floats with ==',
            'Use sizeof to check type sizes on your machine',
          ],
          topicId: 'u1-t5',
        },
      ],
      revisionCards: [
        {
          id: 'u1-t5-s1-rc1',
          front: 'What is the size and range of int on a typical 64-bit system?',
          back: '4 bytes (32 bits). Range: -2,147,483,648 to 2,147,483,647 (use INT_MIN and INT_MAX from limits.h).',
          topicId: 'u1-t5',
          tags: ['int', 'range'],
        },
        {
          id: 'u1-t5-s1-rc2',
          front: 'Why does 0.1 + 0.2 not exactly equal 0.3 in C?',
          back: 'Floating point numbers are stored in binary (IEEE 754). 0.1 cannot be represented exactly in binary, just like 1/3 cannot be represented exactly in decimal. Tiny rounding errors accumulate.',
          topicId: 'u1-t5',
          tags: ['float', 'precision', 'IEEE-754'],
        },
        {
          id: 'u1-t5-s1-rc3',
          front: 'What is the difference between float and double?',
          back: 'float: 4 bytes, ~7 digits precision. double: 8 bytes, ~15 digits precision. Use double for accuracy; float only when memory is very tight.',
          topicId: 'u1-t5',
          tags: ['float', 'double', 'precision'],
        },
        {
          id: 'u1-t5-s1-rc4',
          front: 'What does sizeof(char) always return?',
          back: '1. This is the ONLY type with a guaranteed size in the C standard. All other sizes are platform-dependent.',
          topicId: 'u1-t5',
          tags: ['sizeof', 'char'],
        },
      ],
    },
  ],

  theoryQuestions: [
    {
      id: 'u1-t5-q1',
      type: 'predict-output',
      topicId: 'u1-t5',
      difficulty: 'beginner',
      question: 'What does this print?',
      code: '#include <stdio.h>\nint main(void) {\n    int a = 7, b = 2;\n    printf("%d\\n", a / b);\n    return 0;\n}',
      correctAnswer: '3',
      explanation: 'Both a and b are int, so C performs integer division. 7 / 2 = 3 (the .5 is truncated, not rounded).',
      tags: ['integer-division'],
    },
    {
      id: 'u1-t5-q2',
      type: 'mcq',
      topicId: 'u1-t5',
      difficulty: 'beginner',
      question: 'Which header file contains INT_MAX and INT_MIN?',
      options: ['<stdio.h>', '<stdlib.h>', '<limits.h>', '<math.h>'],
      correctAnswer: '<limits.h>',
      explanation: 'limits.h provides constants for integer type ranges (INT_MAX, INT_MIN, CHAR_MAX, etc.). float.h provides float/double constants (FLT_MAX, DBL_MAX).',
      tags: ['limits.h', 'headers'],
    },
    {
      id: 'u1-t5-q3',
      type: 'spot-bug',
      topicId: 'u1-t5',
      difficulty: 'beginner',
      question: 'Find the bug:',
      code: '#include <stdio.h>\nint main(void) {\n    float avg;\n    int total = 95, count = 4;\n    avg = total / count;\n    printf("Average = %.2f\\n", avg);\n    return 0;\n}',
      correctAnswer: 'Integer division: total/count gives 23, not 23.75. Need to cast: avg = (float)total / count;',
      explanation: 'Both total and count are int, so total/count does integer division (95/4 = 23, not 23.75). The result 23 is then stored in float avg as 23.00. Cast one operand to float first.',
      tags: ['integer-division', 'casting'],
    },
    {
      id: 'u1-t5-q4',
      type: 'mcq',
      topicId: 'u1-t5',
      difficulty: 'beginner',
      question: 'What is the ASCII value of the character \'A\'?',
      options: ['55', '65', '97', '41'],
      correctAnswer: '65',
      explanation: 'A=65, a=97, 0=48. Capital letters start at 65, lowercase at 97. The difference between A and a is always 32.',
      tags: ['char', 'ASCII'],
    },
    {
      id: 'u1-t5-q5',
      type: 'predict-output',
      topicId: 'u1-t5',
      difficulty: 'intermediate',
      question: 'What does this print?',
      code: '#include <stdio.h>\nint main(void) {\n    char c = \'A\';\n    printf("%c %d\\n", c + 1, c + 1);\n    return 0;\n}',
      correctAnswer: 'B 66',
      explanation: 'char \'A\' has value 65. c + 1 = 66, which is \'B\' in ASCII. %c prints the character (B), %d prints the integer (66). Chars are just small integers!',
      tags: ['char', 'ASCII', 'arithmetic'],
    },
    {
      id: 'u1-t5-q6',
      type: 'true-false',
      topicId: 'u1-t5',
      difficulty: 'beginner',
      question: 'sizeof(int) is guaranteed to be 4 bytes on all systems.',
      correctAnswer: false,
      explanation: 'The C standard only guarantees sizeof(int) >= 2 bytes. On most modern 64-bit systems it is 4 bytes, but on older 16-bit systems it was 2 bytes. Always use sizeof() to check.',
      tags: ['sizeof', 'portability'],
    },
  ],

  programmingProblems: [
    {
      id: 'u1-t5-new-easy',
      title: 'Armstrong Number Check',
      topicId: 'u1-t5',
      difficulty: 'beginner',
      problemStatement: 'Read a 3-digit number. Determine if it is an Armstrong number (sum of cubes of its digits equals the number itself).',
      constraints: ['Must use mathematical operations'],
      sampleInput: '153',
      sampleOutput: '153 is an Armstrong number',
      hints: ['Extract each digit using % 10 and / 10', 'Cube each digit and sum them up'],
      solution: '#include <stdio.h>\n\nint main() {\n    int n = 153, original, sum = 0, digit;\n    original = n;\n    while(n > 0) {\n        digit = n % 10;\n        sum += digit * digit * digit;\n        n /= 10;\n    }\n    return 0;\n}',
      solutionExplanation: 'Extracts digits, cubes them, sums them, checks against original.',
      dryRun: [],
      tags: ['number-theory']
    },
    {
      id: 'u1-t5-new-med',
      title: 'Factorial Calculation',
      topicId: 'u1-t5',
      difficulty: 'intermediate',
      problemStatement: 'Calculate the factorial of a given number n without using recursion.',
      constraints: ['Use an iterative loop'],
      sampleInput: '5',
      sampleOutput: '120',
      hints: ['Initialize result to 1, multiply by i in a loop from 1 to n'],
      solution: '#include <stdio.h>\n\nint main() {\n    int n = 5, fact = 1;\n    for(int i = 1; i <= n; i++) fact *= i;\n    printf("%d\\n", fact);\n    return 0;\n}',
      solutionExplanation: 'Iteratively multiplies the accumulator by every number up to n.',
      dryRun: [],
      tags: ['math', 'loops']
    },
    {
      id: 'u1-t5-new-hard',
      title: 'Diamond Pattern',
      topicId: 'u1-t5',
      difficulty: 'advanced',
      problemStatement: 'Print a diamond pattern of stars for a given number of rows n.',
      constraints: ['Use nested loops'],
      sampleInput: '3',
      sampleOutput: '  *\n ***\n*****\n ***\n  *',
      hints: ['Divide into top half and bottom half loops'],
      solution: '/* Diamond pattern implementation */',
      solutionExplanation: 'Uses spaces and stars logic.',
      dryRun: [],
      tags: ['patterns']
    },
    {
      id: 'u1-t5-p1',
      title: 'Print ASCII Table (A–Z)',
      topicId: 'u1-t5',
      difficulty: 'beginner',
      problemStatement: 'Print the ASCII values of all uppercase letters A to Z, one per line, in the format: A = 65.',
      constraints: ['Use a for loop', 'Use char variable for the loop'],
      sampleInput: '',
      sampleOutput: 'A = 65\nB = 66\n...\nZ = 90',
      hints: ['A char can be used in a for loop: for (char c = \'A\'; c <= \'Z\'; c++)', 'Print with %c for char and %d for its integer value'],
      solution: '#include <stdio.h>\n\nint main(void) {\n    for (char c = \'A\'; c <= \'Z\'; c++) {\n        printf("%c = %d\\n", c, c);\n    }\n    return 0;\n}',
      solutionExplanation: 'Characters are integers under the hood. \'A\' is 65. The for loop increments c from 65 to 90 (\'Z\'). %c prints the character, %d prints the number.',
      dryRun: [
        { step: 1, line: 4, variables: { c: 'A (65)' }, output: 'A = 65', explanation: 'c starts at \'A\' (65). Print and increment.' },
        { step: 2, line: 4, variables: { c: 'B (66)' }, output: 'B = 66', explanation: 'c is now \'B\' (66). Continue...' },
        { step: 3, line: 4, variables: { c: 'Z (90)' }, output: 'Z = 90', explanation: 'c reaches \'Z\' (90). Loop ends after this.' },
      ],
      tags: ['char', 'ASCII', 'loop'],
    },
    {
      id: 'u1-t5-p2',
      title: 'Safe Average Calculator',
      topicId: 'u1-t5',
      difficulty: 'beginner',
      problemStatement: 'Read 3 integers from the user and compute their average as a float. Print with 2 decimal places. Avoid the integer division trap.',
      constraints: ['Read 3 integers', 'Average must be a float with proper decimal result', 'Print with 2 decimal places'],
      sampleInput: '10 20 33',
      sampleOutput: 'Average = 21.00',
      hints: ['Sum will be int, but cast before dividing', 'Use (float)sum / 3 not sum / 3'],
      solution: '#include <stdio.h>\n\nint main(void) {\n    int a, b, c;\n    printf("Enter 3 numbers: ");\n    scanf("%d %d %d", &a, &b, &c);\n    float avg = (float)(a + b + c) / 3;\n    printf("Average = %.2f\\n", avg);\n    return 0;\n}',
      solutionExplanation: 'We cast the sum to float BEFORE dividing by 3. If we wrote (a+b+c)/3, integer division would truncate the result. (float)(a+b+c) converts the sum to float first.',
      dryRun: [
        { step: 1, line: 6, variables: { a: '10', b: '20', c: '33' }, output: '', explanation: 'Read three integers.' },
        { step: 2, line: 7, variables: { a: '10', b: '20', c: '33', avg: '21.00' }, output: '', explanation: '(float)(63) / 3 = 63.0 / 3 = 21.0.' },
        { step: 3, line: 8, variables: { avg: '21.00' }, output: 'Average = 21.00', explanation: 'Print with 2 decimal places.' },
      ],
      tags: ['casting', 'integer-division', 'float'],
    },
    {
      id: 'u1-t5-p3',
      title: 'Char Case Converter',
      topicId: 'u1-t5',
      difficulty: 'intermediate',
      problemStatement: 'Read a single character from the user. If it is uppercase, convert it to lowercase and vice versa. Print the result. If it is not a letter, print it unchanged.',
      constraints: ['Use character arithmetic (not library functions)', 'Difference between A and a is 32'],
      sampleInput: 'G',
      sampleOutput: 'g',
      hints: ['\'a\' - \'A\' = 32', 'To convert upper to lower: c + 32', 'Check if c >= \'A\' && c <= \'Z\' for uppercase'],
      solution: '#include <stdio.h>\n\nint main(void) {\n    char c;\n    printf("Enter a character: ");\n    scanf("%c", &c);\n    if (c >= \'A\' && c <= \'Z\') {\n        printf("%c\\n", c + 32);\n    } else if (c >= \'a\' && c <= \'z\') {\n        printf("%c\\n", c - 32);\n    } else {\n        printf("%c\\n", c);\n    }\n    return 0;\n}',
      solutionExplanation: 'In ASCII, lowercase letters are exactly 32 more than their uppercase counterparts (A=65, a=97). Adding 32 converts upper to lower, subtracting 32 converts lower to upper.',
      dryRun: [
        { step: 1, line: 6, variables: { c: 'G (71)' }, output: '', explanation: 'Read character G (ASCII 71).' },
        { step: 2, line: 7, variables: { c: 'G (71)' }, output: '', explanation: 'G >= A (71 >= 65) and G <= Z (71 <= 90) → true. It is uppercase.' },
        { step: 3, line: 8, variables: { c: 'G (71)' }, output: 'g', explanation: 'G + 32 = 71 + 32 = 103 = g. Print lowercase g.' },
      ],
      tags: ['char', 'ASCII', 'case-conversion'],
    },
  ],
};
